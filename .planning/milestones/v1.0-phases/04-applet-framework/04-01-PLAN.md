---
phase: 04-applet-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/host/types.ts
  - src/host/storage.ts
  - src/host/instanceState.ts
  - src/host/theme.ts
  - src/host/aiComplete.ts
  - src/host/open.ts
  - src/host/index.ts
  - src/host/storage.test.ts
  - src/host/aiComplete.test.ts
  - src/host/open.test.ts
  - src/host/index.test.ts
  - src/shell/dockApi.ts
  - src/shell/Dock.tsx
  - src/persistence/workspaceStore.ts
autonomous: true
requirements: [FWK-04, FWK-01]

must_haves:
  truths:
    - "makeHost(instanceId, appletKey) returns an object with exactly five members: storage, ai, open, instanceId, theme"
    - "host.storage.get/set/remove round-trips through a dedicated applets.json store, namespaced sourcerer:<appletKey>:<key>, and never throws on a corrupt read"
    - "host.ai(prompt, {onDelta?}) resolves with the final text on done, rejects with a typed Error on error, and forwards streamed text to onDelta"
    - "host.open(appletKey) focuses an existing panel for that key or opens a fresh instance when none exists"
    - "closing a panel abandons any in-flight host.ai call and removes its instanceState slot"
  artifacts:
    - path: "src/host/types.ts"
      provides: "Host, AppletManifest, AppletModule, ThemeTokens type contracts (leaf module, imported by registry + applets)"
      contains: "interface Host"
    - path: "src/host/index.ts"
      provides: "makeHost(instanceId, appletKey) factory assembling the five host members"
      contains: "export function makeHost"
    - path: "src/host/storage.ts"
      provides: "applets.json LazyStore get/set/remove, namespaced + best-effort"
      contains: "applets.json"
    - path: "src/host/aiComplete.ts"
      provides: "promise+onDelta wrapper over src/host/ai.ts (D-03/D-04/D-05/D-06)"
      contains: "oneshot-"
    - path: "src/host/open.ts"
      provides: "focus-or-open against the live dockview api (D-17)"
      contains: "setActivePanel"
    - path: "src/shell/dockApi.ts"
      provides: "extracted dockApiRef + addAppletToDock + getDockApi so host/open.ts reaches dockview without importing the Dock React component"
      exports: ["dockApiRef", "addAppletToDock", "getDockApi"]
  key_links:
    - from: "src/host/aiComplete.ts"
      to: "src/host/ai.ts"
      via: "composes over the existing ai() event-listener, never re-invokes host_ai directly"
      pattern: "from \"./ai\""
    - from: "src/host/open.ts"
      to: "src/shell/dockApi.ts"
      via: "getDockApi() live handle"
      pattern: "getDockApi"
    - from: "src/host/instanceState.ts"
      to: "src/persistence/workspaceStore.ts"
      via: "get/set/delete of the instanceId-keyed instanceState slot"
      pattern: "instanceState"
---

<objective>
Build the `host` API seam — the single surface applets touch (FWK-04) — as a per-instance factory `makeHost(instanceId, appletKey)` assembling exactly five members: `storage`, `ai`, `open`, `instanceId`, `theme`. This plan finalizes the module signature (FWK-01 type contracts) before any real applet exists, so Phase 5 Notes consumes it unchanged.

Purpose: Everything downstream (registry dispatch, applet modules, catalog) consumes this contract. It is the spine of the phase.
Output: `src/host/types.ts` (leaf type contracts), five host member modules, `src/host/index.ts` (makeHost), an extracted `src/shell/dockApi.ts` handle, workspaceStore instanceState accessors, and unit tests for every host member.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-applet-framework/04-CONTEXT.md
@.planning/phases/04-applet-framework/04-RESEARCH.md
@.planning/phases/04-applet-framework/04-PATTERNS.md

<interfaces>
Existing seams the executor composes over (do not re-implement):

From src/host/ai.ts (Phase 7 — UNCHANGED here):
- `ai(request: {message, sessionId, modes}, onEvent): Promise<void>` — Channel/event client; delivers `text_delta`, `error`, `done` (+ others); NEVER throws (invoke-reject becomes an in-band error+done pair).
- `AssistantEvent` discriminated union on `type`; the 8 event shapes stay PRIVATE to src/host/ (D-03).
- `export const host = { ai, setModes, loadSession }` — module-as-flat-surface idiom to mirror.

From src/shell/Dock.tsx (current, to be refactored):
- `const dockApiRef: { current: DockviewApi | null }` — module-private today; relocate to src/shell/dockApi.ts.
- `addAppletToDock(key, position?)` — opens `${key}:${nanoid()}` panel; relocate to dockApi.ts and re-import in Dock.tsx.
- dockview api exposes `panels: IDockviewPanel[]` and `setActivePanel(panel)` (verified in node_modules/dockview-core .d.ts).

From src/persistence/workspaceStore.ts:
- `WorkspaceRecordV1.instanceState: Record<string, unknown>` — inert slot (D-10), first consumer is Phase 4/5.
- `setSavedLayouts/getSavedLayouts` — the mutate-in-memory-then-persist idiom to mirror for instanceState accessors.
- `scheduleWorkspaceSave()` — the single debounced writer to call after mutating instanceState.
- best-effort try/catch load pattern (loadWorkspaceRecord) to mirror in host/storage.ts.

From @tauri-apps/plugin-store: `LazyStore` — construct `new LazyStore("applets.json")` (D-15); store:default capability is filename-agnostic (verified Phase 3), no Rust/capability change required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type contracts + dockApi extraction + RED test scaffolds</name>
  <files>src/host/types.ts, src/shell/dockApi.ts, src/shell/Dock.tsx, src/host/storage.test.ts, src/host/aiComplete.test.ts, src/host/open.test.ts, src/host/index.test.ts</files>
  <read_first>
    - src/shell/Dock.tsx (current dockApiRef + addAppletToDock definitions to relocate)
    - src/host/ai.ts (the surface aiComplete composes over; the `host` flat-object idiom)
    - src/shell/appletDefs.ts (addAppletToDock reads appletDefs for titles)
    - .planning/phases/04-applet-framework/04-RESEARCH.md (Patterns 2-5, Assumptions A1/A2)
  </read_first>
  <action>
    Create src/host/types.ts as the LEAF type module (no imports of registry or applet code, to avoid an import cycle). Export: `AppletManifest` = { key, glyph, code, title, desc } (all string); `ThemeTokens` = an object of the token keys the handoff `T` object consumes (bg, panel, panel2, line, line2, text, muted, faint, accent, plus font families mono/serif/sans); `Host` = { storage: AppletStorage; ai(prompt: string, opts?: { onDelta?: (text: string) => void }): Promise<string>; open(appletKey: string): void; instanceId: string; theme: ThemeTokens }; `AppletStorage` = { get<T>(key: string, fallback: T): Promise<T>; set(key: string, value: unknown): Promise<void>; remove(key: string): Promise<void> }; `AppletModule` = { manifest: AppletManifest; App: (props: { host: Host }) => JSX.Element }. These five host members are FIXED and exhaustive per FWK-04 — do not add a sixth. Note in a comment that the per-instance state read/write API (D-14) is a reserved additive seam finalized by its first real consumer (Phase 5 Notes); Phase 4 wires only the slot + GC.

    Extract src/shell/dockApi.ts: move `dockApiRef` (the `{ current: DockviewApi | null }` handle) and `addAppletToDock` out of Dock.tsx into this new module; add `getDockApi(): DockviewApi | null` returning `dockApiRef.current`. Then edit src/shell/Dock.tsx to import `dockApiRef` and `addAppletToDock` from ./dockApi instead of defining them locally — Dock.tsx keeps assigning `dockApiRef.current = api` on mount and clearing it on unmount. This is the ONLY Dock.tsx change in this plan; the createComponent signature change and the '+' button belong to later plans. Preserve getDockGroupRects behavior (it can stay in Dock.tsx reading the imported dockApiRef, or move alongside — keep its current call sites working).

    Create the four host test files as RED scaffolds importing the not-yet-built modules, each with `it.todo` or a failing assertion plus a comment `MISSING — implemented in Task 2/3`. This satisfies the Nyquist rule: the test files exist before implementation.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/store/shellStore.test.ts src/shell/Dock.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/host/types.ts exports `Host`, `AppletManifest`, `AppletModule`, `AppletStorage`, `ThemeTokens`; `Host` has exactly the five members storage/ai/open/instanceId/theme (source assertion).
    - src/shell/dockApi.ts exports `dockApiRef`, `addAppletToDock`, `getDockApi`; `grep -c "getDockApi" src/shell/dockApi.ts` >= 1.
    - src/shell/Dock.tsx imports dockApiRef and addAppletToDock from ./dockApi (no local `const dockApiRef` / `function addAppletToDock` definition remains): `grep -c "from \"./dockApi\"" src/shell/Dock.tsx` == 1.
    - `npx tsc --noEmit` passes with the relocated handle.
    - Existing Dock/shell tests still pass (no regression from the extraction).
  </acceptance_criteria>
  <done>Type contracts final, dock api handle extracted and re-imported by Dock.tsx, four RED host test files present, typecheck clean, no test regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: host.storage + instanceState slot + host.theme</name>
  <files>src/host/storage.ts, src/host/instanceState.ts, src/host/theme.ts, src/persistence/workspaceStore.ts, src/host/storage.test.ts</files>
  <read_first>
    - src/persistence/workspaceStore.ts (LazyStore construction, best-effort try/catch load, setSavedLayouts/getSavedLayouts mutate-then-persist idiom, instanceState slot at line ~40)
    - src/host/types.ts (AppletStorage / ThemeTokens shapes from Task 1)
    - src/styles/tokens.css (the token values host.theme must expose — read the color + font custom properties)
  </read_first>
  <behavior>
    - storage.set then storage.get returns the stored value under key `sourcerer:<appletKey>:<key>` (mocked LazyStore).
    - storage.get returns the provided fallback when the underlying get throws (corrupt read never propagates).
    - storage.get returns the fallback when the stored value is null/undefined.
    - storage.remove deletes the namespaced key and saves.
    - two different appletKeys never collide (namespace isolation).
    - theme exposes accent === "#86A38C" and font-family keys matching the handoff T object shape.
  </behavior>
  <action>
    Create src/host/storage.ts exporting `makeAppletStorage(appletKey: string): AppletStorage` backed by a single module-level `new LazyStore("applets.json")` (D-15). Namespace every key as `sourcerer:${appletKey}:${key}` (D-15 literal). get/set/remove return Promises (D-16); get is best-effort (try/catch → return fallback, never throw — mirror workspaceStore.loadWorkspaceRecord); set and remove call `store.set`/`store.delete` then `store.save()`.

    Create src/host/theme.ts exporting a static `theme: ThemeTokens` object literal mirroring src/styles/tokens.css values key-for-key (bg `#0a0a0b`, panel `#131418`, panel2 `#0F1013`, line, line2, text `#e6e4de`, muted, faint, accent `#86A38C`, accentHover `#A3BCA8`, and the three font-family strings). Use a static literal (not getComputedStyle) so it is deterministic under jsdom/tests. Comment that the literal is the discretion-chosen delivery shape (CONTEXT discretion) and must stay in sync with tokens.css.

    Add instanceState accessors to src/persistence/workspaceStore.ts: `getInstanceState(instanceId): unknown`, `setInstanceState(instanceId, value): void` (mutate `inMemory.instanceState` then callers schedule a save), and `deleteInstanceState(instanceId): void` (remove the key). Mirror the setSavedLayouts idiom exactly (spread inMemory, replace slice). Then create src/host/instanceState.ts as a thin re-export/wrapper over those three accessors so host/ consumers import from one place. Do NOT expose instanceState as a sixth `host` member (FWK-04 stays five) — it is used by the dispose GC (Plan 02) and reserved for Phase 5.

    Fill in the storage RED test from Task 1 to GREEN using a mocked LazyStore (mirror how shellStore.test.ts mocks workspaceStore).
  </action>
  <verify>
    <automated>npx vitest run src/host/storage.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "applets.json" src/host/storage.ts` == 1 and keys are namespaced `sourcerer:` + appletKey + key (source assertion).
    - storage.test.ts covers: set→get round-trip, corrupt-read→fallback, null→fallback, remove, cross-appletKey isolation — all passing.
    - src/host/theme.ts exports `theme` with `accent: "#86A38C"` (source assertion).
    - workspaceStore.ts exports getInstanceState/setInstanceState/deleteInstanceState; no new sixth host member added to types.ts.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>host.storage round-trips namespaced + best-effort, host.theme exposes the token set, instanceState slot has get/set/delete accessors, storage tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: host.ai() wrapper + host.open() + makeHost assembly</name>
  <files>src/host/aiComplete.ts, src/host/open.ts, src/host/index.ts, src/host/aiComplete.test.ts, src/host/open.test.ts, src/host/index.test.ts</files>
  <read_first>
    - src/host/ai.ts (the ai() function + AssistantEvent union to compose over; honest-degrade error+done contract)
    - src/shell/dockApi.ts (getDockApi + addAppletToDock from Task 1)
    - src/host/types.ts (Host shape)
    - .planning/phases/04-applet-framework/04-RESEARCH.md (Pattern 3/4, Assumption A2 session-id validity, Pitfalls 1-3)
  </read_first>
  <behavior>
    - aiComplete resolves with the accumulated text once a `done` event arrives.
    - aiComplete rejects with an Error carrying the message when an `error` event arrives (never resolves empty).
    - aiComplete forwards each cumulative text to onDelta on every text_delta.
    - aiComplete generates a sessionId of form `oneshot-<nanoid>` whose first AND last char are alphanumeric (satisfies sidecar SESSION_ID_PATTERN).
    - hostOpen focuses an existing panel whose id splits to appletKey (setActivePanel), and opens a new instance via addAppletToDock when none matches.
    - makeHost returns exactly { storage, ai, open, instanceId, theme }.
  </behavior>
  <action>
    Create src/host/aiComplete.ts exporting `aiComplete(prompt: string, opts?: { onDelta?: (text: string) => void }): Promise<string>` that composes over `ai()` from ./ai (D-03) — never call invoke("host_ai") directly (RESEARCH anti-pattern). Pass `modes: []` (D-05 lean, no tools). Generate `sessionId = "oneshot-" + nanoid()` and guard the LAST character: per RESEARCH Assumption A2 the sidecar SESSION_ID_PATTERN requires first+last char alphanumeric and nanoid's alphabet can end in `_`/`-`; strip a trailing non-alphanumeric char or append a fixed alnum char so validity is guaranteed. Accumulate text on text_delta and call onDelta with the cumulative string; on `error` reject with `new Error(event.message)` guarded by a `settled` flag; on `done` (if not settled) resolve with the accumulated text (D-06 errors reject, never a hang). Add a code comment recording the accepted MVP scope boundaries: one-shot session files accumulate (Pitfall 1), sidecar executes calls sequentially not truly parallel (Pitfall 2), and cancellation is frontend-abandonment only — the sidecar is not aborted mid-turn (Pitfall 3).

    Create src/host/open.ts exporting `hostOpen(appletKey: string): void` implementing D-17 focus-or-open: read `getDockApi()`; if null return; find `api.panels.find(p => p.id.split(":")[0] === appletKey)`; if found call `api.setActivePanel(existing)`, else call `addAppletToDock(appletKey)` (rail's always-new behavior is unchanged — DOCK-04). Keep the `host.open(key, params?)` shape non-precluded: accept only `appletKey` now but do not add anything that would prevent an optional second param later (deferred idea).

    Create src/host/index.ts exporting `makeHost(instanceId: string, appletKey: string): Host` returning { storage: makeAppletStorage(appletKey), ai: aiComplete, open: hostOpen, instanceId, theme }. Mirror the ai.ts flat-object idiom.

    Fill aiComplete.test.ts, open.test.ts, index.test.ts to GREEN (mock ./ai for aiComplete; mock getDockApi/addAppletToDock for open; assert makeHost member set + that instanceId passes through).
  </action>
  <verify>
    <automated>npx vitest run src/host/aiComplete.test.ts src/host/open.test.ts src/host/index.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - aiComplete composes over ./ai: `grep -c "host_ai" src/host/aiComplete.ts` == 0 and `grep -c "from \"./ai\"" src/host/aiComplete.ts` == 1.
    - aiComplete.test.ts asserts: resolve-on-done with accumulated text, reject-on-error, onDelta forwarding, and sessionId last-char is alphanumeric — all passing.
    - open.test.ts asserts focus-or-open both branches (existing → setActivePanel, absent → addAppletToDock) passing.
    - index.test.ts asserts makeHost returns exactly the five keys and instanceId is the passed value.
    - `npx tsc --noEmit` clean; full `npm test` green.
  </acceptance_criteria>
  <done>host.ai promise wrapper, host.open focus-or-open, and makeHost factory complete and unit-tested; the five-member contract is final and green.</done>
</task>

</tasks>

<verification>
- `npm test` full suite green.
- `npx tsc --noEmit` clean across the new host modules.
- The five host members are exhaustive (no sixth member); instanceState is a workspaceStore accessor + reserved seam, not a host member.
- src/host/ai.ts unchanged (Phase 7 client is composed over, never modified).
</verification>

<success_criteria>
makeHost(instanceId, appletKey) delivers storage/ai/open/instanceId/theme; storage round-trips namespaced via applets.json and is crash-safe; ai resolves/rejects/streams per D-03/D-06; open focus-or-opens per D-17; all covered by passing unit tests. FWK-04 seam is implementable-complete and finalized before any real applet consumes it.
</success_criteria>

<output>
Create `.planning/phases/04-applet-framework/04-01-SUMMARY.md` when done.
</output>

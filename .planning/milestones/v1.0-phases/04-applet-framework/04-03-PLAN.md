---
phase: 04-applet-framework
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/applets/Wiki/index.tsx
  - src/applets/Wiki/wikiContent.ts
  - src/applets/Wiki/Wiki.module.css
  - src/applets/Wiki/Wiki.test.tsx
autonomous: true
requirements: [FWK-03]

must_haves:
  truths:
    - "the Wiki applet renders the ported article view: kind eyebrow, serif title, trust chip, lede, claim sections"
    - "selecting a claim opens the provenance inspector (sources, why-this-value-won rule, propagation count)"
    - "an article with disagreeing sources renders the first-class Unresolved block (never a fabricated single answer)"
    - "the edit → dry-run preview → apply → undo flow is reachable and updates the claim value in-place"
    - "the review queue and history tabs render, and a subtle DEMO chip marks the stub as demo"
  artifacts:
    - path: "src/applets/Wiki/index.tsx"
      provides: "the rich Wiki demo module (manifest + App({host})) replacing the templated stub shell"
      contains: "export const manifest"
      min_lines: 120
    - path: "src/applets/Wiki/wikiContent.ts"
      provides: "the hand-authored Ficino/Alberti corpus slice + review-queue data"
      contains: "ficino"
  key_links:
    - from: "src/applets/Wiki/index.tsx"
      to: "src/shell/registry.ts"
      via: "registry already imports Wiki as an override — no registry edit needed"
      pattern: "manifest"
---

<objective>
Port the handoff's rich Wiki demo — "the moat" — into `src/applets/Wiki/index.tsx` as a faithful, pixel-accurate React/JSX module consuming `host` (FWK-03 tier-1). This replaces the Plan-02 templated shell in the Wiki override file; registry.ts is untouched, so this plan runs in parallel with the Library port (Plan 04) and the catalog work (Plan 05).

Purpose: The edit → dry-run → apply → undo flow with provenance and the first-class Unresolved block is the showcase demo that proves "part demo is a feature." Port it faithfully, not a simplified sketch.
Output: the rich Wiki module, its content data, scoped styles, and a component test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-applet-framework/04-CONTEXT.md
@.planning/phases/04-applet-framework/04-PATTERNS.md
@.planning/phases/04-applet-framework/04-UI-SPEC.md

<interfaces>
Source to port (React.createElement → JSX): design-sync-setup-guide/design_handoff_bespoke_rails_shell/wiki.js — full file. Components: TrustChip, DocRow, Provenance, Claim, Unresolved, EditFlow (edit|preview|applied stages), Tab, Wiki (article|review|history views), mountWiki.
Shared store the demo reads: design-sync-setup-guide/design_handoff_bespoke_rails_shell/store.js — `selection.Wiki` (which entity is open) + `reviewCount`.
From src/host/types.ts: `Host`, `AppletManifest`. App signature is `App({ host }: { host: Host })`.
From src/applets/_stub/TemplatedStub.tsx: the DEMO chip treatment to reuse (mono, --color-faint) so the marker is consistent.
Current src/applets/Wiki/index.tsx: the override shell from Plan 02 (manifest + App→TemplatedStub) to REPLACE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port wiki.js content + components to JSX module</name>
  <files>src/applets/Wiki/index.tsx, src/applets/Wiki/wikiContent.ts, src/applets/Wiki/Wiki.module.css</files>
  <read_first>
    - design-sync-setup-guide/design_handoff_bespoke_rails_shell/wiki.js (the full source to port — every component and the ARTICLES/REVIEW data)
    - design-sync-setup-guide/design_handoff_bespoke_rails_shell/store.js (selection.Wiki + reviewCount reads)
    - src/store/shellStore.ts (this repo's vanilla-zustand + useShellStore selector idiom, and railApplet/activeCorpus slices)
    - src/applets/_stub/TemplatedStub.tsx (DEMO chip pattern to reuse)
    - src/host/types.ts (Host / App signature)
  </read_first>
  <action>
    Port wiki.js into src/applets/Wiki/index.tsx as ordinary JSX. Concrete adaptations (CLAUDE.md): replace the `https://esm.sh/react` and `react-dom/client` CDN imports with `import React from "react"`; convert every `h(tag, props, children)` React.createElement call to JSX; drop the `mountWiki(el)` bottom function (dockview + PanelBody own mounting now) and instead export `manifest` (from appletDefs values: key "Wiki", glyph "¶", title "Wiki", desc, code "WIKI") and `App({ host }: { host: Host })` rendering the top-level Wiki component. Preserve ALL behavior: the three tabs (ARTICLE / REVIEW QUEUE / HISTORY), the claim → Provenance inspector split, the Unresolved block for Alberti (birthplace disagreement — keep it first-class, never collapse to one answer), and the full EditFlow modal state machine (edit → preview dry-run diff → applied → undo). Keep React state exactly as ported (view, entity, selClaim, edit, edited, review, toast).

    Move the ARTICLES corpus, FALLBACK, and REVIEW arrays into src/applets/Wiki/wikiContent.ts (data separated from the component). Per RESEARCH Pitfall 4 / UI-SPEC "ported verbatim — do not re-theme": keep the demo's `T` color object as LOCAL hex constants scoped to this module (including amber #D8C69C, amberBg #1E1C17, warm #B8A06E, red #B05A4E) — do NOT promote them into tokens.css. Move the large repeated inline styles into Wiki.module.css where practical, but literal-fidelity to the handoff's look takes priority over exhaustive CSS-module extraction (inline style objects are acceptable where the port is cleaner that way).

    Reconcile the store read: the handoff reads `store.getState().selection.Wiki`. This repo's shellStore has no `selection` slice. Use component-local `useState` seeded to "ficino" for the selected entity (discretion, low risk — a dedicated shell selection store is not required this phase), OR subscribe to shellStore's railApplet if appropriate. Keep the entity-switch effect working with whichever source is chosen. Add the shared DEMO chip (D-12) at the top-left of the Wiki body, matching TemplatedStub's treatment (mono, --color-faint, subtle) — Wiki is still a demo stub.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/shell/registry.test.ts</automated>
    <human-check>Run the app, open Wiki. Confirm: article view renders Ficino with trust chip + claim sections; clicking a claim opens the provenance panel; switching to Alberti shows the Unresolved birthplace block; clicking EDIT on a claim runs edit → PREVIEW CHANGES (red/green diff) → APPLY → UNDO; REVIEW QUEUE and HISTORY tabs render; a subtle DEMO chip is visible and never reads as accent/CTA.</human-check>
  </verify>
  <acceptance_criteria>
    - src/applets/Wiki/index.tsx exports `manifest` (key "Wiki") and `App` taking `{ host }`; no `mountWiki`, no esm.sh imports, no `React.createElement`/`h(` calls remain: `grep -c "esm.sh\|React.createElement\|mountWiki" src/applets/Wiki/index.tsx` == 0.
    - Amber/warm/red literals live only in this module, not added to src/styles/tokens.css: `grep -c "amber\|--color-warn" src/styles/tokens.css` == 0.
    - wikiContent.ts contains the ficino and alberti articles and the REVIEW array (source assertion).
    - `npx tsc --noEmit` clean; registry.test.ts still green (manifest.key === "Wiki").
  </acceptance_criteria>
  <done>Rich Wiki demo ported to JSX consuming host, with local color literals, DEMO chip, and the full article/provenance/unresolved/edit/review/history behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wiki component test — article + edit-flow reachability + DEMO chip</name>
  <files>src/applets/Wiki/Wiki.test.tsx</files>
  <read_first>
    - src/applets/Wiki/index.tsx (the ported module from Task 1)
    - src/shell/PanelBody.test.tsx (this repo's @testing-library/react render+query idiom and host mocking)
  </read_first>
  <action>
    Write src/applets/Wiki/Wiki.test.tsx rendering `<App host={stubHost} />` with a mocked host (host.ai unused by Wiki demo; provide a no-op stub). Assert: the Ficino article title and at least one claim attribute render; a "DEMO" marker is present; clicking a claim's EDIT control advances the EditFlow to the PREVIEW stage (query for the dry-run diff / "APPLY" affordance); the REVIEW QUEUE tab renders review rows. Keep assertions resilient to the ported markup (query by text/role, not brittle class names).
  </action>
  <verify>
    <automated>npx vitest run src/applets/Wiki/Wiki.test.tsx && npm test 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Wiki.test.tsx asserts article render, DEMO marker, edit→preview reachability, and review-queue render — all passing.
    - Full `npm test` green.
  </acceptance_criteria>
  <done>Automated coverage proves the ported Wiki renders and its flagship edit flow + DEMO marker are reachable.</done>
</task>

</tasks>

<verification>
- `npm test` green including the new Wiki test.
- registry.ts untouched (Wiki override module edited in place) — no conflict with Plans 04/05.
- No new tokens added to tokens.css; amber/warm/red stay local literals.
</verification>

<success_criteria>
FWK-03 (Wiki tier-1): the Wiki applet renders the faithful rich demo — article + provenance + first-class Unresolved block + edit→dry-run→apply→undo + review queue + history — carrying a subtle DEMO chip, consuming only `host`.
</success_criteria>

<output>
Create `.planning/phases/04-applet-framework/04-03-SUMMARY.md` when done.
</output>

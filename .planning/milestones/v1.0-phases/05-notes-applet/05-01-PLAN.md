---
phase: 05-notes-applet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/host/instanceState.ts
  - src/applets/Notes/index.tsx
  - src/applets/Notes/store.ts
  - src/applets/Notes/relativeTime.ts
  - src/applets/Notes/Notes.module.css
  - src/applets/Notes/Notes.test.tsx
  - src/shell/registry.ts
autonomous: true
requirements: [NOTE-01]
must_haves:
  truths:
    - "User can click + New Note and a blank note is created and selected at the top of the list"
    - "User can edit a note's title/body; it auto-saves (debounced + on blur) with no Save button and survives relaunch"
    - "Editing a note bumps it to the top of the list (most-recently-updated first)"
    - "User can delete a note via a two-step inline confirm; the next note down is selected, or the empty state when none remain"
    - "Two open Notes tabs mirror each other's edits live via one shared module-level store"
    - "Each tab restores its own last-selected note across relaunch; a missing/GC'd note id falls back silently to the first note or empty state"
    - "Notes renders as a real applet with the APPLET · NOTES · NOTE eyebrow and NO DEMO chip, and passes the applet boundary test"
  artifacts:
    - path: "src/applets/Notes/index.tsx"
      provides: "Notes applet root (manifest + App), two-pane list+editor, CRUD"
      contains: "export const App"
    - path: "src/applets/Notes/store.ts"
      provides: "module-level zustand/vanilla shared notes store, hydrate-once guard, debounced write-through"
      contains: "createStore"
    - path: "src/applets/Notes/relativeTime.ts"
      provides: "hand-written relative timestamp formatter (no date lib)"
      exports: ["relativeTime"]
    - path: "src/applets/Notes/Notes.module.css"
      provides: "two-pane layout + list row/active/delete-hover styling, tokens.css only"
    - path: "src/applets/Notes/Notes.test.tsx"
      provides: "mocked-host component tests for NOTE-01 create/edit/delete"
    - path: "src/host/instanceState.ts"
      provides: "scheduleWorkspaceSave re-export completing the reserved per-instance seam"
      contains: "scheduleWorkspaceSave"
    - path: "src/shell/registry.ts"
      provides: "Notes: NotesModule real registration (FWK-02 stub swap)"
      contains: "Notes: NotesModule"
  key_links:
    - from: "src/applets/Notes/index.tsx"
      to: "host.storage"
      via: "store.ts ensureHydrated / debounced set('notes', notes[])"
      pattern: "storage\\.(get|set)"
    - from: "src/applets/Notes/index.tsx"
      to: "src/host/instanceState.ts"
      via: "getInstanceState/setInstanceState + scheduleWorkspaceSave (selected-note-id)"
      pattern: "setInstanceState"
    - from: "src/shell/registry.ts"
      to: "src/applets/Notes"
      via: "static import + map entry"
      pattern: "Notes: NotesModule"
---

<objective>
Replace the Notes templated stub with the first **real** applet: create / edit / delete
persistent notes stored through `host.storage`, with per-tab selected-note memory through the
reserved `instanceState` seam. This is the NOTE-01 vertical slice and the literal FWK-02 registry
swap that proves a stub can become working functionality.

Purpose: Prove the registry → host → storage half of the framework loop end-to-end (the AI half
is Plan 05-02). Nothing outside `src/applets/Notes/`, the one `registry.ts` line, and the reserved
`instanceState.ts` re-export is touched — the boundary test must stay green.

Output: A working two-pane Notes applet (240px list + editor), a module-level shared notes store,
and the completed per-instance-state seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** researcher, **I want to** create, edit, and delete persistent notes in the Notes applet,
**so that** I can quickly capture scratch thoughts that survive relaunch — proving a demo stub can
be replaced by a real working applet.

<context>
@.planning/PROJECT.md
@.planning/phases/05-notes-applet/05-CONTEXT.md
@.planning/phases/05-notes-applet/05-UI-SPEC.md
@.planning/phases/05-notes-applet/05-RESEARCH.md
@.planning/phases/05-notes-applet/05-PATTERNS.md

<interfaces>
<!-- Contracts the executor consumes unchanged. Full excerpts live in the read_first files below. -->

The five-member Host seam (src/host/types.ts) — do NOT add a sixth member:
- storage: AppletStorage — get<T>(key, fallback): Promise<T>; set(key, value): Promise<void> (never throws, D-16); remove(key): Promise<void>
- ai(prompt, opts?): Promise<string>  (used in Plan 05-02, not here)
- open(appletKey): void
- instanceId: string
- theme: ThemeTokens

Reserved per-instance seam (src/host/instanceState.ts → src/persistence/workspaceStore.ts):
- getInstanceState(instanceId): unknown | undefined
- setInstanceState(instanceId, value): void   (in-memory only)
- deleteInstanceState(instanceId): void
- scheduleWorkspaceSave(): void   ⚠ MUST be added as a re-export (Task 1) — mutate-then-persist idiom pairs setInstanceState with this call (see src/shell/Dock.tsx)

appletDefs.Notes (src/shell/appletDefs.ts): { glyph: "✳", title: "Notes", code: "NOTE", line: "Quick capture — scratch notes that can graduate into the corpus." }

Registration shape (src/applets/Library/index.tsx tail, src/shell/registry.ts): a module exports
`manifest: AppletManifest` + `App: AppletModule["App"]`; registry spreads templatedModules then overrides real keys.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the instanceState seam + author failing NOTE-01 tests (RED)</name>
  <files>src/host/instanceState.ts, src/applets/Notes/Notes.test.tsx</files>
  <read_first>
    - src/host/instanceState.ts (current 4-export re-export list — add the 5th)
    - src/persistence/workspaceStore.ts (confirm scheduleWorkspaceSave is exported there; ~lines 419-424, and the "callers must call scheduleWorkspaceSave() after this to persist" contract ~lines 336-343)
    - src/shell/Dock.tsx (the live mutate-then-persist call site: deleteInstanceState(panel.id) followed by scheduleWorkspaceSave())
    - src/applets/Library/Library.test.tsx (verbatim makeStubHost() idiom + render/screen/fireEvent + afterEach(cleanup) to mirror)
    - .planning/phases/05-notes-applet/05-RESEARCH.md (Pitfall 1; Validation Architecture → Test map for the -t names)
  </read_first>
  <action>
    Add `scheduleWorkspaceSave` to the existing re-export list in src/host/instanceState.ts (it is
    already exported from ../persistence/workspaceStore — this is a one-identifier addition
    completing the seam the file's own header comment reserves for "Phase 5 Notes"). This is the
    ONLY framework-side touch permitted by CONTEXT.md's "framework untouched" scope; it is seam
    completion, not scope creep (RESEARCH Pitfall 1).

    Then create src/applets/Notes/Notes.test.tsx mirroring Library.test.tsx's `makeStubHost()`
    object (copy the full ThemeTokens literal verbatim) and `afterEach(cleanup)`. Import `App` from
    "./index". Write RED tests for NOTE-01 only (summarize tests belong to Plan 05-02, so the wave
    suite stays green there): test names must include the substrings "create", "edit", "delete" so
    the RESEARCH-mapped `-t` selectors resolve. Assert against visible text/roles (not CSS class
    names): (a) "create" — clicking the "+ New Note" control adds a row and lands focus in the
    title input; (b) "edit" — typing in the title input updates the corresponding list row's label
    and calls host.storage.set with key "notes"; (c) "delete" — deleting the selected note selects
    the next note down (or shows "No notes yet" when the list empties). Use a stub host whose
    `storage.get` returns a seeded Note[] and whose `storage.set` is a `vi.fn()` so persistence
    calls are observable. These tests MUST fail now because ./index is still the templated stub.
  </action>
  <verify>
    <automated>npx vitest run src/applets/Notes/Notes.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/host/instanceState.ts export list contains `scheduleWorkspaceSave` (grep: `scheduleWorkspaceSave` present in the `export { ... } from "../persistence/workspaceStore"` block)
    - `npx vitest run` (excluding the new Notes test file) stays green — the re-export adds no regressions
    - src/applets/Notes/Notes.test.tsx exists and defines tests matching `-t "create"`, `-t "edit"`, `-t "delete"`
    - `npx vitest run src/applets/Notes/Notes.test.tsx` exits non-zero (RED) because App is still the templated stub — failures are assertion/element-not-found, not import errors
  </acceptance_criteria>
  <done>scheduleWorkspaceSave re-exported; NOTE-01 test file present and failing for the right reason (stub not yet replaced).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Notes module — shared store + two-pane create/edit/persist slice + registry swap (GREEN create/edit)</name>
  <files>src/applets/Notes/store.ts, src/applets/Notes/index.tsx, src/applets/Notes/Notes.module.css, src/shell/registry.ts</files>
  <read_first>
    - src/store/shellStore.ts (the createStore/useStore vanilla-zustand pattern + seed-then-hydrate discipline to mirror)
    - src/applets/Library/index.tsx (module shape; manifest + App export tail ~lines 871-881; component-local useState precedent; host prop typing)
    - src/applets/_stub/TemplatedStub.tsx (eyebrow format `APPLET · {manifest.title.toUpperCase()} · {manifest.code}` — reuse, omit the .demoChip sibling)
    - src/applets/Library/Library.module.css (host container + eyebrow, DEMO-chip omission)
    - src/shell/LayoutsMenu.module.css (.row/.row.active/.delete hover-reveal/.nameInput classes UI-SPEC mandates mirroring)
    - src/shell/registry.ts (current 2-line-per-applet override pattern)
    - src/styles/tokens.css (every color/space/type value must resolve to an existing custom property — zero new tokens)
    - .planning/phases/05-notes-applet/05-RESEARCH.md (Pattern 1 ensureHydrated guard; Pattern 3 scheduleNotesSave 400ms debounce + flush-on-blur)
    - .planning/phases/05-notes-applet/05-UI-SPEC.md (Layout + Copywriting: "+ New Note", empty-state copy, no Save button)
  </read_first>
  <behavior>
    - Creating a note (addNote) prepends a blank note with a fresh `nanoid()` id, createdAt/updatedAt = Date.now(), empty title/body, and selects it.
    - Editing title or body (updateNote) sets updatedAt = Date.now() and re-sorts notes by updatedAt DESC so the edited note moves to the top (D-01).
    - The shared module-level store is hydrated exactly once from host.storage via a memoized `ensureHydrated(storage)` promise; a second Notes tab reads the already-hydrated store, never re-issuing host.storage.get (D-04 live mirror, Pitfall 3 guard).
    - Every mutation debounces a write-through of the full notes array via host.storage.set("notes", notes) at ~400ms; blurring the title/body flushes immediately (D-05 last-write-wins).
    - Empty list renders the "No notes yet" empty state with the inline "+ New Note" action.
  </behavior>
  <action>
    Create src/applets/Notes/store.ts as a `zustand/vanilla` `createStore` module-level store (mirror
    src/store/shellStore.ts) exporting: a `Note` interface { id, title, body, createdAt, updatedAt },
    the store, a `useNotesStore(selector)` hook (via `useStore` from "zustand"), a memoized
    `ensureHydrated(storage: AppletStorage)` (Pattern 1), actions `addNote()` (returns the new id),
    `updateNote(id, patch)`, `deleteNote(id)` (returns the next-selected id per D-02 — the note that
    takes the deleted visual slot, or last note, or null), all keeping the array sorted updatedAt
    DESC (D-01), and a `scheduleNotesSave(storage, notes)` 400ms debounce (Pattern 3) plus a
    `flushNotesSave(storage, notes)` immediate write for blur. Generate ids with plain `nanoid()` —
    do NOT copy aiComplete.ts's SESSION_ID_PATTERN stripping (RESEARCH anti-pattern).

    Create src/applets/Notes/relativeTime.ts exporting `relativeTime(ms: number): string` returning
    "just now" / "{n}m ago" / "{n}h ago" / "{n}d ago" (RESEARCH Code Examples buckets) — no date lib.

    Create src/applets/Notes/index.tsx: import React hooks, AppletManifest/AppletModule/Host from
    "../../host/types", appletDefs from "../../shell/appletDefs", the store, relativeTime, and
    Notes.module.css. Export `manifest` (key "Notes", pulling glyph/code/title/desc from
    appletDefs.Notes exactly as Library does) and `App = ({ host }) => <Notes host={host} />`.
    Render the eyebrow `APPLET · NOTES · NOTE` via the shared template, WITHOUT the .demoChip div
    (D-12 exception — Notes is real). Build the two-pane body: a fixed ~240px left list pane
    (scrollable rows at --rail-row-h/36px: title label + relativeTime(updatedAt) secondary line;
    selected row gets a 2px --color-accent left border + accent title, mirroring LayoutsMenu .row.active)
    with a "+ New Note" header affordance; and a flex-fill right editor pane with a serif
    --fs-heading borderless title input and a --fs-body --font-sans body textarea, both --color-panel-2
    background on focus (mirroring .nameInput), auto-saving on blur + debounce (no Save button).
    On first mount, call `ensureHydrated(host.storage)`; subscribe via `useNotesStore`. Wire create
    and edit to the store actions; wire persistence to scheduleNotesSave/flushNotesSave with
    host.storage. Render the "No notes yet" empty state + inline "+ New Note" when no note is
    selected or the list is empty (exact copy from UI-SPEC Copywriting). Do NOT implement the
    "Not saved — retrying" state — host.storage.set never rejects (D-16), so that copy is
    unreachable; omit it per RESEARCH Open Question 1 recommendation (a).

    Create src/applets/Notes/Notes.module.css keyed only to tokens.css custom properties (Library
    host container + LayoutsMenu row/active/delete/nameInput excerpts) — zero new sizes/weights/colors.

    Edit src/shell/registry.ts: add `import * as NotesModule from "../applets/Notes";` and add
    `Notes: NotesModule,` to the registry override map after Library (FWK-02 literal swap).

    Do NOT import anything from src/store/** or src/shell/** except appletDefs (boundary test).
  </action>
  <verify>
    <automated>npx vitest run src/applets/Notes/Notes.test.tsx -t "create" && npx vitest run src/applets/Notes/Notes.test.tsx -t "edit" && npx vitest run src/applets/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/applets/Notes/Notes.test.tsx -t "create"` passes (new note appears + title focused)
    - `npx vitest run src/applets/Notes/Notes.test.tsx -t "edit"` passes (edit updates row label and calls host.storage.set with key "notes")
    - `npx vitest run src/applets/boundary.test.ts` stays green (Notes imports only host/** + appletDefs)
    - src/shell/registry.ts contains `Notes: NotesModule`
    - src/applets/Notes/index.tsx eyebrow renders `APPLET · NOTES · NOTE` with no element bearing the DEMO chip text
    - store.ts sorts by updatedAt DESC after updateNote (edited note is notes[0])
    - Notes.module.css contains no hard-coded hex/px literals for tokenized values (values reference `var(--...)`)
  </acceptance_criteria>
  <done>Real Notes applet registered; create + edit + auto-persist working and green; boundary test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete flow, per-tab selected-note memory, empty state (GREEN delete; NOTE-01 complete)</name>
  <files>src/applets/Notes/index.tsx, src/applets/Notes/store.ts, src/applets/Notes/Notes.module.css</files>
  <read_first>
    - src/applets/Notes/index.tsx (current state from Task 2 — before extending)
    - src/applets/Notes/store.ts (the deleteNote return contract from Task 2)
    - src/host/instanceState.ts (getInstanceState/setInstanceState/scheduleWorkspaceSave now all re-exported)
    - src/shell/Dock.tsx (mutate-then-persist idiom: setInstanceState/deleteInstanceState paired with scheduleWorkspaceSave)
    - src/applets/Library/index.tsx (WR-05 single-tracked-timer discipline ~lines 728-741 — reuse for the 3s delete-confirm flip)
    - src/shell/LayoutsMenu.module.css (.delete hover-reveal + :hover danger color)
    - .planning/phases/05-notes-applet/05-CONTEXT.md (D-02, D-06, D-07 exact behavior)
    - .planning/phases/05-notes-applet/05-UI-SPEC.md (Destructive confirmation copy "Delete for real?"; toolbar timestamp)
  </read_first>
  <behavior>
    - Clicking the delete icon flips its label to "Delete for real?" for 3s (single tracked timer, cleared on unmount); a second click within 3s deletes and selects the next note down via store.deleteNote's returned id (D-02); if the list empties, the empty state renders.
    - Selecting a note calls setInstanceState(host.instanceId, { selectedNoteId }) then scheduleWorkspaceSave() (mutate-then-persist).
    - On mount, the selected note is restored from getInstanceState(host.instanceId).selectedNoteId only if that id still exists in the notes array; otherwise it falls back silently to notes[0]?.id ?? null (D-07 — never an error).
    - The editor toolbar shows relativeTime(updatedAt) of the selected note (muted mono).
  </behavior>
  <action>
    Extend src/applets/Notes/index.tsx with the editor-pane toolbar: a muted mono relativeTime label,
    a danger-on-hover delete icon button implementing the two-step confirm (local `confirming` state +
    a `useRef` timer, 3s, cleared on unmount — copy the WR-05 clear-then-reset-timer discipline from
    Library). On confirmed delete, call store.deleteNote(selectedId), take its returned next-selected id,
    set that as the selected note (which also persists via the selection handler), and render the empty
    state when it returns null.

    Add a `selectNote(id)` handler that sets local selected state AND calls
    setInstanceState(host.instanceId, { selectedNoteId: id }) followed by scheduleWorkspaceSave()
    (import all three from "../../host/instanceState" — NOT via the host prop; Host stays five members).
    On first mount, read getInstanceState(host.instanceId) as { selectedNoteId?: string }, and seed the
    selection to that id only if `notes.some(n => n.id === saved.selectedNoteId)`, else notes[0]?.id ?? null
    (D-07 silent fallback). Guard against a GC'd/absent slot (Phase 4 CR-02) — never throw, never show
    an error.

    Add the `.delete` hover-reveal + :hover --color-danger styling to Notes.module.css by mirroring
    LayoutsMenu.module.css (no new tokens).
  </action>
  <verify>
    <automated>npx vitest run src/applets/Notes/Notes.test.tsx -t "delete" && npx vitest run src/applets/Notes</automated>
    <human-check>Build+launch sourcerer.exe detached; create note A, edit it, create note B and select it, quit, relaunch: notes A+B persist and note B is still selected in that tab (NOTE-01 relaunch + D-06). Open a second Notes tab: editing in one mirrors live into the other (D-04).</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/applets/Notes/Notes.test.tsx -t "delete"` passes (delete selects next-down or empty state)
    - `npx vitest run src/applets/Notes` full Notes suite green
    - index.tsx imports `getInstanceState`, `setInstanceState`, `scheduleWorkspaceSave` from "../../host/instanceState" (grep) and does NOT reach into "../../persistence/**"
    - index.tsx delete handler shows "Delete for real?" copy before destructive action (two-step, no modal)
    - src/host/index.test.ts (five-member Host assertion) stays green — no sixth member added
  </acceptance_criteria>
  <done>Delete (D-02), two-step confirm, per-tab selected-note memory (D-06/D-07), and empty state complete; NOTE-01 fully satisfied.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/applets/Notes` — all Notes component tests green (create/edit/delete)
- `npx vitest run src/applets/boundary.test.ts` — host-only seam intact for the new Notes files
- `npx vitest run src/host/index.test.ts` — Host still exactly five members
- `npx vitest run` — full suite green (119 prior tests + new Notes tests), no regressions
- Manual (end-of-phase): notes + per-tab selection survive a real relaunch; two tabs mirror live
</verification>

<success_criteria>
- Notes is a registered real applet (registry.ts) with no DEMO chip
- User can create, edit, delete persistent notes; edits reorder to top; delete selects next-down
- Notes persist via host.storage and survive relaunch; each tab restores its selected note
- boundary + five-member-Host invariants stay green
</success_criteria>

<output>
Create `.planning/phases/05-notes-applet/05-01-SUMMARY.md` when done.
</output>

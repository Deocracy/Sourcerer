---
phase: 03-persistence-layouts
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/persistence/workspaceStore.ts
  - src/persistence/workspaceStore.test.ts
  - src/shell/ResetNotice.tsx
  - src/shell/ResetNotice.module.css
  - src/app/AppShell.tsx
autonomous: true
requirements: [PERS-03]
user_setup: []

must_haves:
  truths:
    - "Corrupt or unmigratable persisted state falls back to the Wiki+Library default without crashing"
    - "Before any destructive reset, the corrupt raw state is copied once to workspace.json.bak (rolling, overwritten each time)"
    - "After a fallback reset, a one-time dismissible inline notice appears and a console warning is logged (not silent)"
    - "A restored layout referencing a missing applet key keeps the pane and renders the generic PanelBody placeholder"
  artifacts:
    - path: "src/shell/ResetNotice.tsx"
      provides: "one-time dismissible inline corrupt-reset banner (D-04)"
      min_lines: 15
    - path: "src/persistence/workspaceStore.ts"
      provides: "corrupt-fallback with .bak copy + resetOccurred signal"
      contains: "workspace.json.bak"
  key_links:
    - from: "src/persistence/workspaceStore.ts"
      to: "workspace.json.bak"
      via: "LazyStore backup write before discard"
      pattern: "workspace\\.json\\.bak"
    - from: "src/app/AppShell.tsx"
      to: "src/shell/ResetNotice.tsx"
      via: "mounted, reads resetOccurred"
      pattern: "ResetNotice"
---

<objective>
Harden the load path (PERS-03): when persisted state is corrupt or from an unmigratable schema version, copy it once to a rolling `workspace.json.bak` (D-08), fall back to the Wiki+Library default (D-05), surface a one-time dismissible inline notice (D-04) plus a console warning — never silently. Prove the missing-applet-key case (D-06) keeps the pane via the existing PanelBody placeholder.

Purpose: turns the Phase-2 "silent try/catch swallow" into PERS-03's visible, recoverable fallback — the RESEARCH anti-pattern this phase must fix.
Output: workspaceStore fallback + .bak + resetOccurred signal, new ResetNotice component mounted in AppShell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-persistence-layouts/03-CONTEXT.md
@.planning/phases/03-persistence-layouts/03-UI-SPEC.md
@.planning/phases/03-persistence-layouts/03-PATTERNS.md
@.planning/phases/03-persistence-layouts/03-02-SUMMARY.md
@src/persistence/workspaceStore.ts
@src/shell/PanelBody.tsx

<interfaces>
<!-- D-04 copy (UI-SPEC Copywriting Contract, verbatim): -->
Notice text: "Workspace was reset after a problem loading your layout."
Dismiss: inline "×", non-blocking inline banner, 13px sans, --color-fg-secondary, 0 border-radius.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corrupt/stale fallback — .bak copy + resetOccurred signal + missing-key coverage</name>
  <files>src/persistence/workspaceStore.ts, src/persistence/workspaceStore.test.ts</files>
  <read_first>
    - src/persistence/workspaceStore.ts (loadWorkspaceRecord try/catch + migrate + DEFAULT_WORKSPACE from 03-01 — extend this exact path)
    - src/persistence/workspaceStore.test.ts (existing corrupt/migrate cases to extend)
    - src/shell/PanelBody.tsx (makeRenderer + PanelBody already fall back to a generic def for unknown keys — D-06 is largely satisfied; the test asserts it)
    - .planning/phases/03-persistence-layouts/03-RESEARCH.md (§Anti-Patterns "Silent reset on corrupt state"; §Architecture diagram — copy corrupt file to .bak BEFORE loading default)
  </read_first>
  <behavior>
    - Corrupt/unmigratable input: the raw corrupt value is written to workspace.json.bak exactly once, then loadWorkspaceRecord resolves to DEFAULT_WORKSPACE, then resetOccurred() reports true.
    - Valid input: no .bak write, resetOccurred() reports false.
    - A record whose dockTree references an applet key absent from appletDefs still loads; makeRenderer(unknownKey) renders PanelBody with the fallback glyph/title, not a throw.
  </behavior>
  <action>
    Extend loadWorkspaceRecord in workspaceStore.ts: in the catch/migrate-null branch, BEFORE returning DEFAULT_WORKSPACE, copy the offending raw value to a second sink `new LazyStore("workspace.json.bak")` (set the same "workspace" key to the raw value + save) — a single rolling backup, overwritten each reset (D-08), wrapped in its own try/catch so a backup failure never blocks the fallback. Set a module-scope `resetOccurred` boolean (default false) to true when the fallback path runs, and export `resetOccurred(): boolean` plus `acknowledgeReset(): void` (sets it back to false, for the notice's dismiss). Also `console.warn("[workspace] reset to default after failing to load persisted layout")`. Extend workspaceStore.test.ts: (e) corrupt input (malformed shape / schemaVersion 0) writes to the mocked workspace.json.bak store exactly once AND resetOccurred() is true afterward; (f) a valid record leaves resetOccurred() false and writes no .bak; (g) a WorkspaceRecordV1 whose dockTree encodes a panel with an unknown applet key loads without throwing (assert loadWorkspaceRecord resolves), and `makeRenderer("NotARealApplet")` returns a renderer whose PanelBody shows the fallback title (import PanelBody/makeRenderer, render, assert the unknown key text appears — D-06).
  </action>
  <verify>
    <automated>npm test -- --run src/persistence/workspaceStore.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "workspace.json.bak" src/persistence/workspaceStore.ts` >= 1.
    - workspaceStore.ts exports `resetOccurred` and `acknowledgeReset`.
    - `grep -c "console.warn" src/persistence/workspaceStore.ts` >= 1.
    - Test cases (e), (f), (g) pass; the .bak write is asserted to happen exactly once on corrupt input and never on valid input.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Corrupt/stale load falls back visibly and recoverably with a rolling backup; missing-key placeholder proven.</done>
</task>

<task type="auto">
  <name>Task 2: ResetNotice component + mount in AppShell</name>
  <files>src/shell/ResetNotice.tsx, src/shell/ResetNotice.module.css, src/app/AppShell.tsx</files>
  <read_first>
    - src/app/AppShell.tsx (where the notice mounts — a non-blocking inline banner sibling of the shell body)
    - src/shell/PanelBody.tsx (nearest structural precedent: CSS-module import + simple functional component)
    - src/shell/TitleBar.module.css (token usage reference: --color-fg-secondary, --color-line, --color-panel, 0 radius)
    - .planning/phases/03-persistence-layouts/03-UI-SPEC.md (§Copywriting Contract corrupt-reset notice row; §Color destructive/× glyph language)
    - src/persistence/workspaceStore.ts (resetOccurred + acknowledgeReset from Task 1)
  </read_first>
  <action>
    Create src/shell/ResetNotice.tsx: a minimal self-contained component (local `useState` for dismissed, no shared toast infra — D-04 is one element, not a system). On mount, read `resetOccurred()` from workspaceStore; if true and not dismissed, render a single-line inline banner with the exact copy "Workspace was reset after a problem loading your layout." (13px sans, `--color-fg-secondary`, `--color-panel` background, 1px `--color-line` border, 0 border-radius) and an inline "×" dismiss button (aria-label "Dismiss notice") that calls `acknowledgeReset()` and sets dismissed true. Render nothing when resetOccurred() is false or after dismiss. Create ResetNotice.module.css with the token-driven styles (no new tokens — reuse tokens.css). Mount `<ResetNotice />` in AppShell.tsx as a non-blocking element above/over the workspace body so it does not shift the title bar or dock layout. Styling metrics are behavior-locked here; exact pixels are the `/gsd-ui-phase 3` pass's concern — match the existing chrome's token language, do not invent an easing/animation.
  </action>
  <verify>
    <automated>npm test -- --run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Workspace was reset after a problem loading your layout." src/shell/ResetNotice.tsx` == 1 (exact copy).
    - ResetNotice renders an element with aria-label "Dismiss notice" that calls acknowledgeReset on click.
    - `grep -c "ResetNotice" src/app/AppShell.tsx` >= 1 (mounted).
    - `grep -c "border-radius" src/shell/ResetNotice.module.css` — if present, value is 0 (radius-0 rule holds; the dropdown/notice are not the outer-window-card exception).
    - `npm run build` exits 0; full `npm test -- --run` green.
  </acceptance_criteria>
  <done>The corrupt-reset notice appears once after a fallback, is dismissible, and is wired to workspaceStore's reset signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corrupt workspace.json → fallback path | Untrusted corrupt input triggers destructive reset; the pre-reset backup is the recovery boundary |
| restored layout with unknown applet key → panel dispatch | A stale/renamed key must not destroy the surrounding layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | DoS | loadWorkspaceRecord fallback | mitigate | visible fallback to DEFAULT_WORKSPACE + console.warn + notice — no silent swallow (RESEARCH anti-pattern closed) |
| T-03-02 | Tampering (self-inflicted) | destructive reset before backup | mitigate | copy corrupt raw to rolling workspace.json.bak BEFORE discarding — manually recoverable (D-08) |
| T-03-03 | DoS (partial) | makeRenderer with missing applet key | accept/mitigate | existing PanelBody generic-placeholder fallback keeps the pane (D-06) — reused verbatim, proven by test (g), no new mitigation built |
</threat_model>

<verification>
- `npm test -- --run` green including corrupt-fallback (.bak once), reset-signal, and missing-key placeholder cases.
- `npm run build` green.
</verification>

<success_criteria>
- Corrupt/stale state → rolling .bak + Wiki+Library default + one-time dismissible notice + console warning; missing applet key keeps its pane. PERS-03 satisfied.
</success_criteria>

<output>
Create `.planning/phases/03-persistence-layouts/03-03-SUMMARY.md` when done.
</output>

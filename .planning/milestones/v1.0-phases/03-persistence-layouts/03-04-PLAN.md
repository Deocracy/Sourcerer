---
phase: 03-persistence-layouts
plan: 04
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/persistence/layouts.ts
  - src/persistence/layouts.test.ts
  - src/shell/LayoutsMenu.tsx
  - src/shell/LayoutsMenu.module.css
  - src/shell/LayoutsMenu.test.tsx
  - src/shell/TitleBar.tsx
autonomous: true
requirements: [PERS-02]
user_setup: []

must_haves:
  truths:
    - "User can save the current workspace as a named layout via the LAYOUTS ▾ dropdown"
    - "User can click a saved layout row to apply it (restores dock tree + rail state)"
    - "User can delete a saved layout row and reset to the Wiki+Library default without touching saved layouts"
    - "Applying/saving/deleting a layout persists through the same unified workspace.json record"
  artifacts:
    - path: "src/persistence/layouts.ts"
      provides: "saveLayout/applyLayout/deleteLayout/resetToDefault over the savedLayouts slice"
      exports: ["saveLayout", "applyLayout", "deleteLayout", "resetToDefault"]
    - path: "src/shell/LayoutsMenu.tsx"
      provides: "LAYOUTS ▾ title-bar dropdown (D-01)"
      min_lines: 40
  key_links:
    - from: "src/shell/TitleBar.tsx"
      to: "src/shell/LayoutsMenu.tsx"
      via: "mounted before RailToggleButtons (D-01)"
      pattern: "LayoutsMenu"
    - from: "src/persistence/layouts.ts"
      to: "src/persistence/workspaceStore.ts"
      via: "mutate savedLayouts slice + scheduleWorkspaceSave"
      pattern: "scheduleWorkspaceSave"
---

<objective>
Deliver the named-layouts vertical slice (PERS-02): a `LAYOUTS ▾` dropdown in the title-bar right cluster (D-01) that lets the user save the current workspace as a named layout, apply a saved layout (restoring the whole dock tree + rail state), delete a layout, and reset to the Wiki+Library default. All operations write through the unified `workspace.json` record's `savedLayouts` slice.

Purpose: PERS-02's user-facing capability — the one net-new UI surface of this phase. Behavior is locked here; pixel-exact chrome is deferred to `/gsd-ui-phase 3`.
Output: layouts.ts service, LayoutsMenu.tsx dropdown + styles + test, TitleBar mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-persistence-layouts/03-CONTEXT.md
@.planning/phases/03-persistence-layouts/03-UI-SPEC.md
@.planning/phases/03-persistence-layouts/03-PATTERNS.md
@.planning/phases/03-persistence-layouts/03-02-SUMMARY.md
@src/persistence/workspaceStore.ts
@src/shell/TitleBar.tsx
@src/shell/DiviChip.tsx
@src/shell/RailToggleButtons.tsx

<interfaces>
<!-- savedLayouts slice shape (from 03-01 WorkspaceRecordV1): -->
savedLayouts: Record<string, { id: string; name: string; record: Omit<WorkspaceRecordV1,'savedLayouts'> }>

<!-- Consumes 03-01/03-02 seams: scheduleWorkspaceSave, loadWorkspaceRecord, DEFAULT_WORKSPACE, -->
<!-- plus the dock-restore + rail-rehydrate machinery built in 03-02 (applyLayout reuses it). -->

<!-- UI-SPEC locked copy (verbatim): trigger "LAYOUTS ▾", save row "Save current…", "Reset", -->
<!-- empty state "No saved layouts yet", name input placeholder "Layout name…". -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: layouts.ts — save/apply/delete/reset over the savedLayouts slice</name>
  <files>src/persistence/layouts.ts, src/persistence/layouts.test.ts</files>
  <read_first>
    - src/persistence/workspaceStore.ts (savedLayouts slice, scheduleWorkspaceSave, loadWorkspaceRecord, DEFAULT_WORKSPACE, the in-memory record accessor + registered state getters)
    - src/store/shellStore.ts (hydrateFromDisk / rail setState pattern from 03-02 — applyLayout rehydrates rail the same way)
    - src/shell/Dock.tsx (how 03-02 restores dockTree via api.fromJSON — applyLayout reuses that dock-restore path)
    - .planning/phases/03-persistence-layouts/03-PATTERNS.md (§layouts.ts — mutate-then-persist two-step idiom)
  </read_first>
  <behavior>
    - saveLayout("Research") snapshots the current record (dockTree + rail + instanceState, minus savedLayouts) into savedLayouts under a fresh nanoid id with name "Research", then persists.
    - applyLayout(id) restores that snapshot: dock tree via the dock-restore seam + rail via shellStore setState, then persists as current.
    - deleteLayout(id) removes the entry from savedLayouts and persists; other layouts untouched.
    - resetToDefault() restores DEFAULT_WORKSPACE (Wiki+Library) as current WITHOUT clearing savedLayouts (D-03), then persists.
  </behavior>
  <action>
    Create src/persistence/layouts.ts exporting saveLayout(name: string), applyLayout(id: string), deleteLayout(id: string), resetToDefault(). Each reads the current in-memory WorkspaceRecordV1 from workspaceStore (add a small `getCurrentRecord()`/`setSavedLayouts()` accessor to workspaceStore.ts if one is not already exported — keep the savedLayouts slice as the single source), mutates the savedLayouts map or triggers a restore, then calls `scheduleWorkspaceSave()` (never writes to disk inline per-action — mirror the mutate-then-persist idiom). Use `nanoid()` for layout ids (already a project dep). saveLayout captures the WHOLE workspace via the registered getDockTree() snapshot + current rail subset (D-02) into `record` (Omit savedLayouts to avoid nesting). applyLayout must restore BOTH sides: call the dock-restore path exposed by Dock (reuse the 03-02 `api.fromJSON` seam — if Dock exposes a `restoreDockTree(json)` function use it; otherwise add a minimal exported restore hook in Dock and register it through workspaceStore, interface-first) AND shellStore.setState the rail subset. resetToDefault reuses DEFAULT_WORKSPACE and MUST NOT delete savedLayouts. Create layouts.test.ts (Vitest, mocked LazyStore + spied scheduleWorkspaceSave) proving all four behaviors above, including that resetToDefault leaves savedLayouts intact.
  </action>
  <verify>
    <automated>npm test -- --run src/persistence/layouts.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - layouts.ts exports saveLayout, applyLayout, deleteLayout, resetToDefault.
    - `grep -c "scheduleWorkspaceSave" src/persistence/layouts.ts` >= 4 (each op persists via the shared writer, not inline disk writes).
    - Test proves resetToDefault does NOT empty savedLayouts (D-03) and applyLayout rehydrates rail state.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The named-layout operations exist and persist through the unified record; ready to wire to the dropdown.</done>
</task>

<task type="auto">
  <name>Task 2: LayoutsMenu.tsx dropdown + TitleBar mount</name>
  <files>src/shell/LayoutsMenu.tsx, src/shell/LayoutsMenu.module.css, src/shell/LayoutsMenu.test.tsx, src/shell/TitleBar.tsx</files>
  <read_first>
    - src/shell/TitleBar.tsx (mount point — LayoutsMenu goes BEFORE `<RailToggleButtons />`, after the drag spacer, per D-01)
    - src/shell/DiviChip.tsx + src/shell/DiviChip.module.css (chip button + store-read idiom to match the trigger styling)
    - src/shell/RailToggleButtons.tsx (multi-action button component precedent + aria-label convention)
    - src/shell/TitleBar.test.tsx (component test idiom — render + getByText/getByLabelText)
    - src/persistence/layouts.ts (saveLayout/applyLayout/deleteLayout/resetToDefault from Task 1)
    - src/persistence/workspaceStore.ts (read savedLayouts for the row list; use useShellStore-style subscription or a small savedLayouts read hook)
    - .planning/phases/03-persistence-layouts/03-UI-SPEC.md (§Interaction Contract — trigger/panel/rows/footer/dismiss/keyboard; §Color; §Copywriting)
  </read_first>
  <action>
    Create src/shell/LayoutsMenu.tsx: a `useState`-driven open/closed dropdown (no popover lib — net-new per D-01). Trigger button labeled `LAYOUTS ▾` (aria-label "Layouts menu"), styled matching DiviChip's chip treatment (mono label, 1px border, accent on hover/open). Panel opens downward right-aligned, `--color-bg` bg, 1px `--color-line` border, 0 border-radius, row height 36px (`--rail-row-h`), max-width ~280px. Render one row per saved layout (most-recently-saved first per UI-SPEC discretion default): label (truncates) + hover-revealed delete "×" (aria-label `Delete layout {name}`) calling deleteLayout(id); the active/applied layout gets the accent left-border + accent label. Clicking a row (not the ×) calls applyLayout(id) and closes. When savedLayouts is empty show "No saved layouts yet". Below a 1px divider render footer rows `Save current…` (opens an inline text input, placeholder `Layout name…`, Enter confirms → saveLayout(name), Escape cancels — no modal) and `Reset` (calls resetToDefault) — both neutral (`--color-faint` → `--color-fg` on hover), NOT accent. Dismiss on click-outside, Escape, or re-click trigger. Keyboard: Arrow up/down move row focus (1px accent focus ring), Enter applies focused row, Delete/Backspace triggers the two-click-confirm delete. Read savedLayouts reactively so new saves appear without a manual refresh. Mount `<LayoutsMenu />` in TitleBar.tsx between `<div className={styles.spacer} data-tauri-drag-region />` and `<RailToggleButtons />` — do NOT add data-tauri-drag-region to it and do NOT disturb the single-drag-region invariant TitleBar.test.tsx asserts. Create LayoutsMenu.module.css (token-driven, no new tokens) and LayoutsMenu.test.tsx proving: trigger renders with its aria-label; clicking it opens the panel; empty state shows "No saved layouts yet"; a Save-current flow calls saveLayout; a saved row's click calls applyLayout and the × calls deleteLayout; Reset calls resetToDefault.
  </action>
  <verify>
    <automated>npm test -- --run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LAYOUTS" src/shell/LayoutsMenu.tsx` >= 1 with the trigger label and aria-label "Layouts menu".
    - `grep -c "LayoutsMenu" src/shell/TitleBar.tsx` == 1 and it appears before `RailToggleButtons` in the JSX.
    - `! grep -q "data-tauri-drag-region" src/shell/LayoutsMenu.tsx` (trigger is not a drag region).
    - Existing TitleBar.test.tsx still passes (single drag region invariant intact).
    - LayoutsMenu.test.tsx proves open/close + save/apply/delete/reset call the layouts.ts functions.
    - `grep -c "No saved layouts yet" src/shell/LayoutsMenu.tsx` == 1 (empty-state copy verbatim).
    - `npm run build` exits 0; full `npm test -- --run` green.
  </acceptance_criteria>
  <done>The LAYOUTS ▾ dropdown is mounted, behavior-complete (save/apply/delete/reset), and persists through the unified record — PERS-02 live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved layout snapshot → applyLayout restore | A saved layout's dockTree/rail is the same untrusted-on-restore JSON as the primary record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | DoS | applyLayout dock restore | mitigate | applyLayout reuses 03-02's try/catch-guarded api.fromJSON restore path; a bad saved layout falls back to default rather than crashing the dock |
| T-03-03 | DoS (partial) | applied layout with missing applet key | accept/mitigate | PanelBody generic placeholder (D-06) keeps the pane — reused, not rebuilt |
</threat_model>

<verification>
- `npm test -- --run` green (layouts.ts ops + LayoutsMenu behavior + unchanged TitleBar invariant).
- `npm run build` green.
</verification>

<success_criteria>
- User can save, apply, delete named layouts and reset to the Wiki+Library default via the LAYOUTS ▾ dropdown; all persist through the unified record. PERS-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/03-persistence-layouts/03-04-SUMMARY.md` when done.
</output>

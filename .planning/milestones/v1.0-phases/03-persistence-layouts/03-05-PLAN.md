---
phase: 03-persistence-layouts
plan: 05
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/persistence/workspaceStore.ts
  - src/persistence/workspaceStore.test.ts
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
autonomous: true
requirements: [PERS-04]
user_setup: []

must_haves:
  truths:
    - "A pending debounced save is force-flushed synchronously before the window actually closes (no unsaved-window race on graceful close)"
    - "The window-close flush is wired via a WindowEvent::CloseRequested arm ADDED alongside the existing Resized arm (maximize-frame-drop fix untouched)"
    - "flushPendingSave() clears the pending debounce timer and writes the latest record immediately"
  artifacts:
    - path: "src/persistence/workspaceStore.ts"
      provides: "flushPendingSave() synchronous force-flush of the pending debounced write"
      contains: "flushPendingSave"
    - path: "src-tauri/src/lib.rs"
      provides: "CloseRequested flush-then-close arm in the existing on_window_event closure"
      contains: "CloseRequested"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "window close flush"
      via: "WindowEvent::CloseRequested + prevent_close + flush + close"
      pattern: "CloseRequested"
    - from: "src/persistence/workspaceStore.ts"
      to: "saveWorkspaceRecord"
      via: "flushPendingSave forces immediate save"
      pattern: "flushPendingSave"
---

<objective>
Close PERS-04's crash-safety gap: ensure the pending debounced workspace write is flushed on window close so a graceful close can never drop the last 300ms of changes. Add a `flushPendingSave()` to the frontend writer and intercept `WindowEvent::CloseRequested` in Rust to flush-then-close — added as a NEW arm alongside the existing maximize-frame-drop `Resized` handler (which must remain byte-for-byte intact per RESEARCH Pitfall 4).

Purpose: PERS-04 — writes debounced and flushed on window close so abrupt termination cannot corrupt/lose the store. The rolling `.bak` (03-03) covers the truly-abrupt-kill case; this plan covers the graceful-close path.
Output: flushPendingSave + its test, CloseRequested arm in lib.rs, any capability needed for the close flush.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-persistence-layouts/03-CONTEXT.md
@.planning/phases/03-persistence-layouts/03-RESEARCH.md
@.planning/phases/03-persistence-layouts/03-PATTERNS.md
@.planning/phases/03-persistence-layouts/03-03-SUMMARY.md
@src/persistence/workspaceStore.ts
@src-tauri/src/lib.rs

<interfaces>
<!-- Existing lib.rs on_window_event closure ALREADY handles WindowEvent::Resized (maximize-frame-drop
     landmine — floating-window-inset-cut.md / 02-06-BUG-maximize-halo.md). ADD a CloseRequested arm;
     DO NOT restructure or remove the Resized arm or the ADJUSTING_MAXIMIZE guard. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: flushPendingSave() — synchronous force-flush of the pending debounce</name>
  <files>src/persistence/workspaceStore.ts, src/persistence/workspaceStore.test.ts</files>
  <read_first>
    - src/persistence/workspaceStore.ts (scheduleWorkspaceSave's 300ms timer + saveWorkspaceRecord — flushPendingSave short-circuits the timer)
    - src/persistence/workspaceStore.test.ts (existing debounce test to extend with a flush case)
    - .planning/phases/03-persistence-layouts/03-RESEARCH.md (§Pitfall 1 — plugin autoSave alone is NOT sufficient for close-flush; one explicit flush authority)
  </read_first>
  <behavior>
    - After a scheduleWorkspaceSave() (timer pending, not yet fired), flushPendingSave() clears the timer and immediately performs one saveWorkspaceRecord with the latest registered getters' values, awaitable to completion.
    - flushPendingSave() with no pending timer still safely resolves (idempotent no-op save or skip).
  </behavior>
  <action>
    Add `flushPendingSave(): Promise<void>` to workspaceStore.ts. It clears the pending debounce timer set by scheduleWorkspaceSave, then builds and awaits one immediate saveWorkspaceRecord from the current registered getDockTree()/getRail() snapshot + in-memory savedLayouts/instanceState/schemaVersion (same record-assembly path scheduleWorkspaceSave uses at flush — factor that assembly into a shared internal helper so the two paths cannot drift). It is the single explicit flush authority (do NOT rely on plugin autoSave for close-safety — RESEARCH Pitfall 1). Expose it in a way the Rust close-hook can trigger: register a Tauri command (or a `getCurrentWindow().onCloseRequested`-driven JS handler) that calls flushPendingSave — decide the JS-vs-Rust split here; the recommended split (RESEARCH Architectural Responsibility Map) is Rust owns the CloseRequested interception + prevent_close, and the actual save is issued from the frontend flushPendingSave via an event/command round-trip OR the store is saved through the Rust store handle. Extend workspaceStore.test.ts: with `vi.useFakeTimers()`, schedule a save, then call flushPendingSave BEFORE advancing timers, and assert exactly one saveWorkspaceRecord ran immediately with the latest values, and that advancing timers afterward does NOT double-write (the timer was cleared).
  </action>
  <verify>
    <automated>npm test -- --run src/persistence/workspaceStore.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - workspaceStore.ts exports `flushPendingSave`.
    - The record-assembly logic is shared between scheduleWorkspaceSave and flushPendingSave (one internal helper — no duplicated assembly).
    - Test proves flushPendingSave writes once immediately and clears the pending timer (no double-write after advancing timers).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The frontend can force a final synchronous flush independent of the debounce timer.</done>
</task>

<task type="auto">
  <name>Task 2: CloseRequested flush-then-close arm in lib.rs</name>
  <files>src-tauri/src/lib.rs, src-tauri/capabilities/default.json</files>
  <read_first>
    - src-tauri/src/lib.rs (the on_window_event closure — Resized arm + ADJUSTING_MAXIMIZE guard; the plugin registration from 03-01; invoke_handler)
    - src-tauri/capabilities/default.json (permissions — may need a close-related permission; core:window:allow-close is already present)
    - .planning/phases/03-persistence-layouts/03-RESEARCH.md (§Pattern 3 + §Open Question 2 — verify CloseRequestApi.prevent_close + the flush-then-close sequence against docs.rs at implementation time; MEDIUM confidence, smoke-test it)
    - MEMORY: floating-window-inset-cut.md context — the Resized arm's maximize-frame-drop fix is a hard-won landmine; do not regress it
  </read_first>
  <action>
    In lib.rs's existing `.on_window_event(|window, event| { ... })` closure, ADD a new arm for `tauri::WindowEvent::CloseRequested { api, .. }` ALONGSIDE the existing `if let tauri::WindowEvent::Resized(_)` arm (use an `else if let` or a separate `if let` — do NOT convert/replace the Resized arm or touch ADJUSTING_MAXIMIZE / set_resizable logic). In the CloseRequested arm: call `api.prevent_close()`, trigger the frontend flush (emit an event the frontend's onCloseRequested/flush handler awaits, or invoke flushPendingSave through the store/command seam chosen in Task 1), and once the flush is issued, actually close the window (`window.close()`), guarding against a re-entrant CloseRequested loop. Verify the exact CloseRequestApi method names and the block_on-vs-spawn sequencing against https://docs.rs/tauri/latest/tauri/struct.CloseRequestApi.html at implementation time (RESEARCH Open Question 2 — this is the one MEDIUM-confidence sequence in the phase; smoke-test that close actually completes and does not hang). Add any store/window permission the flush path requires to capabilities/default.json.
  </action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml && cargo clippy --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
    <human-check>Build the exe (`npm run tauri build` or `cargo run` from src-tauri), open the app, change the layout (dock a panel / reorder rail), close the window immediately, relaunch — the last change is present (flush-on-graceful-close worked). Separately kill the process abruptly mid-session and confirm relaunch either restores or falls back cleanly with the ResetNotice (never a crash). This is the manual-only half of PERS-04 that jsdom/Vitest cannot simulate (real OS CloseRequested + process kill).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "CloseRequested" src-tauri/src/lib.rs` >= 1.
    - The existing `Resized(_)` arm and `ADJUSTING_MAXIMIZE` guard are byte-unchanged (maximize-frame-drop landmine intact — re-maximizing does not reintroduce the halo/grip bug).
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0; clippy reports no new errors.
    - Manual human-check confirms a change made just before a graceful close survives relaunch.
  </acceptance_criteria>
  <done>Graceful window close flushes the pending write before closing; abrupt kill falls back cleanly. PERS-04 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS window-close / abrupt kill → persisted store | The moment between the last debounced change and process exit is the corruption/loss window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering/DoS (self-inflicted) | unflushed write at window close | mitigate | CloseRequested prevent_close + flushPendingSave force-flush before window.close(); rolling .bak (03-03) covers true abrupt kill; 300ms debounce keeps the unsaved window small |
| T-03-05 | DoS | regression of maximize-frame-drop Resized arm | mitigate | CloseRequested added as a separate arm; acceptance gate asserts the Resized/ADJUSTING_MAXIMIZE logic is byte-unchanged |

**Inherent limit (documented, not a gap):** a true OS-level `kill -9` / power loss during an in-flight `.save()` cannot be made fully safe by any userspace mechanism; the rolling `.bak` is the recovery path, not prevention (RESEARCH Pitfall 1).
</threat_model>

<verification>
- `cargo check` + `cargo clippy` green with the CloseRequested arm added and Resized arm intact.
- `npm test -- --run src/persistence/workspaceStore.test.ts` green (flushPendingSave immediate + no-double-write).
- Manual human-check: pre-close change survives relaunch; abrupt kill falls back cleanly.
</verification>

<success_criteria>
- Pending writes flush on graceful window close; the maximize-frame-drop landmine is untouched; abrupt kill degrades to the .bak/default fallback without crashing. PERS-04 satisfied.
</success_criteria>

<output>
Create `.planning/phases/03-persistence-layouts/03-05-SUMMARY.md` when done.
</output>

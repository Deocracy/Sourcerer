---
phase: 07-assistant-harness-core-headless-pi-sidecar-behind-the-host-a
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/host/ai.ts
  - src/assistant/AssistantPanel.tsx
  - src/assistant/AssistantPanel.module.css
  - src/assistant/AssistantPanel.test.tsx
  - src/App.tsx
autonomous: true
requirements: [D-01]

must_haves:
  truths:
    - "D-01: A minimal assistant chat panel renders in the shell — the user types a message and watches a real streamed reply appear token by token."
    - "host.ai() is the ONLY surface the panel uses to reach AI: a typed wrapper over invoke('host_ai') + a Tauri Channel for streaming events (CLAUDE.md: host.ai() is the only AI seam)."
    - "D-06: A backend 'error' event renders as an inline 'assistant unavailable' message in the thread rather than crashing the panel; the composer stays usable."
    - "The mode toggle in the panel calls host.setModes() so the Research/off seam is exercisable from the UI (D-02 plumbing visible)."
  artifacts:
    - path: "src/host/ai.ts"
      provides: "Typed host.ai() + host.setModes() over invoke + Channel"
      contains: "Channel"
    - path: "src/assistant/AssistantPanel.tsx"
      provides: "Composer + streamed message list + mode toggle"
      contains: "host"
    - path: "src/assistant/AssistantPanel.test.tsx"
      provides: "mockIPC streamed-reply + degrade test"
      contains: "mockIPC"
  key_links:
    - from: "src/assistant/AssistantPanel.tsx"
      to: "src/host/ai.ts"
      via: "panel calls host.ai(request, onEvent) and appends deltas to local state"
      pattern: "host\\.ai"
    - from: "src/host/ai.ts"
      to: "invoke('host_ai') + Channel"
      via: "@tauri-apps/api/core invoke + Channel"
      pattern: "invoke"
---

<objective>
Author the frontend `host.ai()` seam (first real implementation of the CLAUDE.md-mandated single AI seam) and a minimal rail chat panel that proves the loop end-to-end: type a message, watch a real streamed reply. The panel keeps chat state local (no Zustand exists yet) and uses only `host.ai()` / `host.setModes()` — never `invoke` directly.

Purpose: D-01 requires real streamed chat visible in the shell, not just a headless backend. This is the accepted minimal coupling to Phase 6 UI; full ASST-01/02/03 polish stays Phase 6.
Output: `src/host/ai.ts` seam + `AssistantPanel` mounted in the app, with a mockIPC test proving streamed replies and honest-degrade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-assistant-harness-core-headless-pi-sidecar-behind-the-host-a/07-CONTEXT.md
@CLAUDE.md
@src/shell/TitleBar.tsx
@src/shell/TitleBar.module.css
@src/shell/WindowControls.test.tsx
@src/styles/tokens.css
@src/App.tsx

<interfaces>
<!-- CANONICAL command contract — MUST match plan 07-03 (Rust) and the 07-01 protocol events. -->
Tauri commands (from plan 07-03):
  invoke("host_ai", { message, sessionId, modes, onEvent: Channel<Event> }): Promise<void>
  invoke("set_modes", { modes }): Promise<void>
Channel Event payloads (verbatim from the sidecar protocol):
  {type:"ready"} | {type:"thinking_delta",id,text} | {type:"text_delta",id,text}
  | {type:"tool_start",id,name} | {type:"tool_end",id,name,ok}
  | {type:"error",id,message} | {type:"done",id}

Design tokens (src/styles/tokens.css): green accent --? (#86A38C / hover #A3BCA8), 0 border-radius
inside, IBM Plex scale, 1px borders. Use var(--...) only — no hardcoded hex/px (CLAUDE.md fidelity).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: host.ai() seam — typed invoke + Channel streaming wrapper</name>
  <files>src/host/ai.ts</files>
  <behavior>
    - host.ai({message, sessionId, modes}, onEvent) invokes "host_ai" with a Channel; each Channel message is forwarded to onEvent; the returned Promise resolves after a "done" event.
    - An "error" event is delivered to onEvent (not thrown) so callers render it inline (D-06).
    - host.setModes(modes) invokes "set_modes".
    - Event payloads are typed as a discriminated union on `type`.
  </behavior>
  <read_first>
    - src/shell/WindowControls.test.tsx (the `@tauri-apps/api/mocks` mockIPC pattern and the real `@tauri-apps/api/core` invoke import path it mirrors)
    - .planning/phases/.../07-PATTERNS.md (src/host/ai.ts section — no in-repo analog; author against Tauri 2 Channel API)
    - Tauri 2 official docs: Channel class in `@tauri-apps/api/core` (canonical_refs) — WindowControls uses fire-and-forget invoke, NOT a good streaming template beyond the import path
  </read_first>
  <action>
    Create `src/host/ai.ts` exporting a typed `AssistantEvent` discriminated union (matching the `<interfaces>` payloads) and a `host` object (or named exports) with `ai(request, onEvent)` and `setModes(modes)`. `ai()` constructs a `Channel<AssistantEvent>` from `@tauri-apps/api/core`, assigns `channel.onmessage = onEvent`, and calls `invoke("host_ai", { message, sessionId, modes, onEvent: channel })`. Resolve the returned Promise when a `done` event arrives (track it inside the onmessage handler or rely on the command's own resolution — but expose completion to the caller). `setModes(modes)` calls `invoke("set_modes", { modes })`. Do NOT throw on an `error` event — deliver it through `onEvent` so the panel renders it inline (D-06). This module is the ONLY place `invoke` for AI is called; the panel imports from here, honoring "host.ai() is the only AI seam."
  </action>
  <verify>
    <automated>npm test -- src/assistant/AssistantPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ai.ts` imports `invoke` and `Channel` from `@tauri-apps/api/core`; the panel does not import `invoke` directly (grep: `invoke` appears only in `src/host/`).
    - `AssistantEvent` is a discriminated union on `type` covering all seven event types.
    - `ai()` forwards Channel messages to `onEvent` and does not throw on `error`.
  </acceptance_criteria>
  <done>The single AI seam exists and streams typed events; the panel reaches AI only through it.</done>
</task>

<task type="auto">
  <name>Task 2: Minimal AssistantPanel (composer + streamed message list + mode toggle) mounted in the app</name>
  <files>src/assistant/AssistantPanel.tsx, src/assistant/AssistantPanel.module.css, src/App.tsx</files>
  <read_first>
    - src/shell/TitleBar.tsx (plain function component + co-located CSS Module + named export shape to copy)
    - src/styles/tokens.css (the exact token variable names for green accent / spacing / type — use var(--...) only)
    - src/host/ai.ts (the seam from Task 1)
    - .planning/phases/.../07-CONTEXT.md (Claude's Discretion: minimal rail chat UI shape; whether to render or suppress thinking_delta)
    - CLAUDE.md (0 border-radius inside, IBM Plex, green #86A38C — via tokens, no hardcoded values)
  </read_first>
  <action>
    Create `AssistantPanel.tsx` as a plain functional component with local `useState` for the message list and composer text (no Zustand exists yet — keep chat state local per the discretion note). Render: a message thread (user + assistant messages), a composer (textarea + send; support ⌘↵/Ctrl+Enter to send per the ASST spirit but keep it minimal), and a Research mode toggle. On send: append the user message, create an in-progress assistant message, call `host.ai({message, sessionId, modes}, onEvent)` where `onEvent` appends `text_delta` text to the in-progress assistant message, optionally renders `tool_start`/`tool_end` as a subtle "searching wiki…" affordance, renders an `error` event as an inline unavailable notice (D-06), and finalizes on `done`. Decide thinking_delta rendering per discretion (default: suppress in the minimal panel). The Research toggle calls `host.setModes(["research"])` / `host.setModes([])` and reflects active state. Use a stable `sessionId` (e.g. a constant or nanoid on mount) so turns land in one file-backed session (D-09 on the sidecar side).

    Style `AssistantPanel.module.css` strictly from tokens (green accent, 0 inner radius, 1px borders, IBM Plex) mirroring TitleBar.module.css conventions. Mount `<AssistantPanel />` into `src/App.tsx` alongside `<AppShell />` (a minimal right/rail placement is fine — full rail integration is Phase 2/6; here it just needs to render and stream). Do not hardcode any hex/px that a token exists for.
  </action>
  <verify>
    <automated>npm test -- src/assistant/AssistantPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the panel imports from `src/host/ai.ts` and never from `@tauri-apps/api/core` directly.
    - Source assertion: `.module.css` uses `var(--...)` tokens; grep finds no raw `#86A38C`/hardcoded hex where a token exists.
    - `App.tsx` renders `<AssistantPanel />`.
    - Behavior (Task 3 test): typing + send produces streamed assistant text in the thread.
  </acceptance_criteria>
  <done>A minimal chat panel renders in the app, streams replies via host.ai(), toggles Research mode, and shows an inline notice on backend error.</done>
</task>

<task type="auto">
  <name>Task 3: AssistantPanel IPC-mock test (streamed reply + honest-degrade)</name>
  <files>src/assistant/AssistantPanel.test.tsx</files>
  <read_first>
    - src/shell/WindowControls.test.tsx (mockIPC/mockWindows/clearMocks harness to copy; beforeEach/afterEach cleanup)
    - .planning/phases/.../07-PATTERNS.md (note: no existing test mocks a Channel/streaming command — extend the pattern; Tauri 2 mockIPC can drive the Channel by invoking the passed channel's onmessage)
    - src/assistant/AssistantPanel.tsx, src/host/ai.ts (units under test)
  </read_first>
  <action>
    Write a Vitest + Testing Library test using `mockIPC` from `@tauri-apps/api/mocks`. Mock the `host_ai` command handler to drive the streamed response: inspect the passed `onEvent` Channel argument and emit a sequence of events (`text_delta` × N then `done`) by calling the channel's message callback (Tauri 2's mockIPC delivers Channel messages via the serialized `onmessage` id — follow the official mocks docs pattern for Channels; if direct Channel invocation is awkward, assert against the `host.ai` seam by mocking at that boundary instead). Assert: (1) after typing a message and sending, the streamed assistant text appears in the thread; (2) an `error` event renders an inline "unavailable" notice and the composer remains enabled (D-06); (3) toggling Research invokes `set_modes` with `["research"]`. Use the WindowControls beforeEach/afterEach clearMocks/cleanup discipline.
  </action>
  <verify>
    <automated>npm test -- src/assistant/AssistantPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts streamed text_delta events render in order into one assistant message.
    - Test asserts an error event yields an inline notice AND composer stays usable (no crash).
    - Test asserts the Research toggle drives `set_modes` with `["research"]`.
    - `npm test` for this file is green.
  </acceptance_criteria>
  <done>Automated proof that the panel streams replies and degrades honestly, using the repo's mockIPC convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered assistant text ← model/tool output | LLM + Databasise content is displayed in the panel (injection surfaces at render) |
| Panel → host.ai() → Rust | User message text crosses the webview→host boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Tampering (XSS / injected markup) | Rendering streamed assistant/tool text | mitigate | Render deltas as React text children (auto-escaped); do NOT use dangerouslySetInnerHTML on model/tool output |
| T-07-15 | Elevation of Privilege | Panel bypassing the host.ai() seam to call invoke directly | mitigate | All AI IPC confined to `src/host/ai.ts`; test/grep gate asserts `invoke` for AI appears only under `src/host/` (CLAUDE.md single-seam rule) |
| T-07-16 | Denial of Service | A never-terminating stream freezing the composer | mitigate | `done`/`error` finalizes the in-progress message and re-enables the composer; the Rust command's timeout guarantees a terminal event (plan 07-03 T-07-12) |
| T-07-17 | Information Disclosure | Rendering raw tool payloads (may contain large/sensitive corpus text) | accept | Local single-user research app; tool text already truncated at 4000 chars in the sidecar (plan 07-02) |
</threat_model>

<verification>
- `npm test -- src/assistant/AssistantPanel.test.tsx` green (streamed reply + degrade + mode toggle).
- Grep proves AI `invoke` usage is confined to `src/host/`; `.module.css` uses tokens only.
- `App.tsx` mounts `<AssistantPanel />`.
</verification>

<success_criteria>
- A minimal chat panel streams a real reply token-by-token through host.ai() (D-01).
- host.ai() is the sole AI seam the panel touches (CLAUDE.md).
- Backend-error events render inline without crashing the panel (D-06); Research toggle exercises the mode seam.
</success_criteria>

<output>
Create `.planning/phases/07-assistant-harness-core-headless-pi-sidecar-behind-the-host-a/07-04-SUMMARY.md` when done.
</output>

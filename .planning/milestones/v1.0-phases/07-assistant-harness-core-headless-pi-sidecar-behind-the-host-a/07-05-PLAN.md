---
phase: 07-assistant-harness-core-headless-pi-sidecar-behind-the-host-a
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04"]
files_modified: []
autonomous: false
requirements: [D-01, D-03, D-06, D-09]

must_haves:
  truths:
    - "D-01: With the app running, the user types a message in the assistant panel and watches a real Pi reply stream in token by token."
    - "D-03: With Databasise running, a Research-mode question triggers a wiki_resolve/kb_query tool call and the reply is grounded in the user's corpus."
    - "D-06: Killing Databasise mid-session yields an honest 'wiki unavailable' result and plain chat keeps working; the app never crashes."
    - "D-09: Restarting the app reloads the prior session's turns from the file-backed JSONL store."
  artifacts: []
  key_links:
    - from: "webview assistant panel"
      to: "Databasise corpus"
      via: "host.ai() → Rust host_ai → Node sidecar → /openapi.json tools → 127.0.0.1:9621"
      pattern: "end-to-end"
---

<objective>
Prove the whole spine live: webview → `host.ai()` → Rust → Node Pi sidecar → Databasise REST → streamed, grounded reply. This is the one manual gate the automated per-plan tests cannot cover because it needs the real Cerebras key, the real sidecar process, and the live Databasise server. It verifies D-01 (real streamed chat), D-03 (Research tool grounding), D-06 (honest degrade), and D-09 (history survives restart).

Purpose: The automated tests in 07-01..07-04 mock IPC and the model. Only a live run proves the integration.
Output: A human sign-off (or a concrete defect list) on the four end-to-end truths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-assistant-harness-core-headless-pi-sidecar-behind-the-host-a/07-CONTEXT.md
@.claude/skills/spike-findings-sourcerer/references/databasise-tools.md
@.planning/phases/07-assistant-harness-core-headless-pi-sidecar-behind-the-host-a/07-01-SUMMARY.md
@.planning/phases/07-assistant-harness-core-headless-pi-sidecar-behind-the-host-a/07-02-SUMMARY.md
@.planning/phases/07-assistant-harness-core-headless-pi-sidecar-behind-the-host-a/07-03-SUMMARY.md
@.planning/phases/07-assistant-harness-core-headless-pi-sidecar-behind-the-host-a/07-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live end-to-end verification — streamed chat, Research grounding, degrade, history</name>
  <read_first>
    - .claude/skills/spike-findings-sourcerer/references/databasise-tools.md (server launch line + WORKING_DIR=./rag_storage — the populated 112-entity store, NOT the empty default)
    - .planning/phases/.../07-CONTEXT.md (D-06 assume-running, D-08 user-owned key, D-09 history)
  </read_first>
  <what-built>
    The full harness: Node Pi sidecar (lean prompt, mode registry, file-backed sessions, Databasise Research tools) spawned and owned by Rust, exposed to the webview through the `host_ai`/`set_modes` Tauri commands and streamed to a minimal assistant panel over a Channel. All per-plan automated tests are green.
  </what-built>
  <how-to-verify>
    PRE-REQ (user, one-time): put a real `CEREBRAS_API_KEY` in `sidecar/.env` (copy from Databasise `runtime/.env` `LLM_BINDING_API_KEY`, per D-08). Start Databasise against the POPULATED store from `D:\Vibe Coding\Databasise\runtime`:
      WORKING_DIR=./rag_storage ../sourcerer-venv/Scripts/python.exe ../sourcerer-lightrag/sourcerer.py
    Confirm it answers at http://127.0.0.1:9621/openapi.json.

    Then launch the app (per CLAUDE.md landmine, verify `cargo run` from src-tauri vs `cargo tauri dev` — use whichever launches this scaffold reliably) and check, in order:
      1. STREAMED CHAT (D-01): type "hello, who are you?" and confirm the reply streams in token-by-token (not one blocking chunk).
      2. RESEARCH GROUNDING (D-03): ensure Research mode is ON, ask a question answerable only from your Databasise corpus (e.g. about a specific entity in the 112-entity wiki). Confirm a wiki/kb tool call fires (the "searching wiki…" affordance or logs) and the answer reflects corpus content, not generic LLM knowledge.
      3. HONEST DEGRADE (D-06): stop the Databasise server, ask another Research question. Confirm the reply says the wiki is unavailable AND plain chat still works (ask a general question and get a normal streamed reply). The app must NOT crash or hang.
      4. HISTORY SURVIVES RESTART (D-09): note the current conversation, fully close and relaunch the app, reopen the assistant, and confirm the prior turns reload from disk.
  </how-to-verify>
  <acceptance_criteria>
    - All four checks pass, OR the human returns a specific defect list (which check, observed vs expected). No subjective "looks fine" — each check has a concrete pass condition above.
    - Streamed (not blocking) rendering confirmed for D-01.
    - A real tool call against Databasise confirmed for D-03 (corpus-grounded answer).
    - Degrade confirmed non-fatal for D-06; reload confirmed for D-09.
  </acceptance_criteria>
  <verify>
    <human-check>The four numbered checks in how-to-verify, each with its explicit pass condition.</human-check>
  </verify>
  <resume-signal>Type "approved" if all four pass, or list the failing check(s) with observed behavior for gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full live path webview→Rust→sidecar→Databasise | First live exercise of every boundary the phase introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-18 | Denial of Service | Live degrade path (Databasise killed) hanging the app | mitigate | Check 3 explicitly verifies the D-06 degrade is non-fatal and non-hanging in a real run — this is the acceptance gate for the mitigations designed in plans 07-02/07-03 |
| T-07-19 | Information Disclosure | Real API key present in sidecar/.env during the live run | accept | User-owned local key in a gitignored file (D-08); verification does not print or transmit it |
| T-07-SC | Tampering | Live third-party Pi package execution under a real key | mitigate | Package legitimacy already gated in plan 07-01 Task 1 before install; this run is the first execution under a real key against those vetted versions |
</threat_model>

<verification>
- Human confirms all four live checks (D-01/D-03/D-06/D-09) pass, or files a defect list for `/gsd-plan-phase 7 --gaps`.
</verification>

<success_criteria>
- The assistant panel streams a real, grounded, restart-durable conversation end-to-end, and degrades honestly when Databasise is down — the phase goal proven live.
</success_criteria>

<output>
Create `.planning/phases/07-assistant-harness-core-headless-pi-sidecar-behind-the-host-a/07-05-SUMMARY.md` when done.
</output>

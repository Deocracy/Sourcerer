---
phase: 01-shell-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src-tauri/Cargo.toml
  - src/main.tsx
  - src/App.tsx
  - index.html
  - src/styles/tokens.css
  - src/fonts.ts
  - src/app/AppShell.tsx
  - src/app/AppShell.module.css
  - vitest.config.ts
  - src/shell/TitleBar.test.tsx
  - src/shell/WindowControls.test.tsx
autonomous: true
requirements: [SHELL-03, SHELL-04, SHELL-01]

must_haves:
  truths:
    - "D-03: react and react-dom are pinned to 18.2.x and @types/react{,-dom} to ^18 in package.json (scaffold's React 19 default overridden)"
    - "D-03: @fontsource/ibm-plex-{sans,mono,serif} are imported via per-weight subpaths only (Sans 400/500/600, Mono 400/500, Serif 400 + 400-italic); no all-weights index import"
    - "D-01: tokens.css exposes the full UI-SPEC token set (all colors, chrome metrics, spacing scale, type roles) as CSS custom properties, and AppShell establishes grid-template-rows 34px 1fr"
    - "D-02: no Zustand or state store is introduced in Phase 1 (package.json has no zustand dependency)"
    - "tauri.conf.json main window has decorations:false AND shadow:false"
    - "capabilities/default.json grants only core:window:allow-minimize/allow-toggle-maximize/allow-close/allow-start-dragging scoped to windows:[main]"
    - "Failing (RED) Vitest specs exist for the title bar and window controls before their components are written"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "Frameless, shadowless main window config"
      contains: "\"decorations\": false"
    - path: "src-tauri/capabilities/default.json"
      provides: "Least-privilege window capability grant"
      contains: "core:window:allow-start-dragging"
    - path: "src/styles/tokens.css"
      provides: "Full UI-SPEC design-token custom properties"
      min_lines: 30
    - path: "src/fonts.ts"
      provides: "Per-weight local IBM Plex font imports"
      contains: "@fontsource/ibm-plex-mono/400.css"
    - path: "src/app/AppShell.tsx"
      provides: "App-shell CSS grid (34px title-row / 1fr body)"
      min_lines: 8
    - path: "vitest.config.ts"
      provides: "jsdom test environment"
      contains: "jsdom"
    - path: "src/shell/TitleBar.test.tsx"
      provides: "RED spec for SHELL-01"
      contains: "TitleBar"
  key_links:
    - from: "src/main.tsx"
      to: "src/fonts.ts"
      via: "import to trigger @fontsource bundling"
      pattern: "import.*fonts"
    - from: "src/main.tsx"
      to: "src/styles/tokens.css"
      via: "global CSS import"
      pattern: "tokens\\.css"
    - from: "src/app/AppShell.module.css"
      to: "tokens.css custom properties"
      via: "var(--...) references for grid + colors"
      pattern: "var\\(--"
---

<objective>
Scaffold the Sourcerer Tauri 2 + React 18 + Vite + TS application from scratch (greenfield repo — no package.json / src-tauri yet), configure the frameless shadowless window with a least-privilege window capability, seed the shared foundation (full tokens.css + app-shell grid + locally-bundled fonts), and stand up the Vitest harness with RED specs for the title bar and window controls that plan 01-02 will turn green.

Purpose: This is the Walking Skeleton's bones. It proves the app compiles and launches as a frameless window and lays the token/font/grid foundation that Phases 2-6 mount into — without any title-bar UI logic yet (that is plan 01-02).
Output: A runnable, empty frameless shell; full design-token stylesheet; bundled fonts; failing test specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-shell-foundation/01-CONTEXT.md
@.planning/phases/01-shell-foundation/01-RESEARCH.md
@.planning/phases/01-shell-foundation/01-UI-SPEC.md
@.planning/phases/01-shell-foundation/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold app, pin React 18, configure frameless shadowless window + capability</name>
  <read_first>
    - .planning/phases/01-shell-foundation/01-RESEARCH.md (Standard Stack verified versions; Pattern 1 config; Pitfall 1 shadow; Pitfall 2 React18 pin; Code Examples capability file)
    - .planning/phases/01-shell-foundation/01-CONTEXT.md (D-03 scaffold reconciliation)
    - CLAUDE.md (Technology Stack table; What NOT to Use; Version Compatibility)
  </read_first>
  <action>
    Scaffold into the repo root with `npm create tauri-app@latest` (React + TypeScript template; app name "sourcerer", window title "Sourcerer"). Re-verify the scaffolder's resolved Vite / @vitejs/plugin-react / TypeScript pins from the generated package-lock.json at execution time (RESEARCH notes these drift — do NOT hardcode Vite 8). Immediately override React: install react@18.2.0 and react-dom@18.2.0, and dev-install @types/react@^18 and @types/react-dom@^18, so all four packages sit on the 18.x line (Pitfall 2). Confirm with `npm ls react react-dom @types/react @types/react-dom` that no React 19 remains. Strip the demo UI: remove the sample greet command from src-tauri (lib.rs), delete the demo App.tsx body/assets, and reduce App.tsx to render only <AppShell /> (AppShell is created in Task 2 — leave a minimal import). In src-tauri/tauri.conf.json set the main window entry to decorations:false AND shadow:false (shadow defaults true → white 1px OS border + Win11 rounded corners, violates spec — Pitfall 1); keep width/height defaults. Confirm tauri.conf.json does NOT set dpiAware:false anywhere (SHELL-03). Rewrite src-tauri/capabilities/default.json to grant exactly core:default, core:window:allow-minimize, core:window:allow-toggle-maximize, core:window:allow-close, core:window:allow-start-dragging, with windows:["main"] (least-privilege, ASVS V4 — mitigates T-01-01; never core:window:default catch-all). Keep the scaffold's @tauri-apps/plugin-opener (harmless, per RESEARCH Open Question 2). Do NOT add zustand (D-02).
  </action>
  <verify>
    <automated>npm run tauri build 2>&1 | tail -5 (compiles clean); npm ls react react-dom @types/react @types/react-dom (all resolve to 18.x); node -e "const c=require('./src-tauri/tauri.conf.json');const w=c.app.windows[0];if(w.decorations!==false||w.shadow!==false)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` pins `react` and `react-dom` to `18.2.x` and `@types/react`/`@types/react-dom` to `^18`; `npm ls react` shows no 19.x
    - `package.json` contains NO `zustand` dependency (D-02)
    - `src-tauri/tauri.conf.json` main window contains `"decorations": false` AND `"shadow": false`; contains no `"dpiAware": false`
    - `src-tauri/capabilities/default.json` `permissions` array contains `core:window:allow-minimize`, `core:window:allow-toggle-maximize`, `core:window:allow-close`, `core:window:allow-start-dragging` and `windows` equals `["main"]`; does NOT contain `core:window:default`
    - `src-tauri` no longer contains the sample `greet` command; `src/App.tsx` renders `<AppShell />` only
    - `npm run tauri build` completes without error
  </acceptance_criteria>
  <done>App scaffolded and compiles; React on 18.x; frameless shadowless main window configured; window capability scoped least-privilege to "main"; demo UI removed.</done>
</task>

<task type="auto">
  <name>Task 2: Seed foundation — full tokens.css, per-weight font bundle, app-shell grid</name>
  <read_first>
    - .planning/phases/01-shell-foundation/01-UI-SPEC.md (Spacing Scale, Typography, Color tables — the complete token set to author)
    - .planning/phases/01-shell-foundation/01-CONTEXT.md (D-01 seed foundation; D-03 font budget)
    - .planning/phases/01-shell-foundation/01-RESEARCH.md (Pattern 3 per-weight font import; Recommended Project Structure)
    - src/main.tsx (scaffold entry — to wire the global imports)
  </read_first>
  <action>
    Create src/styles/tokens.css defining the FULL UI-SPEC token set as CSS custom properties on :root (D-01), not just the title-bar subset: colors (--color-bg #0A0A0B, --color-panel #131418, --color-fg #E6E4DE, --color-danger-hover #7a2a24, --color-line #1E1F22, --color-dim #6E6C66, --color-fg-mid #A5A29A); chrome metrics (--titlebar-h 34px, --titlebar-inset 12px, --cluster-gap 9px, --logo-circle 15px, --logo-stroke 1.5px, --logo-dot 5px, --icon-btn-w 34px, --icon-btn-h 24px, --winctl-w 46px, --winctl-h 34px, --maximize-glyph 10px, --border-w 1px, --radius 0); spacing scale (--space-xs 4px … --space-3xl 64px); and type roles (font families IBM Plex Mono/Sans/Serif, sizes 11/12/13/26px, weights, line-heights, --ls-wordmark 0.08em) exactly per UI-SPEC. Create src/fonts.ts as the single font import point with per-weight @fontsource subpath imports ONLY (SHELL-04, D-03): ibm-plex-mono 400 + 500, ibm-plex-sans 400 + 500 + 600, ibm-plex-serif 400 + 400-italic — never a bare/all-weights import (Pitfall: bundle bloat). Create src/app/AppShell.tsx + AppShell.module.css establishing display:grid with grid-template-rows:var(--titlebar-h) 1fr; render an empty title-row placeholder div (row 1) and an empty body div (row 2) with flat var(--color-bg) background, no border, no placeholder text (UI-SPEC Body Area — plan 01-02 mounts the real TitleBar into row 1). In src/main.tsx import './fonts' and './styles/tokens.css' at the top so Vite bundles fonts locally and tokens load globally. Author every metric in logical CSS px — no devicePixelRatio / 0.5px hacks (SHELL-03).
  </action>
  <verify>
    <automated>grep -q "@fontsource/ibm-plex-mono/400.css" src/fonts.ts && grep -q "@fontsource/ibm-plex-serif/400-italic.css" src/fonts.ts; grep -q "grid-template-rows" src/app/AppShell.module.css; grep -c -- "--color-\|--titlebar\|--space-" src/styles/tokens.css; grep -rn "devicePixelRatio\|0\.5px" src/ && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/tokens.css` defines custom properties for all 7 UI-SPEC colors, the chrome metrics (incl. `--titlebar-h: 34px`, `--border-w: 1px`, `--radius: 0`), the 4pt spacing scale, and the type roles (Mono/Sans/Serif)
    - `src/fonts.ts` imports exactly: mono 400+500, sans 400+500+600, serif 400+400-italic via per-weight subpaths; contains no bare `@fontsource/ibm-plex-*` (no-subpath) import
    - `src/app/AppShell.module.css` sets `grid-template-rows` using `var(--titlebar-h)` and `1fr`; body row uses `var(--color-bg)` with no border and no text
    - `src/main.tsx` imports `./fonts` and `./styles/tokens.css`
    - `grep -rn "devicePixelRatio\|0.5px" src/` returns no matches
  </acceptance_criteria>
  <done>Full design-token stylesheet, per-weight local font bundle, and the 34px/1fr app-shell grid exist and are wired into the entry point; no physical-pixel hacks.</done>
</task>

<task type="auto">
  <name>Task 3: Stand up Vitest harness with RED specs for title bar + window controls (Wave 0)</name>
  <read_first>
    - .planning/phases/01-shell-foundation/01-RESEARCH.md (Validation Architecture: test framework, Phase Requirements → Test Map, Wave 0 Gaps; @tauri-apps/api/mocks usage)
    - .planning/phases/01-shell-foundation/01-VALIDATION.md (Per-Task Verification Map; test infra)
    - .planning/phases/01-shell-foundation/01-UI-SPEC.md (title-bar structure + Interaction Contract the specs assert against)
    - vitest.config.ts (create) and package.json (add test script)
  </read_first>
  <action>
    Dev-install vitest, @testing-library/react, and jsdom (versions per RESEARCH Standard Stack; re-verify latest at execution). Create vitest.config.ts (or a test block in vite.config.ts) with environment:'jsdom' and path aliases matching tsconfig.json. Add a "test" script to package.json running `vitest` and confirm `npm run test -- --run` works. Write src/shell/TitleBar.test.tsx (RED — SHELL-01): render <TitleBar/> and assert the left cluster (logo mark, "Sourcerer" wordmark, "·", "Home" crumb), a spacer element carrying data-tauri-drag-region, and three window-control buttons with aria-labels "Minimize window" / "Maximize window" / "Close window" are present, and that the drag-region attribute is on the spacer ONLY (not on any button or the logo cluster — mitigates T-01-03). Write src/shell/WindowControls.test.tsx (RED — SHELL-02 click wiring): using @tauri-apps/api/mocks mockIPC, assert clicking each control invokes the correct Tauri window command (minimize → minimize, maximize → toggle_maximize, close → close). Both specs import components from src/shell/ that do NOT exist yet, so the suite MUST fail RED now — this is the Nyquist gate for plan 01-02. Do NOT use watch mode; always `--run`.
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | tail -20 (suite RUNS and reports FAILED/RED for TitleBar + WindowControls specs — cannot resolve missing components); grep -q "jsdom" vitest.config.ts; grep -q "data-tauri-drag-region" src/shell/TitleBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies include `vitest`, `@testing-library/react`, `jsdom`; `scripts.test` runs `vitest`
    - `vitest.config.ts` sets `environment: 'jsdom'`
    - `src/shell/TitleBar.test.tsx` asserts the left cluster strings, the `data-tauri-drag-region` spacer, and the 3 aria-labelled control buttons; asserts drag-region is present on exactly one element (the spacer)
    - `src/shell/WindowControls.test.tsx` uses `mockIPC` and asserts minimize/toggle_maximize/close commands fire on the respective clicks
    - `npm run test -- --run` executes and reports the two specs FAILING (RED) because `src/shell/TitleBar.tsx` / `WindowControls.tsx` do not yet exist
  </acceptance_criteria>
  <done>Vitest+jsdom harness runs; SHELL-01 and SHELL-02 specs exist and fail RED, ready for plan 01-02 to turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React webview → Tauri Rust core (IPC) | Window-command calls cross from JS into the native core; the capability ACL gates which succeed |
| npm registry → build/webview | Third-party dependencies execute in the build toolchain and webview JS context |
| Frameless window edge → OS | `data-tauri-drag-region` / `startDragging` translates DOM intent into native window-move messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | src-tauri/capabilities/default.json | mitigate | Least-privilege grant: only `core:window:allow-minimize/toggle-maximize/close/start-dragging` scoped to `windows:["main"]`; never `core:window:default` catch-all (Task 1) |
| T-01-03 | Tampering/Spoofing | title-bar drag region | mitigate | `data-tauri-drag-region` applied to the spacer element ONLY (it does not cascade); asserted in TitleBar.test.tsx so no control button becomes drag-only (Task 3, enforced in 01-02) |
| T-01-14 | Configuration | tauri.conf.json security.csp | accept | Scaffold default `csp:null` — Phase 1 loads no remote/untrusted content; deferred to a future hardening phase (RESEARCH Security Domain V14) |
| T-01-SC | Tampering | npm/cargo installs | mitigate | RESEARCH Package Legitimacy Audit cleared all packages (slopcheck OK, no [ASSUMED]/[SUS], empty postinstall). No blocking checkpoint required; re-run slopcheck for any new dependency |
</threat_model>

<verification>
- `npm run tauri build` compiles clean; `npm run tauri dev` launches a window (visual confirmation is the plan 01-03 human-verify).
- `npm run test -- --run` executes; TitleBar + WindowControls specs fail RED (components not yet built).
- `grep -rn "devicePixelRatio\|0.5px" src/` returns no matches (SHELL-03 regression proxy).
- React resolves to 18.x across react/react-dom/@types.
</verification>

<success_criteria>
- Frameless (`decorations:false` + `shadow:false`) app scaffolded, compiles, launches.
- Full `tokens.css` + per-weight font bundle + `34px 1fr` app-shell grid seeded (D-01/D-03/D-02).
- Vitest+jsdom harness live with RED SHELL-01/02 specs (Nyquist gate for 01-02).
- Window capability scoped least-privilege to "main".
</success_criteria>

<output>
Create `.planning/phases/01-shell-foundation/01-01-SUMMARY.md` when done.
</output>

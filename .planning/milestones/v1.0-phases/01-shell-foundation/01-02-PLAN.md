---
phase: 01-shell-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/shell/LogoCluster.tsx
  - src/shell/LogoCluster.module.css
  - src/shell/TitleBar.tsx
  - src/shell/TitleBar.module.css
  - src/shell/WindowControls.tsx
  - src/shell/WindowControls.module.css
  - src/shell/useMaximizedState.ts
  - src/app/AppShell.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-04]

must_haves:
  truths:
    - "User sees a 34px title bar: logo mark + Sourcerer wordmark + · + Home crumb (left), a flex drag-spacer, and minimize/maximize/close controls (right), rendered in bundled IBM Plex Mono"
    - "User can click minimize, maximize/restore, and close and the correct Tauri window command fires each time"
    - "D-02: the maximize/restore icon + aria-label are driven by isMaximized() re-queried on onResized, with no Zustand/state store introduced"
    - "Only the flex spacer carries data-tauri-drag-region; no control button or the logo cluster swallows clicks"
    - "SHELL-04: a production build contains zero references to fonts.googleapis.com / fonts.gstatic.com"
  artifacts:
    - path: "src/shell/TitleBar.tsx"
      provides: "34px title bar composition"
      min_lines: 15
    - path: "src/shell/WindowControls.tsx"
      provides: "minimize/maximize/close buttons wired to @tauri-apps/api/window"
      contains: "toggleMaximize"
    - path: "src/shell/useMaximizedState.ts"
      provides: "isMaximized() tracked via onResized (no store)"
      contains: "onResized"
    - path: "src/shell/LogoCluster.tsx"
      provides: "logo SVG + wordmark + crumb left cluster"
      min_lines: 8
  key_links:
    - from: "src/shell/WindowControls.tsx"
      to: "@tauri-apps/api/window"
      via: "getCurrentWindow().minimize/toggleMaximize/close"
      pattern: "getCurrentWindow"
    - from: "src/app/AppShell.tsx"
      to: "src/shell/TitleBar.tsx"
      via: "mounts TitleBar into grid row 1"
      pattern: "TitleBar"
    - from: "src/shell/TitleBar.tsx"
      to: "src/shell/TitleBar.module.css"
      via: "tokens-driven styling"
      pattern: "var\\(--titlebar"
---

<objective>
Build the title-bar UI slice that turns plan 01-01's RED specs green: the LogoCluster (left), the flex drag-spacer, and the WindowControls (right), styled pixel-perfect against tokens.css and mounted into the app-shell grid's 34px row. Window controls wire directly to the Tauri window API; the maximize/restore icon reflects actual window state via `isMaximized()` re-queried on resize events (no store — D-02).

Purpose: This is the interactive slice of the Walking Skeleton — after it, a user can see the real title bar and drive the window through custom controls. It closes SHELL-01 and the automatable half of SHELL-02, and verifies SHELL-04's no-network-fonts guarantee against a real build.
Output: A rendered, wired title bar; green SHELL-01/02 specs; confirmed local-only fonts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-shell-foundation/01-UI-SPEC.md
@.planning/phases/01-shell-foundation/01-RESEARCH.md
@.planning/phases/01-shell-foundation/01-CONTEXT.md
@.planning/phases/01-shell-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Contracts established by plan 01-01. Use directly — no codebase exploration needed. -->

Tokens available in src/styles/tokens.css (:root custom properties):
  colors: --color-bg #0A0A0B, --color-panel #131418, --color-fg #E6E4DE,
          --color-danger-hover #7a2a24, --color-line #1E1F22,
          --color-dim #6E6C66, --color-fg-mid #A5A29A
  metrics: --titlebar-h 34px, --titlebar-inset 12px, --cluster-gap 9px,
           --logo-circle 15px, --logo-stroke 1.5px, --logo-dot 5px,
           --winctl-w 46px, --winctl-h 34px, --maximize-glyph 10px,
           --border-w 1px, --radius 0, --ls-wordmark 0.08em

App shell (src/app/AppShell.tsx): grid-template-rows: var(--titlebar-h) 1fr;
  row 1 currently holds an empty title-row placeholder div → replace with <TitleBar/>.

Tauri window API (@tauri-apps/api/window):
  getCurrentWindow(): { minimize(), toggleMaximize(), close(), isMaximized(): Promise<boolean>, onResized(cb): Promise<UnlistenFn> }

RED specs to satisfy (from 01-01):
  src/shell/TitleBar.test.tsx      — SHELL-01 structure + drag-region-on-spacer-only
  src/shell/WindowControls.test.tsx — SHELL-02 click → correct window command (mockIPC)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LogoCluster + TitleBar composition (turns SHELL-01 spec green)</name>
  <read_first>
    - .planning/phases/01-shell-foundation/01-UI-SPEC.md (Spacing/Typography/Color; Copywriting Contract; Interaction Contract; Phase Boundary Note — build ONLY logo+wordmark+·+crumb → spacer → controls)
    - src/shell/TitleBar.test.tsx (the RED spec this must satisfy)
    - src/styles/tokens.css (custom properties to reference)
    - src/app/AppShell.tsx (mount point — grid row 1)
  </read_first>
  <behavior>
    - Renders logo mark (15px ring, 1.5px stroke, 5px filled dot, both accent #E6E4DE — the one allowed border-radius:50% exception), "Sourcerer" wordmark (Plex Mono 12px, letter-spacing 0.08em, accent), "·" separator, and "Home" crumb (Plex Mono 11px, dim #6E6C66) in a left cluster with 9px gap and 12px left inset
    - Renders a flex spacer element (flex:1) carrying data-tauri-drag-region — and it is the ONLY element with that attribute
    - Bar is 34px tall with a 1px #1E1F22 bottom border, background #0A0A0B, radius 0
    - Logo/wordmark cluster click fires an openHome handler (no-op/console stub in Phase 1 — do not block on Phase 2/6); the cluster does NOT carry the drag-region attribute
    - Mounts WindowControls (Task 2) as the right cluster
  </behavior>
  <action>
    Create src/shell/LogoCluster.tsx + LogoCluster.module.css rendering the logo SVG (ring + dot per UI-SPEC metrics), the "Sourcerer" wordmark, the "·" separator, and the "Home" crumb, styled from tokens (var(--color-fg), var(--ls-wordmark), var(--color-dim)); wire an onClick openHome console-stub on the cluster wrapper. Create src/shell/TitleBar.tsx + TitleBar.module.css composing: LogoCluster (left) → a div.spacer with flex:1 and data-tauri-drag-region (the drag region — Pitfall 3: attribute does NOT cascade, put it on the spacer ONLY) → WindowControls (right). Style the bar height var(--titlebar-h), border-bottom var(--border-w) solid var(--color-line), background var(--color-bg), border-radius var(--radius). Do NOT render the LAYOUTS button, rail-cycle button, or assistant-toggle button (UI-SPEC Phase Boundary Note) — the spacer absorbs their future space. Edit src/app/AppShell.tsx to import and mount <TitleBar/> into grid row 1, replacing the placeholder div from 01-01.
  </action>
  <verify>
    <automated>npm run test -- --run src/shell/TitleBar.test.tsx 2>&1 | tail -15 (GREEN)</automated>
  </verify>
  <acceptance_criteria>
    - `src/shell/TitleBar.test.tsx` passes (GREEN)
    - `TitleBar.tsx` renders LogoCluster, a `flex:1` spacer with `data-tauri-drag-region`, and WindowControls; exactly one element in the tree carries `data-tauri-drag-region`
    - `LogoCluster.tsx` renders the logo SVG, "Sourcerer", "·", and "Home" and has an `openHome` click handler on the cluster (not the spacer)
    - `TitleBar.module.css` uses `var(--titlebar-h)`, `var(--border-w)`, `var(--color-line)`, `var(--color-bg)`, `var(--radius)`; no hardcoded 34px/1px literals where a token exists
    - `AppShell.tsx` mounts `<TitleBar/>` in grid row 1 (placeholder removed)
    - The LAYOUTS / rail-cycle / assistant-toggle buttons are NOT rendered
  </acceptance_criteria>
  <done>Title bar renders pixel-perfect per UI-SPEC with drag-region on the spacer only; SHELL-01 spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WindowControls wired to Tauri window API + maximize-state hook (turns SHELL-02 spec green)</name>
  <read_first>
    - .planning/phases/01-shell-foundation/01-UI-SPEC.md (Interaction Contract SHELL-02; window control glyphs; hover colors; aria-labels)
    - .planning/phases/01-shell-foundation/01-RESEARCH.md (Pattern 1 window API wiring; Pattern 2 useMaximizedState; Pitfall 4 icon-state drift)
    - src/shell/WindowControls.test.tsx (the RED spec — mockIPC command assertions)
    - .planning/phases/01-shell-foundation/01-CONTEXT.md (D-02 stateless, no Zustand)
  </read_first>
  <behavior>
    - Minimize button (glyph "—", dim #6E6C66; hover bg #131418, glyph #A5A29A) → getCurrentWindow().minimize()
    - Maximize/restore button (10×10px 1px-border square div, dim border; hover bg #131418) → getCurrentWindow().toggleMaximize()
    - Close button (glyph "✕", dim; hover bg #7a2a24, glyph #E6E4DE) → getCurrentWindow().close()
    - Each button is 46px wide × 34px tall, radius 0
    - aria-label: minimize "Minimize window"; close "Close window"; maximize/restore toggles "Maximize window" ⇄ "Restore window" driven by actual isMaximized() state (re-queried on onResized — not a click-only toggle)
    - Window-command failures fail silently to console (no user-facing error UI)
  </behavior>
  <action>
    Create src/shell/useMaximizedState.ts: a hook that on mount calls getCurrentWindow().isMaximized() and subscribes via onResized to re-query isMaximized() on every resize event (Pattern 2 — maximize events are unreliable standalone, Aero Snap/double-click bypass the button; Pitfall 4), cleaning up the unlisten fn on unmount. No Zustand, no global store (D-02). Create src/shell/WindowControls.tsx rendering the three buttons with the exact glyphs, aria-labels, and click handlers calling getCurrentWindow().minimize()/toggleMaximize()/close() with .catch(console.error) (silent-fail per UI-SPEC Error state); drive the maximize button's glyph title + aria-label from useMaximizedState. Create WindowControls.module.css styling the 46×34 zones, glyphs, and flat hover color shifts (no transitions/shadows) from tokens (var(--winctl-w), var(--color-dim), var(--color-panel), var(--color-danger-hover), var(--color-fg), var(--color-fg-mid), var(--maximize-glyph)).
  </action>
  <verify>
    <automated>npm run test -- --run src/shell/WindowControls.test.tsx 2>&1 | tail -15 (GREEN); npm run test -- --run 2>&1 | tail -5 (full suite green)</automated>
  </verify>
  <acceptance_criteria>
    - `src/shell/WindowControls.test.tsx` passes (GREEN); full `npm run test -- --run` suite is green
    - `WindowControls.tsx` calls `getCurrentWindow().minimize()`, `.toggleMaximize()`, `.close()` with `.catch(console.error)`
    - `useMaximizedState.ts` calls `isMaximized()` and re-queries it inside an `onResized` subscription; returns the maximized boolean; no import of `zustand`
    - Maximize button `aria-label` switches between "Maximize window" and "Restore window" based on the hook's state; minimize/close labels are static
    - `WindowControls.module.css` sets 46×34 zones and hover colors from tokens (danger-hover on close, panel on minimize/maximize); no CSS transitions/shadows
  </acceptance_criteria>
  <done>Window controls fire correct Tauri commands; maximize icon/aria-label track real window state without a store; SHELL-02 click-wiring spec green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify no-network fonts against a real build (SHELL-04 automated gate)</name>
  <read_first>
    - .planning/phases/01-shell-foundation/01-RESEARCH.md (Code Examples: build-output grep check; Validation Architecture SHELL-04 row)
    - .planning/phases/01-shell-foundation/01-UI-SPEC.md (Platform Notes — Fonts: zero fonts.googleapis.com/fonts.gstatic.com requests)
    - src/fonts.ts (the per-weight imports from 01-01)
  </read_first>
  <action>
    Run a production build and assert the bundled output contains zero references to Google Fonts domains (SHELL-04): the prototype's reference `<helmet>` Google Fonts links must NOT have been ported. Add a reusable npm script "verify:fonts" that runs the build-output grep so this stays a repeatable gate. Confirm the built dist/ ships the IBM Plex woff2 assets locally (bundled by Vite from @fontsource).
  </action>
  <verify>
    <automated>npm run build && { grep -rIl "fonts.googleapis.com\|fonts.gstatic.com" dist/ && echo FAIL && exit 1 || echo "PASS: no external font references"; }</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds
    - `grep -rIl "fonts.googleapis.com\|fonts.gstatic.com" dist/` returns no matches (exit 1 from grep → PASS)
    - `dist/` contains bundled IBM Plex `.woff2` font assets (local, from @fontsource)
    - `package.json` has a `verify:fonts` script running the build-output grep gate
  </acceptance_criteria>
  <done>Production build proven free of network font references; local IBM Plex assets bundled; SHELL-04 automated gate in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React webview → Tauri Rust core (IPC) | Window-control clicks issue `@tauri-apps/api/window` commands gated by the capability ACL from 01-01 |
| Frameless title-bar spacer → OS | `data-tauri-drag-region` translates to a native window-move; must not extend to control buttons |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering/Spoofing | title-bar drag region | mitigate | `data-tauri-drag-region` on the spacer element ONLY (does not cascade — Pitfall 3); asserted in TitleBar.test.tsx that exactly one element carries it and no control button does (Task 1) |
| T-01-02 | Denial of Service | window-command handlers | accept | Window commands `.catch(console.error)` fail silently per UI-SPEC (no user-facing error UI for native chrome); low-value local target, no cascading failure |
| T-01-SC | Tampering | npm installs | mitigate | No new runtime deps beyond 01-01's cleared set; re-run slopcheck if any dependency is added |
</threat_model>

<verification>
- `npm run test -- --run` full suite green (SHELL-01 + SHELL-02 specs pass).
- `npm run build` + Google-Fonts grep returns no matches (SHELL-04).
- Maximize aria-label reflects `isMaximized()`; no `zustand` import anywhere (D-02).
- Real drag behavior + visual pixel-fidelity are verified by the human-verify checkpoints in plan 01-03.
</verification>

<success_criteria>
- Title bar renders per UI-SPEC (logo/wordmark/·/crumb → spacer → controls), Plex Mono, 34px, 1px border, radius 0.
- Window controls fire correct Tauri commands; maximize icon tracks real state statelessly (D-02).
- Drag-region on spacer only (no button swallows clicks — automatable half).
- Build proven local-font-only (SHELL-04).
</success_criteria>

<output>
Create `.planning/phases/01-shell-foundation/01-02-SUMMARY.md` when done.
</output>

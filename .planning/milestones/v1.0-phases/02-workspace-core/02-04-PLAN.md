---
phase: 02-workspace-core
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/app/AppShell.tsx
  - src/app/AppShell.module.css
  - src/shell/Rail.tsx
  - src/shell/Rail.module.css
  - src/shell/useRailDrag.ts
  - src/shell/railSnap.ts
  - src/shell/railSnap.test.ts
autonomous: true
requirements: [RAIL-01, RAIL-02, RAIL-03, DOCK-06]
must_haves:
  truths:
    - "User can cycle the rail expanded->compact->hidden via double-clicking the 6px resize handle and via Cmd-backslash / Ctrl-backslash (RAIL-01)"
    - "Dragging the rail right-edge handle live-resizes width; below 44px snaps hidden, below 132px snaps compact, else expanded clamped to [132,520] (RAIL-01, DOCK-06)"
    - "User can reorder rail items by drag (5px threshold, drop-line indicator, arrayMove) and pin items to the bottom group (RAIL-02)"
    - "Active rail item shows a 2px accent left border and badge counts render from the store, hidden when zero (RAIL-03)"
  artifacts:
    - path: "src/shell/Rail.tsx"
      provides: "3-mode rail (expanded/compact/hidden) with rows, glyphs, badges, footer group"
      min_lines: 60
    - path: "src/shell/railSnap.ts"
      provides: "Pure snapWidthToMode(raw) for resize thresholds"
      exports: ["snapWidthToMode"]
    - path: "src/shell/useRailDrag.ts"
      provides: "Pointer-capture resize + reorder hook (bespoke setPointerCapture)"
  key_links:
    - from: "src/app/AppShell.tsx"
      to: "shellStore.railMode"
      via: "grid column bound to rail width"
      pattern: "railMode"
    - from: "src/shell/useRailDrag.ts"
      to: "src/shell/railSnap.ts"
      via: "snapWidthToMode on pointerup"
      pattern: "snapWidthToMode"
---

<objective>
Build the bespoke three-mode left rail: expanded/compact/hidden rendering, pointer-capture drag-resize with snap thresholds, keyboard + double-click cycling, within-rail reorder, and pin-to-bottom-group. This is the highest-fidelity bespoke pointer-event port of the phase (drag-out-to-dock is plan 06).

Purpose: RAIL-01/02/03 + the rail half of DOCK-06 clamp bounds. The rail mounts into the AppShell grid alongside the (later) dock column.
Output: New Rail component + CSS, a useRailDrag pointer hook, a pure railSnap threshold function with unit tests, and the AppShell grid rework.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspace-core/02-UI-SPEC.md
@.planning/phases/02-workspace-core/02-PATTERNS.md

<interfaces>
<!-- Rail geometry constants (tokens.css, plan 01) — mirror as TS constants matching CSS 1:1. -->
<!-- HIDDEN_W=6 COMPACT_W=56 COMPACT_AT=132 CLOSE_AT=44 EXPANDED_MAX=520 ROW_H=36 TILE=40 -->
<!-- Store surface (plan 01): -->
<!-- useShellStore(s => s.railMode | s.railWidth | s.railOrder | s.leftRailPinned | s.badges | s.railApplet) -->
<!-- shellStore.getState(): setRailMode, cycleRailMode, setRailWidth, reorderRail, togglePin, setRailApplet -->
<!-- Applet order + glyphs come from the .dc.html defs map (lines 347-364): {glyph,title,line} per key. -->
<!-- Native-event->React bridging idiom to copy: src/shell/useMaximizedState.ts (useEffect subscribe + cleanup). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppShell grid rework + Rail render (3 modes, rows, badges, footer)</name>
  <files>src/app/AppShell.tsx, src/app/AppShell.module.css, src/shell/Rail.tsx, src/shell/Rail.module.css</files>
  <read_first>
    - src/app/AppShell.tsx (target — current stateless TitleBar + empty body div)
    - src/app/AppShell.module.css (target — current grid-template-rows: var(--titlebar-h) 1fr)
    - src/shell/TitleBar.module.css (CSS-module token-var convention to mirror in Rail.module.css)
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Left Rail — Row anatomy expanded/compact, applet order array, footer group, badge pill)
    - .planning/phases/02-workspace-core/02-PATTERNS.md (Rail.tsx — defs shape, port notes)
    - "design-sync-setup-guide/design_handoff_bespoke_rails_shell/" -> Sourcerer Bespoke Rails.dc.html (defs map lines 347-364; rail row render)
  </read_first>
  <action>
    Rework `AppShell.tsx` from stateless to a thin store-reader: read `railMode`/`railWidth` via `useShellStore`, compute the rail column width (hidden=`var(--rail-hidden-w)` 6px, compact=`var(--rail-compact-w)` 56px, expanded=`railWidth`px clamped [132,520]), render `<TitleBar />` in row 1 and a body row that is a `[rail | dock]` grid — mount `<Rail />` in the left column and a dock-mount placeholder div (`styles.dockHost`, id `dock-host`) in the `1fr` right column (plan 05 fills it). Keep grid metrics in `AppShell.module.css` via CSS custom properties (`grid-template-rows: var(--titlebar-h) 1fr`); the only dynamic inline value is the rail column width. Create `Rail.tsx` rendering from `railOrder` using a typed `defs` const (`{ key: {glyph,title,line} }` ported from the `.dc.html` defs map). Expanded rows: 36px height, 26px glyph column (Plex Mono 18px) + label (Plex Sans 13px, ellipsis) + badge (12px pill, `--color-muted` bg, hidden when `badges[key]` is falsy/0). Compact: 40x40 glyph tiles + badge overlay top-right. Hidden: a 6px hover-to-reopen strip. Active row (from `railApplet`): 2px accent left border + `--color-panel` bg; hover: `--color-panel` bg. Footer group (always last): "Applet Catalog" row + user block (avatar "C" serif italic + "Casey / HUMAN" + settings gear glyph). Clicking a row (no drag) calls `setRailApplet(key)`. Style in `Rail.module.css` with tokens only.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && node -e "const r=require('fs').readFileSync('src/shell/Rail.tsx','utf8'); for(const t of ['railOrder','badges','Applet Catalog']) if(!r.includes(t)){console.error('missing',t);process.exit(1)} const a=require('fs').readFileSync('src/app/AppShell.tsx','utf8'); if(!a.includes('Rail')){console.error('Rail not mounted');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - AppShell grid is `[rail | dock]` with a dock-host placeholder; rail column width derives from `railMode`/`railWidth`.
    - Rail renders all `railOrder` keys with glyph+label+conditional badge (expanded) and tiles (compact); footer group + user block present.
    - Active item shows 2px accent left border; badge hidden when count is 0.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Rail renders in all three modes inside the reworked AppShell grid; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pointer-capture resize hook + snap thresholds + keyboard/double-click cycling</name>
  <files>src/shell/railSnap.ts, src/shell/railSnap.test.ts, src/shell/useRailDrag.ts</files>
  <read_first>
    - src/shell/useMaximizedState.ts (useEffect subscribe + cleanup idiom to mirror for keyboard + pointer listeners)
    - src/shell/Rail.tsx (target consumer — the 6px right-edge handle element)
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Left Rail — resize thresholds CLOSE_AT 44, COMPACT_AT 132, clamp [132,520]; cycle via Cmd-backslash + double-click handle)
    - .planning/phases/02-workspace-core/02-PATTERNS.md (Rail.tsx — startRailResize port; setPointerCapture over window mouse listeners)
  </read_first>
  <behavior>
    - snapWidthToMode(raw): raw<44 -> {mode:'hidden'}; 44<=raw<132 -> {mode:'compact'}; raw>=132 -> {mode:'expanded', width: min(raw,520)}; negative raw clamps to 0 first.
    - Threshold values match tokens (44/132/520) — mirror the CSS numbers as TS constants, do not invent.
  </behavior>
  <action>
    Create `railSnap.ts` exporting the pure `snapWidthToMode(raw: number)` per the behavior block (constants HIDDEN_W=6, COMPACT_W=56, CLOSE_AT=44, COMPACT_AT=132, EXPANDED_MAX=520 matching `tokens.css`). Unit-test it in `railSnap.test.ts` across boundaries (0, 43, 44, 131, 132, 300, 600). Create `useRailDrag.ts`: a hook that on `pointerdown` (button 0) on the rail's 6px right-edge handle calls `setPointerCapture(pointerId)` and tracks `pointermove` to compute live width from `clientX - navRect.left`, pushing a live width for a smooth resize; on `pointerup` it calls `snapWidthToMode` and commits via `shellStore.getState().setRailMode`/`setRailWidth`. Port the prototype's `startRailResize` logic but use `setPointerCapture` + pointer events (CLAUDE.md locked pattern), NOT window mouse listeners. Add a `dblclick` handler on the same handle calling `cycleRailMode()`, and a document `keydown` listener (registered/cleaned in `useEffect`) that on the backslash key with metaKey-or-ctrlKey calls `cycleRailMode()` and `preventDefault()`.
  </action>
  <verify>
    <automated>npx vitest run src/shell/railSnap.test.ts && node -e "const h=require('fs').readFileSync('src/shell/useRailDrag.ts','utf8'); for(const t of ['setPointerCapture','snapWidthToMode','cycleRailMode','keydown']) if(!h.includes(t)){console.error('missing',t);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `railSnap.test.ts` green across all boundary inputs (0/43/44/131/132/300/600).
    - `useRailDrag.ts` uses `setPointerCapture`, commits via `snapWidthToMode`, binds a keydown for the backslash cycle, and a dblclick to `cycleRailMode`.
    - Resize is clamped to [132,520] when expanded (verified via snap test).
  </acceptance_criteria>
  <done>Snap function unit-tested; resize/keyboard/double-click hook wired to the rail handle.</done>
</task>

<task type="auto">
  <name>Task 3: Within-rail reorder (5px threshold, drop-line, arrayMove) + pin</name>
  <files>src/shell/useRailDrag.ts, src/shell/Rail.tsx, src/shell/Rail.module.css</files>
  <read_first>
    - src/shell/useRailDrag.ts (target — extend the same hook with reorder branch)
    - src/shell/Rail.tsx (target — render the drop-line indicator + pin affordance)
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Left Rail — 5px drag threshold, within-rail reorder drop-line, pin to bottom group)
    - .planning/phases/02-workspace-core/02-PATTERNS.md (Rail.tsx — reorder-splice at .dc.html 823-833; arrayMove semantics on railOrder)
  </read_first>
  <action>
    Extend `useRailDrag.ts` (or add a sibling `useRailReorder`) with a row-drag branch: on `pointerdown` over a rail row, only register a drag after 5px of movement (below threshold = a click that opens/focuses the applet via `setRailApplet`). During drag, compute the insertion index between rows and expose it so `Rail.tsx` renders a 1px accent drop-line indicator at that gap. On `pointerup`, call `shellStore.getState().reorderRail(from, to)` (arrayMove). Add a pin affordance (per UI-SPEC — e.g. a pin control on hover or drag into the footer group) that calls `togglePin(key)`; pinned keys render in the always-last bottom group. Render the drop-line + pinned styling in `Rail.module.css` with tokens.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && node -e "const h=require('fs').readFileSync('src/shell/useRailDrag.ts','utf8'); for(const t of ['reorderRail','togglePin']) if(!h.includes(t)){console.error('missing',t);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Drag past 5px reorders via `reorderRail` (arrayMove); below threshold is a click that opens/focuses the applet.
    - A drop-line indicator renders at the computed insertion gap during drag.
    - Pin affordance calls `togglePin`; pinned items render in the bottom group.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Reorder + pin wired to store; drop-line renders; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer input -> store mutation | Drag/resize/reorder events mutate persisted rail state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | resize width input | mitigate | `snapWidthToMode` clamps raw to [0,520] and buckets to valid modes — no out-of-range width can reach the grid/store |
| T-02-07 | Denial | pointermove flood during drag | accept | Bounded to a single active pointer via `setPointerCapture`; listeners cleaned on pointerup/unmount |
</threat_model>

<verification>
- `npx vitest run src/shell/railSnap.test.ts` green.
- Both node source-assertion gates print OK; `npm run build` succeeds.
- Full interactive behavior confirmed in plan 06's human-verify.
</verification>

<success_criteria>
- Rail cycles expanded/compact/hidden via resize-snap, double-click, and Cmd/Ctrl-backslash.
- Rail items reorder (5px threshold, drop-line, arrayMove) and pin to the bottom group.
- Active highlight + store-driven badges render; widths clamp to [132,520].
</success_criteria>

<output>
Create `.planning/phases/02-workspace-core/02-04-SUMMARY.md` when done.
</output>

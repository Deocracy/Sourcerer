---
phase: 02-workspace-core
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false
requirements: [RAIL-01]
must_haves:
  truths:
    - "D-03: the floating-window + transparent-window change is human-verified for DPI (100/125/150%) and title-bar drag before rail/dock build on the frame"
    - "The reworked 40px title bar (wordmark, DIVI chip, corpus label, rail-toggle buttons, window controls) renders correctly inside the floating rounded card"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the Chrome Rework (plans 01-02) on a real Tauri window before any rail/dock work builds on it. D-03 requires this re-verify to be paired with the floating-window change because `transparent:true` + `decorations:false` can regress the 1px crispness, 40px metrics, and title-bar drag that Phase 1 just verified.

Purpose: Catch window-chrome regressions early, on the correct frame, not after rail/dock is stacked on top.
Output: A pass/fail checkpoint record; no code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspace-core/02-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify floating window + DPI + drag on a real window</name>
  <what-built>Plans 01-02 delivered: 40px title bar with DIVI chip, corpus label, and two rail-toggle SVG buttons; green `#86A38C` accent tokens; and the floating rounded window (Tauri `transparent:true`, 20px inset radial backdrop, 10px inner card, 1px border, drop shadow).</what-built>
  <how-to-verify>
    1. Launch the app (use the project's reliable dev invocation — `tauri dev`, or `cargo run` from `src-tauri` per the CLAUDE.md landmine; verify which works for this scaffold).
    2. Floating frame: confirm a rounded 10px card floats on a dark radial backdrop with a ~20px inset all around and a visible drop shadow — not edge-to-edge.
    3. Title bar: confirm it is 40px tall; wordmark "Sourcerer", the "DIVI" chip, a faint "— Default" corpus label, two rail-toggle buttons, and the minimize/maximize/close controls all render left→right.
    4. Rail-toggle buttons: click either — the buttons' SVG fill should change as the rail mode cycles (rail body not built yet; the store value changing is what's verified — inspect via console `shellStore.getState().railMode` cycling expanded→compact→hidden).
    5. Drag: drag the window only by the empty spacer area of the title bar — it must move. Clicking any button or the DIVI chip must NOT drag the window.
    6. DPI: change Windows display scaling to 100%, 125%, and 150% (Settings → Display → Scale) and relaunch at each. Confirm the 1px card border stays crisp (no blur/doubling) and the 40px bar + inset metrics hold.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or describe the regression (which DPI, which element).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview ↔ Tauri window | Verifying the native-window drag/decoration/transparency boundary behaves as intended |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | title-bar drag region | mitigate | Human check step 5 confirms only the spacer drags; buttons/chip do not swallow-then-drag |
| T-02-03 | Elevation | transparent/decorations-off window | accept | Verified visually; local shell with no untrusted web content |
</threat_model>

<verification>
- Human confirms floating frame, 40px chrome, drag isolation, and 1px crispness at 100/125/150% DPI.
</verification>

<success_criteria>
- Chrome Rework passes on a real window at all three DPI scales with correct drag behavior.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-core/02-03-SUMMARY.md` when done (record checkpoint result).
</output>

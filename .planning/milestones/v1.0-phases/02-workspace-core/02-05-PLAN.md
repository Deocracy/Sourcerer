---
phase: 02-workspace-core
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - src/shell/Dock.tsx
  - src/shell/Dock.module.css
  - src/shell/PanelBody.tsx
  - src/shell/PanelBody.module.css
  - src/shell/appletDefs.ts
  - src/app/AppShell.tsx
autonomous: true
requirements: [DOCK-01, DOCK-02, DOCK-03, DOCK-04, DOCK-05, DOCK-06]
must_haves:
  truths:
    - "D-04: dockview-core owns tabs, splits, 5-zone docking, and resizers natively (themed via --dv-*), NOT a bespoke reimplementation"
    - "User can open applets in 34px tab bars with a '+' button; tabs drag-reorder, move between bars, and dock into 5 zones + edge-splits (DOCK-01, DOCK-02)"
    - "D-02: on mount, if no/empty/corrupt saved layout, the workspace auto-opens Wiki then Library (canary-guarded restore, 300ms debounced persist to sourcerer-dockview-bespoke-v2)"
    - "Multiple instances of one applet coexist with stable key:nanoid ids; onDidActivePanelChange is the single focus source of truth (DOCK-04, DOCK-05)"
    - "Closing the last tab of a pane prunes the tree via dockview; pane clamps are dockview library defaults (DOCK-03, DOCK-06)"
  artifacts:
    - path: "src/shell/Dock.tsx"
      provides: "dockview-core mount, theme, restore/canary, focus, multi-instance"
      min_lines: 70
    - path: "src/shell/PanelBody.tsx"
      provides: "makeRenderer dispatcher + generic placeholder body"
    - path: "src/shell/Dock.module.css"
      provides: ".sourcerer-dock --dv-* theme map (19 vars)"
      contains: "--dv-drag-over-border-color"
  key_links:
    - from: "src/shell/Dock.tsx"
      to: "localStorage['sourcerer-dockview-bespoke-v2']"
      via: "onDidLayoutChange 300ms debounce + canary"
      pattern: "sourcerer-dockview-bespoke-v2"
    - from: "src/shell/Dock.tsx"
      to: "shellStore.setRailApplet"
      via: "onDidActivePanelChange"
      pattern: "onDidActivePanelChange"
---

<objective>
Integrate dockview-core as the center dock: mount + theme it (`.sourcerer-dock` `--dv-*` map), build the panel-body dispatcher with a generic placeholder, wire canary-guarded restore with Wiki/Library defaults + debounced persist, and consume dockview's own events for focus and multi-instance ids. Per D-04, dockview owns tabs/splits/docking/resizers natively — theme, do not reimplement.

Purpose: DOCK-01..06. Provides the center workspace the rail docks into, and a proven mounting seam for Phase 4 applets.
Output: New Dock + PanelBody components + CSS, an appletDefs map, and the dock mounted into AppShell's dock-host.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspace-core/02-UI-SPEC.md
@.planning/phases/02-workspace-core/02-PATTERNS.md

<interfaces>
<!-- dockview-core 2.0.0 API (verify exact signatures against the installed package): -->
<!-- createDockview(el, { createComponent }) -> api; api.addPanel({id, component, title, position?}); -->
<!-- api.onDidLayoutChange, api.onDidActivePanelChange, api.toJSON(), api.fromJSON(), api.clear(), api.dispose() -->
<!-- Panel id scheme (DOCK-04): `${key}:${nanoid()}`; active key = String(active.id).split(':')[0] -->
<!-- Store surface (plan 01): shellStore.getState().setRailApplet(key), setActivePaneId(id) -->
<!-- Theme base class: "dockview-theme-abyss sourcerer-dock" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: appletDefs + PanelBody dispatcher + generic placeholder</name>
  <files>src/shell/appletDefs.ts, src/shell/PanelBody.tsx, src/shell/PanelBody.module.css</files>
  <read_first>
    - src/shell/Rail.tsx (the defs map already introduced in plan 04 — reuse/share the same applet defs shape)
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Center Dock — Panel body dispatch: makeRenderer, generic placeholder body glyph+title+desc+dashed box)
    - .planning/phases/02-workspace-core/02-PATTERNS.md (PanelBody.tsx — panelBody(key) content structure lines 874-897; defs shape)
    - "design-sync-setup-guide/design_handoff_bespoke_rails_shell/" -> Sourcerer Bespoke Rails.dc.html (panelBody + defs)
  </read_first>
  <action>
    Extract the applet `defs` (`{ key: {glyph, title, line} }`) into a shared `appletDefs.ts` const so both `Rail.tsx` (plan 04) and the dock consume one source (refactor Rail to import it if it inlined its own; keep the same keys/glyphs). Create `PanelBody.tsx` rendering the generic placeholder body per UI-SPEC: an "APPLET · {TITLE}" mono label, glyph tile, serif title, description line, and a dashed note box (`1px dashed` border). It takes an applet `key` prop and looks up `appletDefs[key]`. Style via `PanelBody.module.css` (CSS-module, tokens only) — NOT inline `style.cssText` strings. Phase 2 only builds the generic placeholder branch (Wiki/Library/Notes real bodies are Phase 4/5); the dispatcher's `makeRenderer` for non-placeholder keys is a Phase-4 seam.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && node -e "const p=require('fs').readFileSync('src/shell/PanelBody.tsx','utf8'); if(!p.includes('appletDefs')){console.error('no defs import');process.exit(1)} require('fs').accessSync('src/shell/appletDefs.ts'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `appletDefs.ts` exports the shared `{glyph,title,line}` map; Rail imports it (single source).
    - `PanelBody.tsx` renders the APPLET label + glyph tile + serif title + dashed note box from `appletDefs[key]`, styled via CSS module (no inline cssText).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Shared defs + placeholder panel body render per spec; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Dock mount + --dv-* theme + '+' open + nanoid multi-instance + focus</name>
  <files>src/shell/Dock.tsx, src/shell/Dock.module.css, src/app/AppShell.tsx</files>
  <read_first>
    - src/shell/useMaximizedState.ts (useEffect + cleanup idiom for wrapping dockview's imperative API + dispose)
    - src/app/AppShell.tsx (target — the dock-host placeholder div from plan 04 that Dock mounts into)
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Center Dock + Dock Theme Tokens block lines 272-294; base class dockview-theme-abyss sourcerer-dock)
    - .planning/phases/02-workspace-core/02-PATTERNS.md (Dock.tsx — initDock/makeRenderer/addApplet/syncOpen; panel-id nanoid; focus split-on-colon)
  </read_first>
  <action>
    Create `Dock.tsx`: in a `useEffect`, call `createDockview` on a ref div (`position:absolute; inset:0`) with `createComponent` wired to `PanelBody`/`makeRenderer` (dispatch by `String(opts.name||opts.id).split(':')[0]` -> `appletDefs`, else generic placeholder). Apply className `dockview-theme-abyss sourcerer-dock`. Add an `addApplet(key)` helper using panel id `` `${key}:${nanoid()}` `` (DOCK-04) so multiple instances coexist; wire the tab-bar "+" action (dockview's `addPanel`/group action per the installed API) to open a new applet in the active group (DOCK-01). Subscribe `api.onDidActivePanelChange` -> derive `activeKey = String(active.id).split(':')[0]` -> push into the store via `shellStore.getState().setRailApplet(activeKey)` and `setActivePaneId(active.id)` (DOCK-05, single source of truth — no bespoke focus tracking). Clean up with `api.dispose()` in the effect's return. Copy the 19-variable `--dv-*` theme block verbatim from UI-SPEC "Dock Theme Tokens" into `Dock.module.css` under `.sourcerer-dock`. Mount `<Dock />` into AppShell's dock-host column. Do NOT reimplement tabs/splits/resizers — dockview owns them (D-04; DOCK-02/06 are library-native).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && node -e "const d=require('fs').readFileSync('src/shell/Dock.tsx','utf8'); for(const t of ['createDockview','onDidActivePanelChange','nanoid','dispose','sourcerer-dock']) if(!d.includes(t)){console.error('missing',t);process.exit(1)} const c=require('fs').readFileSync('src/shell/Dock.module.css','utf8'); if(!c.includes('--dv-drag-over-border-color')){console.error('theme map incomplete');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `Dock.tsx` mounts dockview via `createDockview`, disposes on unmount, uses `key:nanoid()` ids, and updates the store from `onDidActivePanelChange`.
    - `Dock.module.css` `.sourcerer-dock` contains the full `--dv-*` map including `--dv-drag-over-border-color: #86A38C` and `--dv-tabs-and-actions-container-height: 34px`.
    - Dock is mounted into AppShell's dock-host; `npm run build` succeeds.
  </acceptance_criteria>
  <done>Themed dockview mounts, opens applets via '+', supports multi-instance ids, drives focus from its own event; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Canary-guarded restore + Wiki/Library default + 300ms debounced persist</name>
  <files>src/shell/Dock.tsx</files>
  <read_first>
    - src/shell/Dock.tsx (target — extend the same effect with restore + persist)
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Center Dock — Empty/fallback DOCK-03: canary, Wiki/Library default, 300ms debounce, key sourcerer-dockview-bespoke-v2)
    - .planning/phases/02-workspace-core/02-PATTERNS.md (Dock.tsx — initDock canary/restore/default block lines 604-652 to port verbatim)
    - .planning/phases/02-workspace-core/02-CONTEXT.md (D-02 — minimal dockview-native persistence only, NOT the Phase-3 contract)
  </read_first>
  <action>
    Extend `Dock.tsx`'s mount effect with the canary-guarded restore (port the prototype's `initDock` block): key `LAYOUT_KEY = 'sourcerer-dockview-bespoke-v2'`, canary `LAYOUT_KEY + ':canary'`. On mount: if the canary is present (previous restore crashed), drop the saved layout; set the canary; try `api.fromJSON(JSON.parse(localStorage[LAYOUT_KEY]))` in a try/catch that on failure clears the api + removes the key; schedule canary removal after ~4000ms. If not restored or `api.panels.length === 0`, `api.clear()` then `addApplet('Wiki'); addApplet('Library')` (D-02 default). Subscribe `api.onDidLayoutChange` to a 300ms debounced write of `api.toJSON()` to `LAYOUT_KEY` (wrapped in try/catch). This is scaffolding only per D-02 — do NOT add schemaVersion, migrations, named layouts, flush-on-close, or tauri-plugin-store (Phase 3). Closing the last tab prunes via dockview natively (DOCK-03).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && node -e "const d=require('fs').readFileSync('src/shell/Dock.tsx','utf8'); for(const t of ['sourcerer-dockview-bespoke-v2','canary','fromJSON','onDidLayoutChange','300']) if(!d.includes(t)){console.error('missing',t);process.exit(1)} if(!(d.includes(\"addApplet('Wiki')\")||d.includes('addApplet(\"Wiki\")'))){console.error('no Wiki default');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Restore is canary-guarded: a crash-on-restore layout is discarded before it can re-crash; corrupt JSON falls back silently to defaults.
    - No/empty/corrupt layout -> Wiki then Library auto-open.
    - `onDidLayoutChange` persists `toJSON()` to `sourcerer-dockview-bespoke-v2` debounced 300ms.
    - No Phase-3 persistence machinery added; `npm run build` succeeds.
  </acceptance_criteria>
  <done>Canary restore + Wiki/Library default + debounced persist wired (D-02 minimal scope); build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage layout JSON -> dockview restore | Persisted layout is untrusted input that could crash restore |
| dockview panel id -> applet dispatch | Panel id string is parsed to select a component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `sourcerer-dockview-bespoke-v2` restore | mitigate | Canary key detects crash-on-restore and drops the layout; `fromJSON` wrapped in try/catch -> clear + default Wiki/Library (DOCK-03) |
| T-02-08 | Denial | onDidLayoutChange write flood | mitigate | 300ms debounce coalesces writes; single timer cleared per change |
| T-02-09 | Tampering | panel-id -> component dispatch | mitigate | Dispatch splits on `:` prefix and falls back to the generic placeholder for unknown keys (no crash on unexpected id) |
</threat_model>

<verification>
- All three node source-assertion gates print OK; `npm run build` succeeds.
- Real docking (5-zone, tabs, splits, focus, multi-instance, restore) confirmed in plan 06's human-verify.
</verification>

<success_criteria>
- Themed dockview center dock with 34px tab bars, '+' open, native 5-zone docking/splits/resizers (D-04).
- Canary-guarded restore with Wiki/Library defaults and 300ms debounced persist (D-02).
- Stable multi-instance ids + focus driven solely by onDidActivePanelChange.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-core/02-05-SUMMARY.md` when done.
</output>

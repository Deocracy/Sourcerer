---
phase: 04-applet-framework
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/applets/Library/index.tsx
  - src/applets/Library/libraryContent.ts
  - src/applets/Library/Library.module.css
  - src/applets/Library/Library.test.tsx
autonomous: true
requirements: [FWK-03]

must_haves:
  truths:
    - "the Library applet renders the ported dashboard (corpus stats, review CTA, ingest CTA, document list)"
    - "the ingest view and document-detail view render with trust/status chips"
    - "the promote/delete confirm flow is reachable and non-destructive"
    - "the dashboard's review CTA calls host.open('Wiki') — proving cross-applet open through the host seam"
    - "a subtle DEMO chip marks the stub as demo"
  artifacts:
    - path: "src/applets/Library/index.tsx"
      provides: "the rich Library demo module (manifest + App({host})) replacing the templated stub shell"
      contains: "export const manifest"
      min_lines: 120
    - path: "src/applets/Library/libraryContent.ts"
      provides: "the demo corpora / documents / stats data"
      contains: "corpora"
  key_links:
    - from: "src/applets/Library/index.tsx"
      to: "src/host/open.ts (via host.open)"
      via: "the review CTA calls host.open('Wiki')"
      pattern: "host.open"
---

<objective>
Port the handoff's rich Library demo into `src/applets/Library/index.tsx` as a faithful JSX module consuming `host` (FWK-03 tier-1). It replaces the Plan-02 templated shell in the Library override file (registry.ts untouched), so this plan runs in parallel with the Wiki port (Plan 03) and the catalog work (Plan 05). The Library demo's review CTA calls `host.open('Wiki')`, doubling as a live demonstration of the FWK-04 `open` seam.

Purpose: Library is the second showcase demo (corpus/document tree, ingest, document detail, trust flags). Port it faithfully.
Output: the rich Library module, its data, scoped styles, and a component test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-applet-framework/04-CONTEXT.md
@.planning/phases/04-applet-framework/04-PATTERNS.md
@.planning/phases/04-applet-framework/04-UI-SPEC.md

<interfaces>
Source to port: NEW Design sync setup guide/design_handoff_bespoke_rails_shell/library.js — full file. Components: TrustChip, StatusChip, Wire, Tab, Stat, Dashboard, Ingest, DocDetail, ConfirmFlow (promote|delete), Library (dashboard|ingest|document views), mountLibrary. Reads store.getState() for selection.Library, activeCorpus, corpora. Uses `ctx.open('Wiki')` for the review CTA — port this to `host.open('Wiki')`.
Shared store: NEW Design sync setup guide/design_handoff_bespoke_rails_shell/store.js — corpora list, activeCorpus, selection.Library, reviewCount, select(applet,id).
From src/host/types.ts: `Host` (has `open(appletKey)`), `AppletManifest`. App signature `App({ host }: { host: Host })`.
From src/applets/_stub/TemplatedStub.tsx: DEMO chip treatment to reuse.
Current src/applets/Library/index.tsx: the Plan-02 override shell (manifest + App→TemplatedStub) to REPLACE.
appletDefs Library entry: glyph "▥", title "Library", desc, code "LIB".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port library.js to JSX module wired to host.open</name>
  <files>src/applets/Library/index.tsx, src/applets/Library/libraryContent.ts, src/applets/Library/Library.module.css</files>
  <read_first>
    - NEW Design sync setup guide/design_handoff_bespoke_rails_shell/library.js (full source — every component + corpora/documents/stats data)
    - NEW Design sync setup guide/design_handoff_bespoke_rails_shell/store.js (corpora, activeCorpus, selection.Library, select())
    - src/store/shellStore.ts (this repo's vanilla-zustand idiom + activeCorpus slice already present in ShellState)
    - src/applets/Wiki/index.tsx is being built in parallel — do NOT import from it; reuse patterns via the handoff source, not the sibling module (avoid a cross-plan file dependency)
    - src/host/types.ts (Host.open signature)
    - src/applets/_stub/TemplatedStub.tsx (DEMO chip)
  </read_first>
  <action>
    Port library.js into src/applets/Library/index.tsx as JSX (same adaptations as the Wiki port): replace esm.sh React/react-dom imports with `import React from "react"`; convert all `h(...)` calls to JSX; drop `mountLibrary(el)` and instead export `manifest` (key "Library", glyph "▥", title "Library", desc from appletDefs, code "LIB") and `App({ host }: { host: Host })` rendering the top-level Library component. Preserve ALL behavior: the three views (DASHBOARD / INGEST / DOCUMENT), the Dashboard stats + Stat tiles, the Ingest view, DocDetail with StatusChip/TrustChip, and the ConfirmFlow (promote|delete) modal — keep promote/delete NON-destructive (the demo does not actually remove data; UI-SPEC confirms no destructive action ships this phase).

    Wire cross-applet open through the host seam: the Dashboard's `onReview` (handoff `ctx.open && ctx.open('Wiki')`) becomes `host.open('Wiki')`. This is the plan's live proof that FWK-04 `open` works from a real applet.

    Move the corpora/documents/stats demo data into src/applets/Library/libraryContent.ts. Keep the `T` color object as LOCAL hex constants (amber/warm/red included) — do NOT add them to tokens.css (RESEARCH Pitfall 4 / UI-SPEC "do not re-theme"). Reconcile store reads: use shellStore's existing `activeCorpus` slice where it maps cleanly; use component-local `useState` for the selected document (the handoff `selection.Library`) since shellStore has no `selection` slice (discretion, low risk). Add the shared DEMO chip (D-12) top-left, matching TemplatedStub's treatment.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/shell/registry.test.ts</automated>
    <human-check>Run the app, open Library. Confirm: dashboard renders corpus stats + Stat tiles; the review CTA opens the Wiki applet (focus-or-open); INGEST and DOCUMENT views render with trust/status chips; the promote/delete confirm flow is reachable and reverses/cancels cleanly; a subtle DEMO chip is visible.</human-check>
  </verify>
  <acceptance_criteria>
    - src/applets/Library/index.tsx exports `manifest` (key "Library") and `App({host})`; no esm.sh imports, no `React.createElement`/`h(`, no `mountLibrary`: `grep -c "esm.sh\|React.createElement\|mountLibrary" src/applets/Library/index.tsx` == 0.
    - the review CTA calls host.open('Wiki'): `grep -c "host.open" src/applets/Library/index.tsx` >= 1.
    - amber/warm/red literals not added to tokens.css: `grep -c "amber\|--color-warn" src/styles/tokens.css` == 0.
    - libraryContent.ts contains the corpora + documents data (source assertion).
    - `npx tsc --noEmit` clean; registry.test.ts green (manifest.key === "Library").
  </acceptance_criteria>
  <done>Rich Library demo ported to JSX consuming host, review CTA wired to host.open('Wiki'), local color literals, DEMO chip, full dashboard/ingest/document/confirm behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Library component test — views + host.open + confirm flow + DEMO chip</name>
  <files>src/applets/Library/Library.test.tsx</files>
  <read_first>
    - src/applets/Library/index.tsx (the ported module from Task 1)
    - src/shell/PanelBody.test.tsx (host mocking + render/query idiom)
  </read_first>
  <action>
    Write src/applets/Library/Library.test.tsx rendering `<App host={stubHost} />` with a mocked host whose `open` is a spy (vi.fn). Assert: the dashboard renders (corpus name / at least one Stat tile); a "DEMO" marker is present; triggering the review CTA calls `stubHost.open` with "Wiki"; navigating to the document/ingest view renders its chips; and the promote (or delete) confirm flow opens its modal. Query by text/role to stay resilient to ported markup.
  </action>
  <verify>
    <automated>npx vitest run src/applets/Library/Library.test.tsx && npm test 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Library.test.tsx asserts dashboard render, DEMO marker, `host.open("Wiki")` called via the review CTA, a secondary view render, and confirm-flow reachability — all passing.
    - Full `npm test` green.
  </acceptance_criteria>
  <done>Automated coverage proves the ported Library renders, calls host.open through the seam, and its confirm flow + DEMO marker are reachable.</done>
</task>

</tasks>

<verification>
- `npm test` green including the new Library test.
- registry.ts untouched (Library override edited in place) — no conflict with Plans 03/05.
- host.open exercised from a real applet (cross-applet open proven live).
- No new tokens added to tokens.css.
</verification>

<success_criteria>
FWK-03 (Library tier-1): the Library applet renders the faithful rich demo — dashboard + ingest + document detail + non-destructive confirm flow — with a subtle DEMO chip, consuming only `host`, and demonstrating `host.open('Wiki')`.
</success_criteria>

<output>
Create `.planning/phases/04-applet-framework/04-04-SUMMARY.md` when done.
</output>

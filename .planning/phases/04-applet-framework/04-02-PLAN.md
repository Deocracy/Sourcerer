---
phase: 04-applet-framework
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/shell/registry.ts
  - src/shell/registry.test.ts
  - src/applets/_stub/TemplatedStub.tsx
  - src/applets/_stub/TemplatedStub.module.css
  - src/applets/_stub/demoRows.ts
  - src/applets/templated.ts
  - src/applets/Wiki/index.tsx
  - src/applets/Library/index.tsx
  - src/shell/appletDefs.ts
  - src/shell/PanelBody.tsx
  - src/shell/PanelBody.test.tsx
  - src/shell/Dock.tsx
autonomous: true
requirements: [FWK-01, FWK-02, FWK-03]

must_haves:
  truths:
    - "registry maps every applet key to a { manifest, App } module"
    - "opening a dockview panel for a registered key renders that module's App({host}) with a live per-instance host, not the old generic PanelBody placeholder"
    - "every templated stub renders a glyph tile, serif title, code crumb, believable demo rows, and a subtle DEMO chip"
    - "an unknown/unregistered key still falls back to the generic PanelBody placeholder (never crashes)"
    - "Wiki and Library are override modules in the registry (initially rendering the templated stub) so their rich ports drop in without touching registry.ts"
  artifacts:
    - path: "src/shell/registry.ts"
      provides: "static key→AppletModule map (generated templated modules + Wiki/Library overrides)"
      contains: "export const registry"
    - path: "src/applets/_stub/TemplatedStub.tsx"
      provides: "the one shared templated stub component with DEMO chip + demo rows"
      contains: "DEMO"
    - path: "src/applets/templated.ts"
      provides: "generated templated AppletModule per appletDefs key"
      contains: "manifest"
    - path: "src/shell/PanelBody.tsx"
      provides: "makeRenderer registry dispatch + parameters.api.id instanceId + dispose auto-cancel/GC"
      contains: "parameters.api.id"
  key_links:
    - from: "src/shell/PanelBody.tsx"
      to: "src/shell/registry.ts"
      via: "registry[appletKey].App({ host: makeHost(instanceId, appletKey) })"
      pattern: "registry\\["
    - from: "src/shell/PanelBody.tsx"
      to: "src/host/index.ts"
      via: "makeHost(instanceId, appletKey)"
      pattern: "makeHost"
    - from: "src/shell/Dock.tsx"
      to: "src/shell/PanelBody.tsx"
      via: "createComponent passes full opts.id to makeRenderer"
      pattern: "makeRenderer\\(opts.id"
---

<objective>
Close the framework loop: a static registry (FWK-01) whose registered keys render through their own module + a live per-instance `host` (FWK-02), with a high-fidelity templated demo stub for every applet (FWK-03 baseline) carrying a subtle DEMO chip. Wiki and Library become override modules that initially render the templated stub, so Plans 03/04 fill in the rich ports by editing only their own module files.

Purpose: This is the vertical slice that proves "registered key replaces its stub" end-to-end. After this plan, dropping a real applet module into the registry demonstrably swaps its placeholder.
Output: registry.ts, the shared TemplatedStub, generated templated modules, Wiki/Library override shells, PanelBody registry dispatch with instanceId wiring, and the Dock createComponent id passthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-applet-framework/04-CONTEXT.md
@.planning/phases/04-applet-framework/04-RESEARCH.md
@.planning/phases/04-applet-framework/04-PATTERNS.md
@.planning/phases/04-applet-framework/04-UI-SPEC.md

<interfaces>
From src/host/types.ts (Plan 01): `AppletManifest {key,glyph,code,title,desc}`, `AppletModule {manifest, App}`, `Host`.
From src/host/index.ts (Plan 01): `makeHost(instanceId: string, appletKey: string): Host`.
From src/shell/appletDefs.ts: `appletDefs: Record<string, {glyph,title,line}>` — 13 keys (Sources, Library, Wiki, Graph, Chat, Writing, Browser, Kanban, News, KeyPass, Builder, Dadabase, Notes). `line` maps to manifest `desc`.
From src/shell/PanelBody.tsx (current): `PanelBody({appletKey})` generic placeholder (keep as unknown-key fallback); `makeRenderer(appletKey): DockContentRenderer` with `init: () => {}` and `dispose`.
From dockview-core (verified .d.ts): content renderer `init(parameters: GroupPanelPartInitParameters)` where `parameters.api.id` is the FULL panel id (e.g. "Wiki:aB3xY").
From src/shell/Dock.tsx (current): `createComponent: (opts) => { const key = String(opts.name||opts.id).split(":")[0]; return makeRenderer(key); }` — must pass the full opts.id through.
From src/host/instanceState.ts (Plan 01): `deleteInstanceState(instanceId)` for dispose GC.
From src/host/aiComplete cancellation: dispose must abandon in-flight calls (frontend-side) — see D-07.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — registry + dispatch integration tests</name>
  <files>src/shell/registry.test.ts, src/shell/PanelBody.test.tsx</files>
  <read_first>
    - src/shell/PanelBody.tsx (current placeholder + makeRenderer to extend)
    - src/store/shellStore.test.ts (how this repo mocks workspaceStore / host modules in tests)
    - src/host/types.ts (AppletModule shape)
    - .planning/phases/04-applet-framework/04-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map)
  </read_first>
  <behavior>
    - registry has an entry for every appletDefs key; each entry has a manifest {key,glyph,code,title,desc} and an App function.
    - registry.Wiki and registry.Library are override modules (distinct from the generated templated ones).
    - rendering a registered templated module shows the DEMO chip and the applet's title.
    - makeRenderer for an unknown key renders the generic PanelBody fallback (no throw).
  </behavior>
  <action>
    Write src/shell/registry.test.ts asserting: `Object.keys(registry)` is a superset of `Object.keys(appletDefs)`; each module's manifest.key equals its registry key; manifest carries non-empty glyph/title/code/desc; Wiki and Library entries exist. Write src/shell/PanelBody.test.tsx (jsdom + @testing-library/react) asserting: mounting a templated applet's App with a stub host renders the applet title and a "DEMO" marker; and that makeRenderer on an unregistered key renders the generic placeholder without throwing. Mock src/host/index.ts's makeHost to return a stub host (no real Tauri/sidecar). These tests fail RED now (registry.ts, TemplatedStub not built yet).
  </action>
  <verify>
    <automated>npx vitest run src/shell/registry.test.ts src/shell/PanelBody.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist and FAIL for the right reason (missing registry/TemplatedStub), not a syntax/import-config error.
    - Tests reference makeHost via a mock, never the real sidecar.
  </acceptance_criteria>
  <done>Two RED integration tests present, failing because the registry/dispatch does not yet exist.</done>
</task>

<task type="auto">
  <name>Task 2: TemplatedStub + generated templated modules + registry + manifest merge</name>
  <files>src/applets/_stub/TemplatedStub.tsx, src/applets/_stub/TemplatedStub.module.css, src/applets/_stub/demoRows.ts, src/applets/templated.ts, src/applets/Wiki/index.tsx, src/applets/Library/index.tsx, src/shell/registry.ts, src/shell/appletDefs.ts</files>
  <read_first>
    - src/shell/PanelBody.tsx + src/shell/PanelBody.module.css (the exact visual pattern to lift: eyebrow, glyphTile, serif title, desc, noteBox)
    - src/shell/appletDefs.ts (13 defs to add `code` crumbs to and feed manifests)
    - src/host/types.ts (AppletManifest, AppletModule, Host)
    - Design sync setup guide/design_handoff_sourcerer_tauri/reference/applets/_TemplateApplet.js (manifest+App shape, `code: 'TMPL'` crumb convention)
    - .planning/phases/04-applet-framework/04-UI-SPEC.md (DEMO chip = mono, --color-faint, subtle, never accent; typography/spacing table)
  </read_first>
  <action>
    Add a `code` field to each entry in src/shell/appletDefs.ts (short 3-5 char uppercase mono crumb per applet — e.g. Wiki→WIKI, Library→LIB, Kanban→KANB, News→NEWS, Sources→SRC, Graph→GRPH, Chat→CHAT, Writing→WRIT, Browser→PBRW, KeyPass→KEYP, Builder→BLDR, Dadabase→DDBS, Notes→NOTE; Claude's discretion per D-specifics). appletDefs remains the single source of truth (glyph/title/desc via `line`/`code`) — no drift.

    Create src/applets/_stub/TemplatedStub.tsx: `App({ manifest, host, rows })` rendering the PanelBody visual (glyph tile, serif title, code crumb, description) plus a per-applet demo-rows section and a subtle "DEMO" chip. Lift the layout/styling from PanelBody.module.css into TemplatedStub.module.css (do not change the shipped metrics — D-11 preserves layout). The DEMO chip is mono, uppercase, `--color-faint` text (never accent, never a full banner — D-12/UI-SPEC). Rows render believable demo content.

    Create src/applets/_stub/demoRows.ts: a `Record<appletKey, DemoRow[]>` with believable per-applet rows where cheap (D-13): Kanban → fake card titles across columns; News → fake feed-item headlines; Graph → fake node/relation lines; Chat → fake message snippets; Sources → fake corpus/pipeline entries; Writing → fake draft sections; Browser → fake clipped-page rows; KeyPass → fake vault entries (labels only, never secrets); Builder → fake component tiles; Dadabase → fake table/query rows; Notes → fake note titles. Generic single-line note where a believable list is not cheap. Every row is obviously demo data.

    Create src/applets/templated.ts: build one `AppletModule` per appletDefs key — `manifest` assembled from appletDefs (key, glyph, title, desc=line, code) and `App: ({host}) => <TemplatedStub manifest={manifest} host={host} rows={demoRows[key]} />`. Export as `templatedModules: Record<string, AppletModule>`. This satisfies D-11 (stubs are registered applet modules) while keeping the 11 uniform stubs generated from the single source of truth rather than 11 duplicate files.

    Create src/applets/Wiki/index.tsx and src/applets/Library/index.tsx as OVERRIDE modules: each exports `manifest` (from appletDefs) and `App` that — for now — renders `<TemplatedStub manifest rows host />`. These are the seams Plans 03/04 fill with the rich ports without touching registry.ts.

    Create src/shell/registry.ts: `export const registry: Record<string, AppletModule> = { ...templatedModules, Wiki: WikiModule, Library: LibraryModule }` (overrides after the spread). Export `AppletManifest`/`AppletModule` re-exports from host/types for consumer convenience. Add the header comment documenting FWK-02: to make a real applet, add its override module to this map (replacing its templated entry); a new key added to appletDefs auto-gets a templated stub and appends to the rail.

    Turn Task 1's registry.test.ts green (registry shape); PanelBody.test.tsx's stub-render assertions go green once TemplatedStub exists (dispatch itself is Task 3).
  </action>
  <verify>
    <automated>npx vitest run src/shell/registry.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const registry" src/shell/registry.ts` == 1; registry spreads templatedModules then overrides Wiki/Library (source assertion).
    - appletDefs.ts has a `code` field on all 13 entries: `grep -c "code:" src/shell/appletDefs.ts` >= 13.
    - TemplatedStub renders a "DEMO" marker with faint (not accent) styling — assert the DEMO text node in PanelBody.test.tsx.
    - demoRows.ts provides rows for at least Kanban, News, Graph, Chat (source assertion) and KeyPass rows contain no secret values (labels only).
    - registry.test.ts green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Registry maps all keys to modules; templated stub + believable demo rows + DEMO chip render; Wiki/Library are override shells; manifests carry code crumbs from the single source of truth.</done>
</task>

<task type="auto">
  <name>Task 3: PanelBody registry dispatch + instanceId + Dock createComponent passthrough</name>
  <files>src/shell/PanelBody.tsx, src/shell/Dock.tsx, src/shell/PanelBody.test.tsx</files>
  <read_first>
    - src/shell/PanelBody.tsx (current makeRenderer to extend — capture parameters.api.id at init, dispatch registry before fallback)
    - src/shell/Dock.tsx (createComponent callback at ~line 84; must pass full opts.id to makeRenderer)
    - src/host/index.ts (makeHost) + src/host/instanceState.ts (deleteInstanceState for dispose GC)
    - .planning/phases/04-applet-framework/04-RESEARCH.md (Pattern 2 — capture the id at init(), not createComponent; Assumption A1 — assert opts.id === parameters.api.id; Pitfall 6 — instanceState GC)
  </read_first>
  <action>
    Rewrite src/shell/PanelBody.tsx `makeRenderer` to signature `makeRenderer(fullPanelId: string, appletKey: string)`. Change the `DockContentRenderer.init` type to receive dockview's `init(parameters)` and derive `instanceId = parameters.api.id` (Pattern 2 — the full "Key:nanoid" id, not the split key). Render LAZILY inside init: look up `registry[appletKey]`; if found, `createRoot(element).render(<mod.App host={makeHost(instanceId, appletKey)} />)`; else render the generic `<PanelBody appletKey={appletKey} />` fallback (D-11 / Phase 3 D-06 — unknown key never crashes). In `dispose`: abandon any in-flight host.ai for this instance (frontend-side flag — D-07), call `deleteInstanceState(instanceId)` (Pitfall 6 GC), then `root?.unmount()`. Assert once (A1) that `parameters.api.id` equals the id string dockview passed to createComponent — add a dev-time console.warn if they ever diverge.

    Edit src/shell/Dock.tsx createComponent to `makeRenderer(opts.id, String(opts.name || opts.id).split(":")[0])` — pass the FULL opts.id through so init() can read parameters.api.id and derive host.instanceId (the current code discards the nanoid suffix). No other Dock.tsx change in this task (the '+' button is Plan 05).

    Complete PanelBody.test.tsx to green: registered key renders the module's App (title + DEMO); makeRenderer/init flow yields a stable instanceId; unknown key renders the generic placeholder.
  </action>
  <verify>
    <automated>npx vitest run src/shell/PanelBody.test.tsx src/shell/registry.test.ts && npx tsc --noEmit && npm test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parameters.api.id" src/shell/PanelBody.tsx` >= 1 (instanceId derived at init, per Pattern 2).
    - `grep -c "makeRenderer(opts.id" src/shell/Dock.tsx` == 1 (full id passthrough).
    - dispose calls deleteInstanceState and unmounts (source assertion) — abandoned calls do not warn/throw.
    - PanelBody.test.tsx: registered-key-renders-module and unknown-key-falls-back both green.
    - Full `npm test` green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Dockview panels dispatch through the registry with a live per-instance host; unknown keys still fall back; dispose GCs the instance slot; the FWK-02 replace-a-stub loop is demonstrably closed for the templated tier.</done>
</task>

</tasks>

<verification>
- `npm test` green including the two new integration test files.
- Opening any registered applet renders through registry + makeHost, not the pre-Phase-4 placeholder (except unknown keys, which still fall back).
- registry.ts is the sole registration point; Wiki/Library are overrides ready for Plans 03/04 without a registry edit.
- appletDefs.ts remains the single source of truth for glyph/title/desc/code.
</verification>

<success_criteria>
FWK-01: registry maps every key to a {manifest, App} module. FWK-02 (templated tier): a registered key renders its own module in place of the generic placeholder; adding a key to appletDefs auto-produces a stub. FWK-03 (baseline): every applet renders a high-fidelity templated stub with glyph tile, serif title, code crumb, believable demo rows, and a subtle DEMO chip.
</success_criteria>

<output>
Create `.planning/phases/04-applet-framework/04-02-SUMMARY.md` when done.
</output>

---
phase: 04-applet-framework
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/shell/AppletCatalog.tsx
  - src/shell/AppletCatalog.module.css
  - src/shell/AppletCatalog.test.tsx
  - src/store/shellStore.ts
  - src/store/shellStore.test.ts
  - src/app/AppShell.tsx
  - src/shell/Dock.tsx
  - src/shell/Rail.tsx
autonomous: true
requirements: [FWK-02]

must_haves:
  truths:
    - "the tab-bar '+' opens a real Applet Catalog picker (glyph + title + desc per applet) instead of cycling keys"
    - "the rail's Applet Catalog footer row (and compact icon) opens the same picker"
    - "clicking a catalog row opens that applet via host.open (focus-or-open)"
    - "the picker supports ArrowUp/ArrowDown + Enter keyboard navigation and click-outside/Escape close"
    - "an applet key present in appletDefs but absent from a restored railOrder appends at the bottom of the main group (D-19), never disturbing existing order"
  artifacts:
    - path: "src/shell/AppletCatalog.tsx"
      provides: "the registry-fed picker (LayoutsMenu pattern) shared by the Dock '+' and the Rail footer"
      contains: "registry"
    - path: "src/store/shellStore.ts"
      provides: "session-only catalog open-state + D-19 railOrder append in hydrateFromDisk"
      contains: "hydrateFromDisk"
  key_links:
    - from: "src/shell/AppletCatalog.tsx"
      to: "src/host/open.ts (via host.open / hostOpen)"
      via: "row click focuses-or-opens the applet"
      pattern: "open"
    - from: "src/shell/Dock.tsx"
      to: "src/store/shellStore.ts"
      via: "'+' button sets catalog open-state instead of nextKey() cycling"
      pattern: "Catalog"
    - from: "src/store/shellStore.ts"
      to: "src/shell/appletDefs.ts"
      via: "hydrateFromDisk merges appletDefs keys missing from railOrder (D-19)"
      pattern: "appletDefs"
---

<objective>
Replace the two placeholder "Applet Catalog" affordances with one real registry-fed picker (D-18) and implement D-19 rail append. The Dock tab-bar '+' (currently a key-cycling hack) and the Rail footer catalog row (currently a `console.log` no-op) both open the same `<AppletCatalog>` picker; clicking a row opens the applet via `host.open` (focus-or-open). A registered key not present in a restored railOrder appends at the bottom of the rail's main group.

Purpose: Completes FWK-02 — a new key appends to the rail, and opening applets flows through a real picker instead of a cycling hack. Registry.ts is only read (imported), and this plan's files are disjoint from the Wiki/Library ports, so it runs in Wave 3 parallel with Plans 03/04.
Output: AppletCatalog picker + styles + test, shellStore catalog open-state + D-19 append, and wiring in Dock, Rail, and AppShell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-applet-framework/04-CONTEXT.md
@.planning/phases/04-applet-framework/04-RESEARCH.md
@.planning/phases/04-applet-framework/04-PATTERNS.md
@.planning/phases/04-applet-framework/04-UI-SPEC.md

<interfaces>
From src/shell/LayoutsMenu.tsx: the full dropdown pattern to mirror — open/close state, document mousedown click-outside + Escape (useEffect), WR-06 focus-into-panel on open (panelRef.current.focus()), ArrowUp/ArrowDown + Enter keyboard nav on a role="menu" panel with role="menuitem" rows. Reuse structure wholesale (RESEARCH "Don't Hand-Roll").
From src/shell/registry.ts (Plan 02): `registry: Record<string, AppletModule>` — each `.manifest` has {key, glyph, code, title, desc}. Catalog rows source glyph/title/desc from registry manifests (single source, no re-derive).
From src/host/open.ts (Plan 01): `hostOpen(appletKey)` focus-or-open (D-17). Import this for row clicks.
From src/shell/Dock.tsx (current): createRightHeaderActionComponent builds the '+' button; its onclick currently calls `addApplet(nextKey())` using the `orderedKeys`/`keyCursor`/`nextKey` cycling code (lines ~88-104, 147-161) — remove the cycling, open the catalog instead.
From src/shell/Rail.tsx (current): `openCatalog()` no-op stub (lines ~18-21) called by the footer `.catalogRow` and the compact `.catalogIcon`.
From src/store/shellStore.ts: `hydrateFromDisk(record)` sets railOrder from disk (line ~135) — the D-19 merge point; session-only slices (railApplet, badges) show the pattern for a non-persisted `catalogOpen` slice.
From src/app/AppShell.tsx: composes Rail + Dock — the mount point for a single `<AppletCatalog/>` instance.
From src/shell/appletDefs.ts: `appletDefs` keys are the authoritative applet-key set for the D-19 append (import this, NOT registry, to avoid an import cycle shellStore→registry→applets→shellStore).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppletCatalog picker + shellStore catalog open-state + AppShell mount</name>
  <files>src/shell/AppletCatalog.tsx, src/shell/AppletCatalog.module.css, src/store/shellStore.ts, src/app/AppShell.tsx, src/shell/AppletCatalog.test.tsx</files>
  <read_first>
    - src/shell/LayoutsMenu.tsx + src/shell/LayoutsMenu.module.css (the dropdown pattern + metrics to mirror: 36px rows, --color-bg panel, 1px --color-line border, radius 0)
    - src/shell/registry.ts (manifest source for rows)
    - src/host/open.ts (hostOpen for row clicks)
    - src/store/shellStore.ts (session-only slice pattern; hydrateFromDisk)
    - src/app/AppShell.tsx (composition/mount point)
    - .planning/phases/04-applet-framework/04-UI-SPEC.md (Component Inventory → Applet Catalog picker contract; copy "+ Open Applet")
  </read_first>
  <action>
    Add a session-only catalog slice to src/store/shellStore.ts (NOT persisted — keep it out of getRailSubset, exactly like railApplet/badges): `catalogOpen: boolean` (default false) and `catalogAnchor: { x: number; y: number } | null`, with actions `openAppletCatalog(anchor?)` and `closeAppletCatalog()`. Do not call scheduleWorkspaceSave for these (session-only).

    Create src/shell/AppletCatalog.tsx mirroring LayoutsMenu's structure: a panel (role="menu", tabIndex -1, focus-on-open per WR-06) listing one row per `registry` manifest — each row shows the glyph (mono, --color-accent), title (sans, --color-fg), and one-line desc (sans, --color-muted, truncated). Rows are sorted by appletDefs order (stable). Bind open-state via `useShellStore((s) => s.catalogOpen)` + anchor; render nothing when closed. Row click (or Enter on focused row) calls `hostOpen(manifest.key)` then `closeAppletCatalog()`. Implement click-outside (document mousedown) + Escape close and ArrowUp/ArrowDown + Enter nav, copied from LayoutsMenu. Position the panel at catalogAnchor (absolute), falling back to a sensible default near the tab bar. No search/filter in v1 (13 rows fit; reuse LayoutsMenu's max-height scroll). Trigger label copy where a label is shown: "+ Open Applet" (UI-SPEC).

    Create src/shell/AppletCatalog.module.css by adapting LayoutsMenu.module.css metrics (36px rows, panel bg/border/radius). Mount a single `<AppletCatalog />` in src/app/AppShell.tsx (one addition alongside Rail/Dock) so both imperative triggers share one instance.

    Write src/shell/AppletCatalog.test.tsx: with catalogOpen true, the picker lists a row per registry manifest (assert a known key like "Wiki" and "Kanban" appear with their titles); clicking a row calls hostOpen with that key (mock src/host/open) and closes the catalog; ArrowDown+Enter opens the focused row; Escape closes.
  </action>
  <verify>
    <automated>npx vitest run src/shell/AppletCatalog.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - AppletCatalog lists one row per registry manifest with glyph/title/desc (source + test assertion); a row click calls hostOpen(key) then closeAppletCatalog.
    - shellStore has catalogOpen/catalogAnchor + openAppletCatalog/closeAppletCatalog, and these are NOT in getRailSubset (session-only): `grep -c "catalogOpen" src/store/shellStore.ts` >= 1 and getRailSubset does not return catalogOpen.
    - `<AppletCatalog />` is mounted once in AppShell.tsx.
    - AppletCatalog.test.tsx covers list, click→hostOpen, keyboard nav, Escape — all passing; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A registry-fed Applet Catalog picker exists with LayoutsMenu-grade keyboard/click-outside behavior, driven by a session-only shellStore open-state, mounted once in AppShell, opening applets via host.open.</done>
</task>

<task type="auto">
  <name>Task 2: Wire both triggers + D-19 rail append</name>
  <files>src/shell/Dock.tsx, src/shell/Rail.tsx, src/store/shellStore.ts, src/store/shellStore.test.ts</files>
  <read_first>
    - src/shell/Dock.tsx (createRightHeaderActionComponent '+' button onclick + the orderedKeys/keyCursor/nextKey cycling code to delete)
    - src/shell/Rail.tsx (openCatalog() no-op + its two call sites: footer .catalogRow and compact .catalogIcon)
    - src/store/shellStore.ts (hydrateFromDisk railOrder assignment — the D-19 merge point; openAppletCatalog from Task 1)
    - src/shell/appletDefs.ts (authoritative key set for the append)
    - src/store/shellStore.test.ts (existing workspaceStore mock + railOrder cases to extend)
    - .planning/phases/04-applet-framework/04-RESEARCH.md (Pitfall 5 — one shared picker, two triggers; D-19 append semantics)
  </read_first>
  <action>
    Dock.tsx: change the '+' header-action button onclick to call `shellStore.getState().openAppletCatalog({ x, y })` (anchor from the button's bounding rect) instead of `addApplet(nextKey())`. Delete the now-dead cycling code (`orderedKeys`, `keyCursor`, `nextKey`). Keep addAppletToDock/addApplet for the restore-default path (Wiki+Library) — only the '+' trigger changes. Keep the button title/label consistent with "+ Open Applet".

    Rail.tsx: replace the `openCatalog()` no-op body with `shellStore.getState().openAppletCatalog()` so the footer `.catalogRow` and the compact `.catalogIcon` both open the shared picker (Pitfall 5 — one component, two triggers). Leave `openSettings()` untouched (out of scope).

    shellStore.ts D-19: in `hydrateFromDisk`, before setting railOrder, compute `merged = [...record.rail.railOrder, ...Object.keys(appletDefs).filter(k => !record.rail.railOrder.includes(k))]` and set `railOrder: merged`. This appends any appletDefs key missing from the saved order at the end (bottom of the main group), deterministically, never reordering existing entries. Import appletDefs (leaf module — no cycle). Guard against duplicate keys.

    Extend src/store/shellStore.test.ts with a D-19 case: given a restored record whose railOrder omits a key present in appletDefs, hydrateFromDisk yields a railOrder that preserves the saved order and appends the missing key(s) at the end.
  </action>
  <verify>
    <automated>npx vitest run src/store/shellStore.test.ts && npx tsc --noEmit && npm test 2>&1 | tail -8</automated>
    <human-check>Run the app. Click the dock tab-bar '+' — the Applet Catalog picker opens; click a row (e.g. Graph) and it opens/focuses that applet. Open the left rail footer "Applet Catalog" row (and the compact ◍ icon) — the same picker opens. Confirm keyboard ArrowDown+Enter opens a row and Escape/click-outside closes it.</human-check>
  </verify>
  <acceptance_criteria>
    - Dock '+' onclick calls openAppletCatalog and the cycling code is gone: `grep -c "nextKey\|keyCursor" src/shell/Dock.tsx` == 0.
    - Rail openCatalog calls openAppletCatalog (no console.log stub remains): `grep -c "no-op stub" src/shell/Rail.tsx` == 0.
    - hydrateFromDisk appends missing appletDefs keys after the saved order (D-19) — asserted by the new shellStore.test.ts case, preserving existing-order and appending at the end.
    - Full `npm test` green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Both catalog triggers open the shared picker; the '+' cycling hack is removed; a new appletDefs key appends to the rail bottom on hydrate — FWK-02 complete.</done>
</task>

</tasks>

<verification>
- `npm test` green including AppletCatalog and the extended shellStore tests.
- registry.ts read-only (imported) — no conflict with the parallel Wiki/Library ports.
- Both the Dock '+' and the Rail catalog affordances open one shared picker; no dangling no-op remains.
- D-19 append preserves user order and never disturbs existing rail entries.
</verification>

<success_criteria>
FWK-02: a real Applet Catalog picker (fed by the registry) opens applets via host.open from both the dock '+' and the rail footer; a registered key absent from a restored railOrder appends at the rail bottom deterministically.
</success_criteria>

<output>
Create `.planning/phases/04-applet-framework/04-05-SUMMARY.md` when done.
</output>

---
phase: 06-dashboard-assistant-home
plan: 05
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/home/cardDefs.ts
  - src/home/HomeCard.tsx
  - src/shell/Home.tsx
  - src/shell/Home.module.css
  - src/shell/DiviChip.tsx
  - src/shell/LogoCluster.tsx
  - src/app/AppShell.tsx
  - src/shell/Home.test.tsx
autonomous: true
requirements: [HOME-01]
must_haves:
  truths:
    - "User can summon the Home metro dashboard by clicking DIVI or the logo cluster (title bar)"
    - "Home renders PINNED / FRESH / LIVING / ARCHIVE sections with the curated demo cards"
    - "DiviChip active styling reflects the real homeOpen state (single source of truth), not railApplet==='Home'"
  artifacts:
    - path: "src/shell/Home.tsx"
      provides: "Metro card dashboard overlay (sections + static card render)"
      contains: "PINNED"
    - path: "src/home/cardDefs.ts"
      provides: "Ported cardDefs registry + DEFAULT_SECTIONS + section labels (static demo data)"
      contains: "export const cardDefs"
    - path: "src/home/HomeCard.tsx"
      provides: "CardBody/CardFrame port (visual variants) as a JSX component"
      contains: "CardFrame"
  key_links:
    - from: "src/shell/DiviChip.tsx"
      to: "src/store/shellStore.ts"
      via: "toggleHomeOpen() action + useShellStore(s => s.homeOpen) selector"
      pattern: "toggleHomeOpen|homeOpen"
    - from: "src/app/AppShell.tsx"
      to: "src/shell/Home.tsx"
      via: "homeOpen-gated overlay mounted over the main/dock column"
      pattern: "homeOpen"
---

<objective>
## Phase Goal

**As a** scholar, **I want to** open a metro-style Home dashboard of my pinned, fresh, living, and
archived research cards from the title bar, **so that** I have an at-a-glance command surface over
my corpus without a populated dock.

Deliver HOME-01: port the handoff's `home-cards.js` card registry and card renderers into real TSX
(`cardDefs.ts`, `HomeCard.tsx`), render the four metro sections (PINNED / FRESH / LIVING / ARCHIVE)
as an overlay (`Home.tsx`, absolute-positioned sibling over the still-mounted dockview per RESEARCH
Open Q2), and wire the currently-no-op `DiviChip.toggleDivi` / `LogoCluster.openHome` stubs to a
single `homeOpen` boolean in `shellStore` (Plan 06-01). This plan renders cards STATICALLY — drag,
persistence, and ＋MAKE CARD land in Plan 06-06 (which installs dnd-kit and wraps these same cards in
a DndContext).

Purpose: HOME-01 (summonable metro dashboard, four sections, curated demo cards).
Output: `cardDefs.ts`, `HomeCard.tsx`, `Home.tsx` overlay, wired DiviChip/LogoCluster, AppShell mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dashboard-assistant-home/06-CONTEXT.md
@.planning/phases/06-dashboard-assistant-home/06-UI-SPEC.md
@.planning/phases/06-dashboard-assistant-home/06-PATTERNS.md

<interfaces>
Port source (near-verbatim, React.createElement -> JSX, esm.sh -> npm/none):
NEW Design sync setup guide/design_handoff_bespoke_rails_shell/home-cards.js
- `cardDefs` (33 entries), `DEFAULT_SECTIONS` {pins,fresh,living,archive}, `SECTION_ORDER`,
  `SECTION_LABELS`, `markEl`, `shim`, `CardBody`, `CardFrame`, `SectionHeader`, `Home`.
- `CardBody({t})` returns `{ kids, barColor, bgOverride, onClickExtra }` with ~18 `t.variant`
  branches (metric/spark/progress/excerpt/timeline/skeleton/compare/graph/chain/feed/annotation/
  cluster/stack/action/entitywiki/entitylive/audit + default). Preserve every variant.
- `CardFrame({t, sec, listView, body, style, innerRef, extra})` — living/archive-list special-cases.

Shell wiring (after Plan 06-01):
```typescript
useShellStore(s => s.homeOpen): boolean
shellStore.getState().toggleHomeOpen(): void
shellStore.getState().setHomeOpen(open: boolean): void
```
Current stubs to REPLACE:
- src/shell/DiviChip.tsx: `const active = useShellStore(s => s.railApplet === "Home")` (STALE — RESEARCH Pitfall 4) + `toggleDivi` console no-op.
- src/shell/LogoCluster.tsx: `openHome` console no-op.
AppShell.tsx body: `<div className={styles.main}><Dock/><ResetNotice/></div>` — mount the overlay here.
</interfaces>

UI-SPEC: Home padding `20px 24px`; card grid `repeat(auto-fill, 176px)`, gap 4px, min-height 82px;
LIVING = single wide block; ARCHIVE has a ◧ CARDS / ≡ LIST toggle; overlay `position:absolute;
inset:0; z-index:55` over the dock. Empty section copy: heading "No cards yet", body "Cards appear
here as you pin items or the assistant mints one via ＋ MAKE CARD."
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port cardDefs.ts + HomeCard.tsx (static registry + card renderers)</name>
  <read_first>
    - NEW Design sync setup guide/design_handoff_bespoke_rails_shell/home-cards.js (full — cardDefs, CardBody, CardFrame, markEl, shim)
    - src/host/theme or src/styles/tokens.css (existing color/font tokens — the port may keep the handoff's literal inline hex/font values verbatim, matching home-cards.js)
  </read_first>
  <action>
    Create `src/home/cardDefs.ts`: export a typed `cardDefs` (all 33 entries verbatim from
    home-cards.js lines 22–56 — do NOT drop or simplify any entry or field), `DEFAULT_SECTIONS`,
    `SECTION_ORDER = ["pins","fresh","living","archive"]`, and `SECTION_LABELS`. Give the card a
    TypeScript type covering the union of fields used across variants (span, kind, mark, title, foot,
    bg, to, variant, and the variant-specific fields events/tiles/left/right/steps/items/claims/
    rows/tasks/bullets/quote/desc/note/big/pct/footColor/bar/dashed/dim/italic/claimsN/openN).
    Create `src/home/HomeCard.tsx`: port `markEl`, `shim`, `CardBody`, and `CardFrame` from
    home-cards.js, converting every `h(tag, props, kids)` call to JSX and keeping the inline style
    objects and all ~18 `t.variant` branches verbatim (literal hex/px values preserved — pixel-perfect
    fidelity). Export `HomeCard({ id, sec, listView, onOpen })` that computes `CardBody({t})` and
    renders `CardFrame` for a static (non-draggable) card — no useSortable/dnd import in this plan.
    Keep the interactive local-state variants (stack cycle, action resolve/dismiss, audit checkboxes)
    working as in the reference.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
    cardDefs.ts holds all 33 typed entries + sections; HomeCard.tsx renders every card variant as JSX
    with the handoff's literal styling; compiles clean; no dnd-kit import yet.
  </done>
  <acceptance_criteria>
    - `grep -c "kind:" src/home/cardDefs.ts` returns at least 33.
    - `grep -n "export const cardDefs\|export const DEFAULT_SECTIONS\|export const SECTION_ORDER" src/home/cardDefs.ts` all present.
    - `grep -c "useSortable\|@dnd-kit" src/home/HomeCard.tsx` returns 0 (static this plan).
    - `npx tsc --noEmit` clean for src/home.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Home.tsx overlay + wire DiviChip/LogoCluster + AppShell mount</name>
  <read_first>
    - NEW Design sync setup guide/design_handoff_bespoke_rails_shell/home-cards.js (Home component: sections, grid(), SectionHeader, archive toggle)
    - src/home/cardDefs.ts, src/home/HomeCard.tsx (Task 1)
    - src/shell/DiviChip.tsx, src/shell/LogoCluster.tsx (stubs to replace)
    - src/app/AppShell.tsx (and src/app/AppShell.module.css for the .main container)
    - src/store/shellStore.ts (homeOpen, toggleHomeOpen, setHomeOpen)
  </read_first>
  <behavior>
    - <Home/> renders four labelled sections PINNED/FRESH/LIVING/ARCHIVE with their DEFAULT_SECTIONS cards.
    - LIVING renders as a single wide block; ARCHIVE shows a ◧ CARDS / ≡ LIST view toggle.
    - Clicking DIVI toggles homeOpen; DiviChip shows active styling iff homeOpen is true.
    - Clicking the logo cluster sets homeOpen true (opens Home).
    - When homeOpen is false, the Home overlay is not present in the DOM; when true it renders over .main.
  </behavior>
  <action>
    Create `src/shell/Home.tsx`: port the reference `Home` component (SECTION render loop,
    `grid(sec)`, `SectionHeader`, archive CARDS/LIST toggle via local `archiveView` state) rendering
    `HomeCard` for each id in `DEFAULT_SECTIONS[sec]`. Card `onOpen` may call
    `shellStore.getState().setHomeOpen(false)` then a best-effort dock open (or a no-op placeholder —
    real navigation is out of scope). Section membership/order come from local state seeded with
    DEFAULT_SECTIONS this plan (persistence + drag are Plan 06-06). Render an empty-section state with
    the UI-SPEC copy. Add `src/shell/Home.module.css` (overlay container `position:absolute; inset:0;
    z-index:55; overflow-y:auto; background var(--color-bg)`; padding 20px 24px). In
    `DiviChip.tsx`: replace the stale `railApplet === "Home"` selector with
    `const active = useShellStore(s => s.homeOpen)` and the no-op handler with
    `onClick={() => shellStore.getState().toggleHomeOpen()}`. In `LogoCluster.tsx`: replace the
    `openHome` no-op body with `shellStore.getState().setHomeOpen(true)`. In `AppShell.tsx`: read
    `const homeOpen = useShellStore(s => s.homeOpen)` and mount `{homeOpen && <Home />}` as an
    absolute overlay inside the `.main` column (so the title bar and rail stay visible above/beside
    it and dockview stays mounted underneath). Additionally, wire an empty-dock default: after the
    dock api is available (`dockApiRef` from src/shell/dockApi), if there are zero panels, call
    `setHomeOpen(true)` (best-effort, guarded for a null api) — matching D-04 "empty-dock state AND
    summonable". Create `src/shell/Home.test.tsx` covering each <behavior> bullet (render sections;
    DiviChip toggles homeOpen and reflects active; overlay presence gated by homeOpen).
  </action>
  <verify>
    <automated>npx vitest run src/shell/Home.test.tsx src/shell/DiviChip.test.tsx</automated>
  </verify>
  <done>
    Home overlay renders the four sections from a single homeOpen boolean; DiviChip + LogoCluster both
    drive that one boolean (no second "is Home showing" signal); overlay mounts over .main; tests green.
  </done>
  <acceptance_criteria>
    - `grep -n "PINNED\|FRESH\|LIVING\|ARCHIVE" src/shell/Home.tsx` shows all four sections.
    - `grep -c "railApplet === \"Home\"\|railApplet === 'Home'" src/shell/DiviChip.tsx` returns 0 (stale selector removed).
    - `grep -n "homeOpen" src/shell/DiviChip.tsx src/app/AppShell.tsx` present in both.
    - `grep -n "setHomeOpen(true)" src/shell/LogoCluster.tsx` present.
    - `npx vitest run src/shell/Home.test.tsx` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static cardDefs → DOM | Curated in-repo demo data rendered as cards (no external input this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Injection (XSS) | Card title/foot/excerpt rendered | mitigate | Static in-repo data rendered as React text children; no dangerouslySetInnerHTML |
| T-06-05-02 | Info disclosure | Home overlay covering the dock leaking focus/keys | accept | Overlay is a sibling; dockview underneath is inert while covered — no sensitive data path this phase |
| T-06-05-SC | Tampering | npm installs | accept | No installs this plan (dnd-kit deferred to 06-06) |
</threat_model>

<verification>
- `npx vitest run src/shell src/home` green.
- End-of-phase human-check: DIVI and logo both open Home; four sections render; DIVI highlights while open.
</verification>

<success_criteria>
- HOME-01 satisfied: summonable metro overlay, four sections, curated demo cards, single homeOpen
  source of truth (RESEARCH Pitfall 4 resolved).
</success_criteria>

<output>
Create `.planning/phases/06-dashboard-assistant-home/06-05-SUMMARY.md` when done.
</output>

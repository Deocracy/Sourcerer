---
phase: 06-dashboard-assistant-home
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shell/useRailDrag.ts
  - src/shell/useRailDrag.test.tsx
  - src/shell/railSnap.ts
  - src/shell/railSnap.test.ts
  - src/shell/Rail.tsx
  - src/shell/Rail.module.css
autonomous: true
gap_closure: true
requirements: [RAIL-01, ASST-03]
user_setup: []

must_haves:
  truths:
    - "Dragging the left-rail grip resizes the rail fluidly with the workspace reflowing in step (no lag/tearing) — matching the prototype startRailResize feel."
    - "The left rail cycles expanded / compact / hidden via the title-bar left toggle button, double-click on the grip, and Cmd/Ctrl-backslash."
    - "An interrupted left-rail drag (pointercancel: window blur, OS gesture, touch cancel) tears down without leaving the live snap cue pinned or listeners leaked (parity with useAssistantResize WR-06)."
  artifacts:
    - path: "src/shell/useRailDrag.ts"
      provides: "Live-reflowing resize with pointercancel teardown; workspace relayout in step with the drag"
      contains: "pointercancel"
    - path: "src/shell/railSnap.ts"
      provides: "Pure snap thresholds (unchanged unless diagnosis proves a divergence from the prototype)"
      contains: "snapWidthToMode"
    - path: "src/shell/Rail.tsx"
      provides: "Live width already applied from liveSnap (lines ~55-66) — verified, kept in step with the workspace"
  key_links:
    - from: "src/shell/useRailDrag.ts"
      to: "dockview workspace"
      via: "window resize dispatched during pointermove so the center dock re-layouts in step"
      pattern: "resize"
    - from: "src/shell/useRailDrag.ts"
      to: "liveSnap teardown"
      via: "pointercancel handler clears liveSnap and removes listeners"
      pattern: "pointercancel"
---

<objective>
Close GAP-2 from 06-HUMAN-UAT.md: the left-rail drag is janky and its toggles do not behave
like the demo prototype. This is a Phase 2 (RAIL-01) left-rail behavior surfaced as a
regression during Phase 6 live testing; it shares the bespoke-pointer resize pattern that
GAP-1 fixes for the assistant (hence the ASST-03 tag).

Inspection findings to confirm/refine in the diagnosis task:
- Rail.tsx (lines ~55-66) ALREADY applies the live snap width during drag, so the width
  plumbing is not the jank source. The prototype's startRailResize calls `relayout()` on EVERY
  pointermove so the center workspace reflows in step; useRailDrag does not trigger a live
  workspace relayout, so the dock lags the rail during the drag (the likely tearing/jank).
- useRailDrag has NO pointercancel handler — an interrupted drag leaks the move/up listeners and
  can pin the live snap cue. useAssistantResize already fixed this (WR-06); the left rail lacks
  the parity fix.
- The title-bar left toggle (RailToggleButtons first button -> cycleRailMode), the grip
  double-click (onResizeDoubleClick -> cycleRailMode), and Cmd/Ctrl-backslash all exist; the
  "toggle doesn't work" report is likely perceptual jank rather than a broken handler — the
  diagnosis task must confirm which, and only touch handlers if genuinely broken.

Purpose: make the left-rail drag/toggle feel identical to the prototype's startRailResize /
cycleRail. Output: fluid in-step resize, robust drag teardown, verified toggles.

Requirement: RAIL-01 (rail cycle via grip drag/double-click/Cmd-backslash); tagged ASST-03 for
the shared bespoke-pointer resize seam. NOTE: RailToggleButtons.tsx and shellStore.ts are owned
by plan 06-07 — this plan MUST NOT edit them (files stay disjoint so the two gap plans do not
conflict). The left toggle button already calls cycleRailMode correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dashboard-assistant-home/06-HUMAN-UAT.md
@.planning/phases/06-dashboard-assistant-home/06-CONTEXT.md

<interfaces>
Prototype UX source of truth (extract only the left-rail logic):
"NEW Design sync setup guide/design_handoff_bespoke_rails_shell/Sourcerer Bespoke Rails.dc.html"
  - startRailResize (lines ~443-465): move() sets liveW = raw<CLOSE_AT?HIDDEN_W:raw<COMPACT_AT?COMPACT_W:raw AND calls this.relayout() every move; up() commits mode + requestAnimationFrame(relayout).
  - effLeftW (lines ~436-442): liveW overrides while dragging.
  - cycleRail / left toggle binding (lines ~892, ~999): expanded -> compact -> hidden via button, and global Cmd-\ keydown.

Current left-rail code:
  - src/shell/railSnap.ts: HIDDEN_W=6, COMPACT_W=56, CLOSE_AT=44, COMPACT_AT=132, EXPANDED_MAX=520 (mirrors tokens.css; do NOT re-derive).
  - src/shell/useRailDrag.ts: onResizePointerDown uses setPointerCapture + move/up on the grip; NO pointercancel; no live workspace relayout. onResizeDoubleClick -> cycleRailMode; Cmd/Ctrl-\ global keydown present.
  - src/shell/Rail.tsx (lines 55-66): mode/width already derived from liveSnap during drag.
  - useAssistantResize.ts is the WR-06 teardown reference (releasePointerCapture in try/catch, remove pointermove/pointerup/pointercancel, clear live state).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diagnose left-rail divergences vs the prototype and encode testable ones</name>
  <files>src/shell/useRailDrag.test.tsx, src/shell/railSnap.test.ts</files>
  <read_first>
    - src/shell/useRailDrag.ts (onResizePointerDown move/up; no pointercancel; no live relayout)
    - src/shell/railSnap.ts (thresholds — compare 1:1 to the prototype)
    - src/shell/railSnap.test.ts (existing boundary-coverage test conventions)
    - src/shell/Rail.tsx (lines 55-66: liveSnap already drives width — confirm)
    - src/shell/useRailDrag.test.tsx (existing hook test harness, if present)
    - "NEW Design sync setup guide/design_handoff_bespoke_rails_shell/Sourcerer Bespoke Rails.dc.html" (lines 436-465 startRailResize/effLeftW; 892/999 cycleRail + Cmd-\)
  </read_first>
  <action>
    Diff useRailDrag.ts + railSnap.ts + Rail.tsx against the prototype's startRailResize /
    effLeftW / cycleRail and record each divergence with file:line in the SUMMARY. Confirm the
    specific findings: (a) no live workspace relayout during pointermove (prototype calls
    relayout() each move) — the primary jank suspect; (b) no pointercancel teardown (WR-06 parity
    gap) — leaks listeners / pins the live cue; (c) whether railSnap thresholds diverge from the
    prototype (expected: they match — record "no divergence" if so, do NOT change them); (d)
    whether the toggle handlers (left button cycleRailMode, double-click, Cmd-\) actually fire and
    cycle — run the app / inspect and record whether the reported "toggle doesn't work" is a real
    handler bug or perceptual jank. Encode the testable divergences as failing tests:
    - In useRailDrag.test.tsx: assert a pointercancel during a resize drag clears liveSnap and
      removes the move/up listeners (mirroring the useAssistantResize WR-06 test). Fails: no
      pointercancel handling today.
    - If (d) finds a genuinely broken handler, add a test asserting the correct cycle; otherwise
      record that the handlers are correct and the fix is drag-feel only (human-verified).
    Do NOT modify production code in this task. Note explicitly in the SUMMARY that drag "feel"
    (fluidity) is human-verified at end-of-phase since jsdom cannot exercise live pointer feel.
  </action>
  <verify>
    <automated>npx vitest run src/shell/useRailDrag.test.tsx src/shell/railSnap.test.ts 2>&1 | grep -E "fail|FAIL|pass"</automated>
  </verify>
  <acceptance_criteria>
    - Divergence list in SUMMARY with file:line for (a) missing live relayout, (b) missing pointercancel, (c) threshold comparison result, (d) toggle-handler verification result.
    - A failing pointercancel-teardown test exists in useRailDrag.test.tsx (fail-pre-fix).
    - railSnap thresholds are documented as matching the prototype (or the specific divergence is named); no threshold changed in this task.
    - No production source file changed in this task (only *.test.* files).
  </acceptance_criteria>
  <done>Left-rail divergences enumerated with file:line; failing pointercancel test committed; toggle-handler status confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix drag jank — live workspace relayout in step + pointercancel teardown</name>
  <files>src/shell/useRailDrag.ts, src/shell/Rail.module.css</files>
  <read_first>
    - src/shell/useRailDrag.ts (onResizePointerDown move/up flow to extend)
    - src/shell/Rail.tsx (lines 55-66 confirm liveSnap width application stays intact)
    - src/shell/Rail.module.css (confirm no width transition fights the drag; add data-dragging guard only if a transition exists)
    - src/assistant/useAssistantResize.ts (WR-06 teardown reference to mirror exactly)
    - "NEW Design sync setup guide/design_handoff_bespoke_rails_shell/Sourcerer Bespoke Rails.dc.html" (lines 443-465 startRailResize relayout cadence)
  </read_first>
  <action>
    In useRailDrag.onResizePointerDown, dispatch a window resize event during handleMove (after
    setLiveSnap) so dockview re-layouts the center workspace in step with the rail width, matching
    the prototype's per-move relayout() — this removes the dock-lags-rail tearing. Add a shared
    teardown() mirroring useAssistantResize (WR-06): releasePointerCapture in try/catch, remove
    pointermove/pointerup/pointercancel listeners, clear liveSnap; call it from both handleUp and a
    new handleCancel bound to pointercancel. A cancelled drag applies NO snap (rail stays at its
    pre-drag mode/width). Do not change railSnap thresholds. If Rail.module.css has a width
    transition on the rail element, suppress it during the drag (data-dragging attribute) so the
    live width never lags the pointer; if there is no transition, record that and add none.
  </action>
  <verify>
    <automated>npx vitest run src/shell/useRailDrag.test.tsx src/shell/railSnap.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - handleMove dispatches a window resize each pointermove so the workspace reflows in step (Task 1 jank diagnosis addressed).
    - pointercancel is handled via a shared teardown clearing liveSnap and removing all listeners; the Task 1 pointercancel test is green.
    - A cancelled drag applies no snap; a normal drag still commits mode/width on pointerup as before.
    - railSnap thresholds unchanged; tsc --noEmit clean; no rail-metric/token changes.
  </acceptance_criteria>
  <done>Left-rail grip drag resizes fluidly with the workspace reflowing in step, and interrupted drags tear down cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify (and only if broken, fix) the left-rail toggle paths</name>
  <files>src/shell/useRailDrag.ts, src/shell/useRailDrag.test.tsx</files>
  <read_first>
    - src/shell/useRailDrag.ts (onResizeDoubleClick -> cycleRailMode; Cmd/Ctrl-\ keydown effect)
    - src/shell/Rail.tsx (line ~74 grip onClick cycleRailMode; line ~79/206 onDoubleClick wiring)
    - src/shell/useRailDrag.test.tsx (add/confirm toggle coverage)
    - "NEW Design sync setup guide/design_handoff_bespoke_rails_shell/Sourcerer Bespoke Rails.dc.html" (cycleRail: expanded -> compact -> hidden bounce, Cmd-\)
  </read_first>
  <action>
    Confirm the three left-rail toggle paths cycle expanded -> compact -> hidden as in the
    prototype cycleRail: (1) the grip double-click (onResizeDoubleClick), (2) Cmd/Ctrl-backslash
    global keydown, (3) the grip single-click at line ~74. Add a test asserting double-click and
    Cmd-\ each advance railMode by one cycle step. The RailToggleButtons title-bar left button is
    owned by plan 06-07 and already calls cycleRailMode — do NOT edit RailToggleButtons.tsx here.
    Only if the diagnosis (Task 1) found a genuinely broken handler in useRailDrag/Rail (e.g. a
    single-click drag-threshold that swallows the click, or a liveSnap state that pins the mode
    after a canceled drag), apply the minimal fix. If all handlers are already correct, record that
    and rely on the Task 2 drag-feel fix for the reported "toggle" perception, noting human
    verification at end-of-phase.
  </action>
  <verify>
    <automated>npx vitest run src/shell/useRailDrag.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert double-click and Cmd/Ctrl-\ each advance railMode one cycle step (expanded -> compact -> hidden -> expanded).
    - RailToggleButtons.tsx and shellStore.ts are NOT modified by this plan.
    - Any handler fix (if needed) is minimal and covered by a test; if no fix needed, SUMMARY records why.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>Left-rail expanded/compact/hidden cycling is verified across double-click, Cmd-\, and click; any real handler bug fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer input -> rail width | Live drag width already clamped to [0, EXPANDED_MAX=520] in railSnap; this plan adds no new input source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06g2-01 | Denial of Service | window resize dispatch during pointermove | mitigate | Dispatched only while a resize drag is active; the shared teardown (pointerup/pointercancel) removes listeners so no runaway resize storms after the drag ends. |
| T-06g2-02 | Tampering | liveSnap left pinned after interrupted drag | mitigate | New pointercancel teardown clears liveSnap and releases pointer capture, preventing a stuck rail mode/cue (WR-06 parity). |
</threat_model>

<verification>
- Full suite green: `npx vitest run` (no regression from baseline; new pointercancel + toggle tests included).
- `npx tsc --noEmit` clean.
- Human verification (end-of-phase, feel): drag the left-rail grip and confirm the rail AND the center workspace resize together with no lag/tearing; cycle the rail via double-click, Cmd-\, and the title-bar left button; interrupt a drag (e.g. Alt-Tab mid-drag) and confirm the rail settles at its pre-drag width with no stuck cue.
</verification>

<success_criteria>
- GAP-2's three must_have truths hold: fluid in-step left-rail resize, working expanded/compact/hidden cycling across all three paths, and clean pointercancel teardown.
- Files stay disjoint from plan 06-07 (no edits to RailToggleButtons.tsx or shellStore.ts); railSnap thresholds unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-dashboard-assistant-home/06-08-SUMMARY.md` when done, including the Task 1 divergence list (file:line), the threshold-comparison result, and the toggle-handler verification outcome.
</output>

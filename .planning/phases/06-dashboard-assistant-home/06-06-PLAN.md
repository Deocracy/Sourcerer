---
phase: 06-dashboard-assistant-home
plan: 06
type: execute
wave: 3
depends_on: [06-03, 06-05]
files_modified:
  - package.json
  - src/home/SortableCard.tsx
  - src/home/homeCardsReducer.ts
  - src/home/homeCardsReducer.test.ts
  - src/home/homeCards.storage.ts
  - src/shell/Home.tsx
  - src/shell/Home.module.css
  - src/home/Home.dnd.test.tsx
autonomous: true
requirements: [HOME-02]
user_setup: []
must_haves:
  truths:
    - "User can drag a card to reorder within a section and move it between sections, with dnd-kit FLIP transform"
    - "Section membership and order survive an app restart (persisted via host.storage, debounced)"
    - "Approving an assistant proposal and clicking ＋MAKE CARD mints a new card into Home (D-06 consumer half)"
  artifacts:
    - path: "src/home/homeCardsReducer.ts"
      provides: "Pure move/reorder reducer over the SectionMap (independently unit-tested)"
      contains: "export function"
    - path: "src/home/homeCards.storage.ts"
      provides: "Debounced host.storage read/write for section membership+order (D-05)"
      contains: "home-cards-v1"
    - path: "src/home/SortableCard.tsx"
      provides: "useSortable wrapper port"
      contains: "useSortable"
  key_links:
    - from: "src/shell/Home.tsx"
      to: "src/home/homeCards.storage.ts"
      via: "debounced host.storage.set on onDragEnd section state"
      pattern: "home-cards-v1"
    - from: "src/shell/Home.tsx"
      to: "src/store/shellStore.ts"
      via: "consume pendingCardMint -> add card + clearPendingCardMint (D-06)"
      pattern: "pendingCardMint|clearPendingCardMint"
---

<objective>
## Phase Goal

**As a** scholar, **I want to** rearrange my Home cards by dragging them within and between the
PINNED/FRESH/LIVING/ARCHIVE sections and have that layout stick, and turn an approved assistant
proposal into a new card, **so that** Home is a durable, self-curated command surface wired to the
assistant.

Deliver HOME-02: install dnd-kit (locked, audited `[OK]`), wrap the Plan-06-05 static cards in a
`DndContext` + per-section `SortableContext` (FLIP via dnd-kit's own transform/transition), persist
section membership+order through `host.storage` debounced (RESEARCH Pitfall 3), and close the D-06
loop by consuming `shellStore.pendingCardMint` (written by the assistant's ＋MAKE CARD in Plan 06-03)
to mint a new card into Home. The drag reducer is extracted as a pure function and unit-tested
independently of dnd-kit's DOM wiring (RESEARCH Wave-0 note).

Purpose: HOME-02 (drag between sections + FLIP + persistence + assistant ＋MAKE CARD mint).
Output: dnd-kit install, `SortableCard.tsx`, `homeCardsReducer.ts` (+test), `homeCards.storage.ts`,
DndContext-wired `Home.tsx`, D-06 consumer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dashboard-assistant-home/06-CONTEXT.md
@.planning/phases/06-dashboard-assistant-home/06-UI-SPEC.md
@.planning/phases/06-dashboard-assistant-home/06-PATTERNS.md
@.planning/phases/06-dashboard-assistant-home/06-RESEARCH.md

<interfaces>
dnd-kit (RESEARCH Standard Stack — pinned, audited [OK]):
```
@dnd-kit/core@6.3.1  @dnd-kit/sortable@10.0.0  @dnd-kit/utilities@3.2.2
```
Port source (home-cards.js): `SortableCard` (useSortable shape), `OverlayCard`, sensors
`useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 6 } }))`, `onDragOver`,
`onDragEnd`, `DndContext`/`SortableContext(rectSortingStrategy)`/`DragOverlay`.

host.storage best-effort contract (src/host/storage.ts makeAppletStorage) — never throws:
```typescript
get<T>(key, fallback): Promise<T>;  set(key, value): Promise<void>;  remove(key): Promise<void>;
```
Debounce pattern to mirror (src/persistence/workspaceStore.ts scheduleWorkspaceSave):
```typescript
let saveTimer; export function scheduleSave(){ if(saveTimer) clearTimeout(saveTimer); saveTimer=setTimeout(flush, 300); }
```
shellStore D-06 (after Plan 06-01/06-03): `useShellStore(s => s.pendingCardMint): {title,foot}|null`,
`shellStore.getState().clearPendingCardMint()`.

Reducer contract (extract from home-cards.js lines 283–308, make pure):
```typescript
type SectionMap = { pins: string[]; fresh: string[]; living: string[]; archive: string[] };
// pure fns over SectionMap, no React/DOM:
export function moveBetweenSections(map, activeId, overId): SectionMap; // onDragOver logic
export function reorderWithinSection(map, activeId, overId): SectionMap; // onDragEnd arrayMove logic
export function findSection(map, id): keyof SectionMap | null;
```
</interfaces>

Note: `Home is a shell surface, not an applet` — do NOT import `makeHost()`. Use a dedicated
shell-scoped storage helper analogous to `makeAppletStorage("home")` (RESEARCH Pitfall 1).
Minted card lands in FRESH (D-06 planner's call); card content stays the curated demo data plus the
minted title/foot.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dnd-kit + pure homeCardsReducer.ts + tests</name>
  <read_first>
    - .planning/phases/06-dashboard-assistant-home/06-RESEARCH.md (Package Legitimacy Audit — all three [OK] approved; version pins)
    - NEW Design sync setup guide/design_handoff_bespoke_rails_shell/home-cards.js (lines 283–308: findSec/onDragOver/onDragEnd)
    - src/home/cardDefs.ts (SectionMap keys: pins/fresh/living/archive)
  </read_first>
  <behavior>
    - findSection(map, id) returns the section key containing id, or null.
    - moveBetweenSections removes activeId from its section and inserts it at the over-target index in the destination section; no-op when same section.
    - reorderWithinSection arrayMoves activeId to overId's index within one section; no-op when indices equal or id missing.
    - All three functions return a NEW SectionMap (no mutation of the input).
  </behavior>
  <action>
    Install the three pinned packages: `npm install @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0
    @dnd-kit/utilities@3.2.2` (RESEARCH audit cleared all three `[OK]` against the live registry — no
    legitimacy checkpoint required). Create `src/home/homeCardsReducer.ts` extracting the reference's
    `findSec`/`onDragOver`/`onDragEnd` bodies into the three pure functions in the <interfaces> block
    (import `arrayMove` from `@dnd-kit/sortable`), operating over `SectionMap` with no React/DOM. Create
    `src/home/homeCardsReducer.test.ts` covering each <behavior> bullet with plain object fixtures
    (mirrors railSnap.test.ts structure — pure, no dnd-kit DOM), including the same-section no-op and
    the immutability assertion (input object unchanged).
  </action>
  <verify>
    <automated>npx vitest run src/home/homeCardsReducer.test.ts</automated>
  </verify>
  <done>
    dnd-kit installed at the pinned versions; reducer functions are pure, immutable, and correct for
    move/reorder/find; tests green.
  </done>
  <acceptance_criteria>
    - `grep -n "@dnd-kit/core\|@dnd-kit/sortable\|@dnd-kit/utilities" package.json` shows all three at the pinned versions.
    - `grep -n "export function moveBetweenSections\|export function reorderWithinSection\|export function findSection" src/home/homeCardsReducer.ts` all present.
    - `grep -c "react" src/home/homeCardsReducer.ts` returns 0.
    - `npx vitest run src/home/homeCardsReducer.test.ts` passes including the immutability case.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SortableCard + homeCards.storage debounced persistence</name>
  <read_first>
    - NEW Design sync setup guide/design_handoff_bespoke_rails_shell/home-cards.js (SortableCard lines 234–254, OverlayCard 256–261)
    - src/home/HomeCard.tsx (CardFrame/CardBody from Plan 06-05 — SortableCard wraps these)
    - src/host/storage.ts (makeAppletStorage best-effort contract)
    - src/persistence/workspaceStore.ts (scheduleWorkspaceSave debounce shape)
  </read_first>
  <action>
    Create `src/home/SortableCard.tsx`: port the reference `SortableCard` — call
    `const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id })`,
    compute `style = { transform: CSS.Transform.toString(transform), transition, opacity: isDragging ? 0.25 : 1 }`
    (import `CSS` from `@dnd-kit/utilities`), and render `HomeCard`/`CardFrame` with `innerRef=setNodeRef`
    and `extra={...attributes, ...listeners, onClick}` guarded against click-after-drag (the 250ms
    lastDrag guard from the reference). Also port `OverlayCard` for the DragOverlay. Create
    `src/home/homeCards.storage.ts`: a dedicated shell-scoped storage helper (`makeAppletStorage("home")`
    or an equivalent shell-scoped factory — do NOT use `makeHost()`), exposing `loadSections(): Promise<SectionMap>`
    (returns the persisted `home-cards-v1` value or DEFAULT_SECTIONS) and `scheduleSaveSections(map: SectionMap)`
    that debounces (300ms, mirror scheduleWorkspaceSave) a single best-effort `host.storage.set("home-cards-v1", map)`
    — so a rapid drag firing onDragOver on every hover frame does NOT fire a disk write per frame
    (RESEARCH Pitfall 3). On a write failure the contract never throws (WR-01).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
    SortableCard renders a draggable card via useSortable with the 250ms click-after-drag guard;
    homeCards.storage.ts loads/saves the SectionMap under `home-cards-v1` with a 300ms debounce,
    never throwing; compiles clean.
  </done>
  <acceptance_criteria>
    - `grep -n "useSortable\|CSS.Transform.toString" src/home/SortableCard.tsx` present.
    - `grep -n "home-cards-v1" src/home/homeCards.storage.ts` present.
    - `grep -n "setTimeout\|clearTimeout" src/home/homeCards.storage.ts` shows the debounce.
    - `grep -c "makeHost" src/home/homeCards.storage.ts` returns 0 (shell-scoped, not applet host).
    - `npx tsc --noEmit` clean for src/home.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire DndContext into Home.tsx + persistence + D-06 ＋MAKE CARD consumer</name>
  <read_first>
    - src/shell/Home.tsx (Plan 06-05 static version — sections render, grid())
    - src/home/SortableCard.tsx, src/home/homeCardsReducer.ts, src/home/homeCards.storage.ts (Tasks 1–2)
    - src/store/shellStore.ts (pendingCardMint, clearPendingCardMint from Plan 06-01)
    - NEW Design sync setup guide/design_handoff_bespoke_rails_shell/home-cards.js (DndContext wiring 323–330, grid 310–315)
  </read_first>
  <action>
    Edit `src/shell/Home.tsx`: hold `sections` in state seeded via `loadSections()` (async, resolved
    after first paint — start from DEFAULT_SECTIONS, replace on resolve). Wrap the four sections in a
    `DndContext` (sensors = `useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 6 } }))`,
    `collisionDetection={closestCenter}`) with `onDragStart`/`onDragOver`/`onDragEnd`/`onDragCancel`
    delegating to the pure reducer (`moveBetweenSections` on over, `reorderWithinSection` on end), each
    section a `<SortableContext items={sections[sec]} strategy={rectSortingStrategy}>` rendering
    `SortableCard`, plus a `<DragOverlay>` with `OverlayCard`. After every `onDragEnd` state update,
    call `scheduleSaveSections(nextSections)` (debounced — do NOT persist on intermediate onDragOver
    frames). For D-06: read `const pendingCardMint = useShellStore(s => s.pendingCardMint)`; in an
    effect keyed on it, when non-null mint a new card — allocate a fresh id, register its def in a
    local minted-cards map merged with `cardDefs` for HomeCard lookup (title/foot from
    `pendingCardMint`, a sensible default `kind`), prepend the id to `sections.fresh`, persist via
    `scheduleSaveSections`, then `shellStore.getState().clearPendingCardMint()`. Ensure HomeCard/
    SortableCard resolve a card def from the merged (cardDefs + minted) lookup so minted cards render.
    Create `src/home/Home.dnd.test.tsx`: since dnd-kit pointer simulation in jsdom is unreliable
    (RESEARCH Wave-0 note), assert the reducer-driven state transitions at the component boundary
    where feasible AND assert the D-06 path directly — set `pendingCardMint` in the store, render
    Home, and verify a new card with the minted title appears in FRESH and `pendingCardMint` is
    cleared. (The pure drag transitions are already covered by homeCardsReducer.test.ts.)
  </action>
  <verify>
    <automated>npx vitest run src/home/Home.dnd.test.tsx</automated>
  </verify>
  <done>
    Home cards are draggable within/between sections with dnd-kit FLIP; layout persists via debounced
    host.storage; approving a proposal + ＋MAKE CARD mints a card into FRESH and clears pendingCardMint;
    tests green.
  </done>
  <acceptance_criteria>
    - `grep -n "DndContext\|SortableContext\|DragOverlay" src/shell/Home.tsx` all present.
    - `grep -n "scheduleSaveSections" src/shell/Home.tsx` present (called after onDragEnd, not per onDragOver frame).
    - `grep -n "pendingCardMint\|clearPendingCardMint" src/shell/Home.tsx` present.
    - `npx vitest run src/home/Home.dnd.test.tsx` passes the D-06 mint assertion.
    - `npx vitest run` (full suite) stays green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| applets.json (host.storage) → Home | Persisted section map loaded at mount |
| shellStore.pendingCardMint (model-derived) → Home card | Minted card title/foot originates from model text |
| npm registry → build | Three new dnd-kit packages installed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | Corrupt persisted `home-cards-v1` map | mitigate | loadSections falls back to DEFAULT_SECTIONS on any read error (host.storage never throws); ids not in cardDefs∪minted are skipped at render |
| T-06-06-02 | Injection (XSS) | Minted card title/foot from model text | mitigate | Rendered as React text children only; title bounded/truncated; never dangerouslySetInnerHTML |
| T-06-06-03 | Denial of Service | Rapid drag firing many disk writes | mitigate | 300ms debounce persists only the settled onDragEnd state (RESEARCH Pitfall 3) |
| T-06-06-SC | Tampering | dnd-kit npm install (supply chain) | mitigate | RESEARCH Package Legitimacy Audit: all three `[OK]` (slopcheck 2026-07-14, same clauderic/dnd-kit monorepo CLAUDE.md already locked); versions pinned exactly |
</threat_model>

<verification>
- `npx vitest run` full suite green (reducer + D-06 mint covered; dnd-kit pointer DOM sim not relied on).
- Post-install smoke (RESEARCH A2): confirm `useSortable()` returns the expected shape
  (`attributes, listeners, setNodeRef, transform, transition, isDragging`) after the 6.x→10.x bump.
- End-of-phase human-check: drag a card between sections (persists across restart); approve a
  proposal + ＋MAKE CARD -> a card appears in FRESH.
</verification>

<success_criteria>
- HOME-02 satisfied: drag between sections with FLIP, persisted membership/order, and the assistant
  ＋MAKE CARD mint closing the D-06 loop end-to-end (proposal accept -> pendingCardMint -> Home card).
</success_criteria>

<output>
Create `.planning/phases/06-dashboard-assistant-home/06-06-SUMMARY.md` when done.
</output>

---
phase: 03-persistence-layouts
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/shell/Dock.tsx
  - src/store/shellStore.ts
  - src/store/shellStore.test.ts
autonomous: true
requirements: [PERS-01]
user_setup: []

must_haves:
  truths:
    - "The whole dock tree persists on change and restores on launch through workspace.json (no localStorage)"
    - "Rail order/pins/mode/width persist on change and restore on launch through the same unified record"
    - "The crash-on-restore canary is honored from the record's restoreCanary field, not a localStorage key"
  artifacts:
    - path: "src/shell/Dock.tsx"
      provides: "dockview hydrate-from-record + register dock snapshot source + route layout changes into scheduleWorkspaceSave"
      contains: "registerStateSources"
    - path: "src/store/shellStore.ts"
      provides: "rail subset persisted via workspaceStore record; hydrateFromDisk(); actions call scheduleWorkspaceSave"
      contains: "scheduleWorkspaceSave"
  key_links:
    - from: "src/shell/Dock.tsx"
      to: "src/persistence/workspaceStore.ts"
      via: "registerStateSources getDockTree + scheduleWorkspaceSave"
      pattern: "scheduleWorkspaceSave|registerStateSources"
    - from: "src/store/shellStore.ts"
      to: "src/persistence/workspaceStore.ts"
      via: "scheduleWorkspaceSave on each rail action + hydrateFromDisk"
      pattern: "scheduleWorkspaceSave"
---

<objective>
Complete the persist/restore vertical slice: re-home the two Phase-2 localStorage scaffolds (`sourcerer-dockview-bespoke-v2` in Dock.tsx, `sourcerer-shell-store-v1` in shellStore.ts) into the unified `workspace.json` record from 03-01. After this plan a user's whole workspace — dock tree AND rail order/pins/mode/width — survives relaunch through plugin-store, satisfying PERS-01's "persists on change and restores on launch."

Purpose: this is the user-observable capability of the slice — the record engine from 03-01 becomes live by registering Dock + rail as its state sources and consuming its load on boot.
Output: modified Dock.tsx + shellStore.ts (localStorage removed), a shellStore hydrate test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-persistence-layouts/03-CONTEXT.md
@.planning/phases/03-persistence-layouts/03-PATTERNS.md
@.planning/phases/03-persistence-layouts/03-01-SUMMARY.md
@src/persistence/workspaceStore.ts

<interfaces>
<!-- Consuming the 03-01 seam. Both consumers register live getters and call the shared writer. -->
registerStateSources({ getDockTree: () => unknown|null, getRail: () => RailSubset }): void
loadWorkspaceRecord(): Promise<WorkspaceRecordV1>
scheduleWorkspaceSave(): void
DEFAULT_WORKSPACE: WorkspaceRecordV1

RailSubset = { railMode, railWidth, railOrder, leftRailPinned }  // exactly shellStore's persisted D-02 subset
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-home shellStore rail persistence into the unified record</name>
  <files>src/store/shellStore.ts, src/store/shellStore.test.ts</files>
  <read_first>
    - src/store/shellStore.ts (full file — LS_KEY, load(), persist(), the createStore init reading `saved`, all rail actions that call persist(get))
    - src/persistence/workspaceStore.ts (registerStateSources, loadWorkspaceRecord, scheduleWorkspaceSave, DEFAULT_WORKSPACE — the seam to consume)
    - src/shell/TitleBar.test.tsx (Vitest idiom for the new shellStore.test.ts)
  </read_first>
  <action>
    Remove `LS_KEY`, `load()`, `persist()`, and the module-init `const saved = load()` from shellStore.ts. Keep the synchronous createStore initialization but seed the persisted fields (railMode, railWidth, railOrder, leftRailPinned) from `DEFAULT_WORKSPACE.rail` so first render is valid before disk hydration. Replace every `persist(get)` call inside the rail actions (setRailMode, cycleRailMode, setRailWidth, reorderRail, togglePin) with a call to `scheduleWorkspaceSave()` imported from ../persistence/workspaceStore — the single writer now owns disk I/O. Register the rail state source once at module load: `registerStateSources`-compatible getter `getRail: () => ({ railMode, railWidth, railOrder, leftRailPinned })` reading `shellStore.getState()` (co-register with Dock's getDockTree — see Task 2 note; if registerStateSources takes both getters together, expose a `registerRailSource`-style partial or have Dock pass both — pick one registration call site so neither getter clobbers the other; simplest: workspaceStore.registerStateSources accepts a partial and merges). Add an exported async `hydrateFromDisk()` that awaits `loadWorkspaceRecord()` and `shellStore.setState()`s the rail subset from `record.rail` (also set railOpen = railMode !== "hidden"); it will be called once at app boot in Task 2's mount path (or App bootstrap). Do NOT persist the session-only fields (activeCorpus/activePaneId/railApplet/badges) — keep them out of the record exactly as the D-02 subset excluded them.
  </action>
  <verify>
    <automated>npm test -- --run src/store/shellStore.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "localStorage" src/store/shellStore.ts` (localStorage fully removed).
    - `grep -c "scheduleWorkspaceSave" src/store/shellStore.ts` >= 5 (one per rail mutating action).
    - shellStore.ts exports `hydrateFromDisk`.
    - New `src/store/shellStore.test.ts` proves: after `hydrateFromDisk()` resolves against a mocked record with railMode "compact"/railWidth 180, the store's railMode is "compact" and railWidth 180; and calling `setRailMode("hidden")` triggers `scheduleWorkspaceSave` (spy) exactly once.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Rail state persists and restores through workspace.json; no localStorage remains in shellStore.</done>
</task>

<task type="auto">
  <name>Task 2: Re-home Dock persistence + hydrate dock tree from the record</name>
  <files>src/shell/Dock.tsx</files>
  <read_first>
    - src/shell/Dock.tsx (full file — LAYOUT_KEY/CANARY_KEY block lines 14-15 + 129-186, dockApiRef, addApplet Wiki/Library default, onDidLayoutChange debounce, cleanup)
    - src/persistence/workspaceStore.ts (registerStateSources getDockTree, loadWorkspaceRecord, scheduleWorkspaceSave, restoreCanary handling)
    - src/store/shellStore.ts (hydrateFromDisk from Task 1 — call it alongside dock hydration at boot)
    - .planning/phases/03-persistence-layouts/03-PATTERNS.md (§workspaceStore.ts re-home instructions — keep the canary-then-4s-clear SHAPE)
  </read_first>
  <action>
    Delete `LAYOUT_KEY`, `CANARY_KEY`, and the localStorage canary/restore/save block from Dock.tsx. Register Dock's dock-tree snapshot source once inside the mount effect: `getDockTree: () => dockApiRef.current ? dockApiRef.current.toJSON() : null` via workspaceStore.registerStateSources (merging with the rail getter from Task 1). Replace the restore path: `await loadWorkspaceRecord()`, and if `record.restoreCanary` is already set, treat the persisted `record.dockTree` as poisoned and fall through to the Wiki+Library default (same semantics as the old canary-set-on-crash check); otherwise set restoreCanary true + persist, then `api.fromJSON(record.dockTree)` inside a try/catch when dockTree is non-null, clearing the canary ~4s later via scheduleWorkspaceSave (re-home the exact 4s-clear shape). If dockTree is null or restore throws or leaves zero panels, `api.clear()` then `addApplet("Wiki")` + `addApplet("Library")` (DEFAULT_WORKSPACE, D-05). Replace the `onDidLayoutChange` 300ms localStorage debounce with a single `onDidLayoutChange(() => scheduleWorkspaceSave())` — the debounce now lives in the writer (do NOT keep a second timer here; RESEARCH Pitfall 3). Because plugin-store load is async while dockview mounts synchronously, structure the effect so it creates the dockview instance, registers the source, then runs an async IIFE that awaits loadWorkspaceRecord() before hydrating; guard against the effect cleanup racing the async restore (check the instance is still current). Call shellStore's `hydrateFromDisk()` once here (or in App bootstrap) so rail + dock hydrate from the same load. Keep dockApiRef, addAppletToDock, getDockGroupRects, the "+" action, and onDidActivePanelChange focus wiring unchanged.
  </action>
  <verify>
    <automated>npm test -- --run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "localStorage" src/shell/Dock.tsx` (localStorage fully removed).
    - `grep -c "scheduleWorkspaceSave" src/shell/Dock.tsx` >= 1 and `grep -c "loadWorkspaceRecord" src/shell/Dock.tsx` == 1.
    - `grep -c "registerStateSources" src/shell/Dock.tsx` == 1 with a getDockTree returning `dockApiRef.current.toJSON()`.
    - The Wiki+Library default branch (`addApplet("Wiki")` + `addApplet("Library")`) is preserved for the null/empty/throw path.
    - `npm run build` (tsc + vite build) exits 0; full `npm test -- --run` green.
  </acceptance_criteria>
  <done>Dock tree persists/restores through workspace.json with the canary re-homed onto the record; PERS-01 slice is live end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace.json dockTree → api.fromJSON | Restored dockview JSON is untrusted; a poisoned tree could crash the dock on hydrate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | DoS | Dock.tsx api.fromJSON hydrate | mitigate | restoreCanary crash-guard re-homed onto the record + try/catch around fromJSON with Wiki+Library default fallback — a crash-on-restore layout is dropped, never re-applied |
| T-03-04 | DoS | shellStore hydrateFromDisk | mitigate | 03-01's loadWorkspaceRecord already validates+defaults; hydrate only setState()s a validated rail subset, never raw disk bytes |
</threat_model>

<verification>
- Full `npm test -- --run` green (shellStore hydrate + all existing suites).
- `npm run build` green.
- No `localStorage` token remains in Dock.tsx or shellStore.ts.
</verification>

<success_criteria>
- Whole workspace (dock tree + rail subset) persists on change and restores on launch via the unified plugin-store record — PERS-01 live.
- Both localStorage scaffolds removed; the crash-guard canary re-homed onto the record.
</success_criteria>

<output>
Create `.planning/phases/03-persistence-layouts/03-02-SUMMARY.md` when done.
</output>

---
phase: 02-workspace-core
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - src/shell/useRailDragOut.ts
  - src/shell/RailDragGhost.tsx
  - src/shell/RailDragGhost.module.css
  - src/shell/dockZones.ts
  - src/shell/dockZones.test.ts
  - src/shell/Dock.tsx
  - src/shell/Rail.tsx
autonomous: false
requirements: [RAIL-02, DOCK-02]
must_haves:
  truths:
    - "D-01: dragging a rail item out past the rail's right edge docks it as a new dockview panel with the full bespoke preview — floating ghost + green 28%-zone drop overlay"
    - "Outer 28% of the target dockview group maps to left/right/top/bottom (referenceGroup + direction into api.addPanel); center 44% = tab-join (RAIL-02, DOCK-02)"
    - "If zone computation fails or the drop is ambiguous, the drop resolves to a new tab (working fallback path) so a fragile preview never blocks the phase"
  artifacts:
    - path: "src/shell/useRailDragOut.ts"
      provides: "Bespoke pointer drag-out bridging rail -> dockview api.addPanel"
    - path: "src/shell/dockZones.ts"
      provides: "Pure zone math: point + group rects -> {referenceGroup, direction} | tab-join | null"
      exports: ["resolveDropZone"]
    - path: "src/shell/RailDragGhost.tsx"
      provides: "Floating cursor ghost (glyph + title, #131418/#26272B)"
  key_links:
    - from: "src/shell/useRailDragOut.ts"
      to: "dockview api.addPanel"
      via: "resolveDropZone -> position {referenceGroup, direction}"
      pattern: "addPanel"
    - from: "src/shell/useRailDragOut.ts"
      to: "src/shell/dockZones.ts"
      via: "resolveDropZone on pointermove/up"
      pattern: "resolveDropZone"
---

<objective>
Build the phase's highest-risk slice (D-01): dragging a rail item out past the rail's right edge docks it into dockview as a new panel with the full bespoke preview — a floating ghost following the cursor and a green 28%-zone drop overlay resolving to a dockview group + direction. Includes a working fallback (ambiguous drop -> new tab). Ends with the consolidated human-verify of the whole interactive workspace.

Purpose: Completes RAIL-02 (drag into workspace to dock) + the bespoke half of DOCK-02, and verifies the full Phase 2 workspace end-to-end.
Output: New drag-out hook, ghost component, pure zone-math module + tests, wired into Dock/Rail, plus the final checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspace-core/02-UI-SPEC.md
@.planning/phases/02-workspace-core/02-PATTERNS.md
@.planning/phases/02-workspace-core/02-CONTEXT.md

<interfaces>
<!-- The one seam bridging bespoke pointer logic and dockview (CONTEXT D-01). -->
<!-- dockview groups: iterate api.groups, each group.element.getBoundingClientRect() for zone math. -->
<!-- api.addPanel({ id:`${key}:${nanoid()}`, component:key, title, position:{ referenceGroup, direction } }) -->
<!--   direction: 'left'|'right'|'above'|'below' for splits; omit position (or 'within') for tab-join. -->
<!-- Overlay colors (UI-SPEC): fill rgba(134,163,140,0.18), accent border #86A38C. -->
<!-- Ghost (UI-SPEC): glyph + title, bg #131418, border #26272B. Threshold: 5px move to start drag. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure drop-zone math (28% edges / 44% center) + tests</name>
  <files>src/shell/dockZones.ts, src/shell/dockZones.test.ts</files>
  <read_first>
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Left Rail — drag-out: outer 28% -> left/right/top/bottom, center 44% = tab-join)
    - .planning/phases/02-workspace-core/02-CONTEXT.md (D-01 — 28% zones, referenceGroup+direction, fallback to new tab)
    - .planning/phases/02-workspace-core/02-PATTERNS.md (Rail.tsx drag-out note — getBoundingClientRect zone math, fallback)
  </read_first>
  <behavior>
    - resolveDropZone(point, groupRects): given a pointer {x,y} and an array of {groupId, rect}, find the group whose rect contains the point.
    - Within that rect, if the point is in the outer 28% band on the left -> {referenceGroup: groupId, direction: 'left'}; right -> 'right'; top -> 'above'; bottom -> 'below'.
    - If the point is in the center 44% region -> {referenceGroup: groupId, direction: undefined} (tab-join).
    - If no group contains the point -> null (caller uses the new-tab fallback).
    - Corner precedence and exact band math must be deterministic and unit-tested at boundaries (28% edges, 44% center, corners, outside).
  </behavior>
  <action>
    Create `dockZones.ts` exporting the pure `resolveDropZone(point, groupRects)` per the behavior block (no DOM access — take precomputed rects so it is unit-testable). Encode the 28% edge band and 44% center as fractions of each rect's width/height. Write `dockZones.test.ts` covering: point in each of the four 28% edges (correct direction), center 44% (tab-join), corners (deterministic winner), and a point outside all rects (null).
  </action>
  <verify>
    <automated>npx vitest run src/shell/dockZones.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `dockZones.test.ts` green: four edges map to left/right/above/below, center maps to tab-join, outside maps to null, corners deterministic.
    - `resolveDropZone` is pure (no DOM/window references).
  </acceptance_criteria>
  <done>Zone math unit-tested and deterministic; ready to wire to live dockview rects.</done>
</task>

<task type="auto">
  <name>Task 2: Drag-out hook + floating ghost + green 28% overlay + addPanel (with fallback)</name>
  <files>src/shell/useRailDragOut.ts, src/shell/RailDragGhost.tsx, src/shell/RailDragGhost.module.css, src/shell/Dock.tsx, src/shell/Rail.tsx</files>
  <read_first>
    - src/shell/useRailDrag.ts (plan 04 — the 5px-threshold pointer pattern to branch from for drag-OUT past the rail edge)
    - src/shell/Dock.tsx (plan 05 — expose an addApplet-with-position path + a way to read live group rects)
    - src/shell/Rail.tsx (plan 04 — the rows that initiate the drag)
    - src/shell/dockZones.ts (task 1 — resolveDropZone)
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Left Rail drag-out — ghost + overlay colors/geometry)
    - .planning/phases/02-workspace-core/02-CONTEXT.md (D-01 — full-fidelity preview + fallback path)
  </read_first>
  <action>
    Create `useRailDragOut.ts`: extend the rail row pointer drag so that when the pointer crosses past the rail's right edge into the workspace, it enters drag-out mode. Render a `RailDragGhost` (glyph + title, bg `#131418`, border `#26272B`) following the cursor. On `pointermove`, read live dockview group rects (from a `Dock.tsx`-exposed accessor over `api.groups` -> `element.getBoundingClientRect()`), call `resolveDropZone`, and draw a green drop overlay (fill `rgba(134,163,140,0.18)`, `#86A38C` border) over the resolved edge/center of the target group. On `pointerup`: if `resolveDropZone` returned a zone, call `Dock.tsx`'s addApplet with `position:{ referenceGroup, direction }` (direction undefined = tab-join); if it returned null OR any error is thrown computing zones, resolve to a new tab via a plain `addApplet(key)` (D-01 fallback — a fragile preview never blocks docking). Use `nanoid` ids consistent with plan 05. Wire the hook into `Rail.tsx` rows and expose the needed accessors from `Dock.tsx`. Style the ghost in `RailDragGhost.module.css` (tokens). Keep zone math in the pure module; this file only bridges DOM rects -> `resolveDropZone` -> `api.addPanel`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && node -e "const h=require('fs').readFileSync('src/shell/useRailDragOut.ts','utf8'); for(const t of ['resolveDropZone','addPanel','referenceGroup']) if(!h.includes(t)){console.error('missing',t);process.exit(1)} const l=h.toLowerCase(); if(!l.includes('fallback')&&!l.includes('catch')){console.error('no fallback path');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Dragging a rail row past the rail's right edge shows a floating ghost and a green 28%-zone overlay on the hovered dockview group.
    - Drop in an outer 28% edge splits the group (referenceGroup + direction); center 44% joins as a tab.
    - Ambiguous/failed zone resolution falls back to a new tab (no crash).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Rail drag-out docks panels with full bespoke preview + working fallback; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the full interactive workspace</name>
  <what-built>The complete Phase 2 workspace: 3-mode rail (resize/snap, double-click, Cmd/Ctrl-backslash, reorder, pin), themed dockview center dock (tabs, '+', 5-zone docking, splits, multi-instance, focus, Wiki/Library default + canary restore), and rail drag-out-to-dock with the green 28%-zone preview + ghost.</what-built>
  <how-to-verify>
    1. Launch the app. Confirm the workspace opens with Wiki + Library panels as default tabs/panes.
    2. Rail modes: drag the rail's right edge — width follows the cursor and snaps to hidden (<44px), compact (<132px), or expanded (clamped 132-520). Double-click the handle cycles modes. Press Cmd-\ (or Ctrl-\) — rail cycles. Rail-toggle buttons in the title bar also cycle.
    3. Rail reorder + pin: drag a rail item up/down — a drop-line shows the insertion point and the order changes on release. Pin an item — it moves to the bottom group.
    4. Dock tabs: click '+' in a tab bar to open an applet. Drag a tab to reorder within its bar and to another bar. Confirm 34px tab bars and green accent on drag-over.
    5. 5-zone docking: drag a tab toward a pane's left/right/top/bottom edge and center — dockview shows its native drop preview and docks accordingly; edge-splits work at the workspace boundary. Drag a split resizer — panes resize.
    6. Rail drag-out (the D-01 highlight): drag a rail item OUT past the rail's right edge into the workspace — a floating ghost follows the cursor and a GREEN 28%-zone overlay appears on the hovered pane. Drop in an outer edge -> new split; drop in the center -> new tab. Try a drop in an ambiguous/empty area -> it still docks as a new tab (fallback, no crash).
    7. Multi-instance + focus: open the same applet twice — two panes coexist. Click between panes — focus (active tab styling) follows the clicked pane; the active rail item highlights accordingly.
    8. Prune: close the last tab of a pane — the pane is removed and the layout reflows.
    9. Persistence sanity: rearrange, relaunch — the layout restores (Phase-2 scaffolding). Corrupt `localStorage['sourcerer-dockview-bespoke-v2']` to invalid JSON, relaunch — it silently falls back to Wiki+Library (no crash).
  </how-to-verify>
  <resume-signal>Type "approved" if all nine checks pass, or describe failures (which step, which interaction).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bespoke pointer drag -> dockview api | Drag-out is the one seam mutating dockview layout from bespoke pointer logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Denial | drag-out zone computation over live rects | mitigate | `resolveDropZone` is pure + unit-tested; the hook wraps live-rect resolution in a try/catch that falls back to a new-tab `addPanel` (D-01) so a preview failure never blocks docking or crashes |
| T-02-07 | Denial | pointermove flood during drag-out | accept | Single captured pointer; overlay redraw is bounded; listeners cleaned on pointerup |
</threat_model>

<verification>
- `npx vitest run src/shell/dockZones.test.ts` green.
- Task 2 node gate prints OK; `npm run build` succeeds.
- Human confirms all nine workspace-interaction checks (final gate).
</verification>

<success_criteria>
- Rail drag-out docks panels with the full bespoke green 28%-zone preview + ghost, with a new-tab fallback.
- The full interactive workspace (rail + dock + drag-out) passes human verification.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-core/02-06-SUMMARY.md` when done (record final checkpoint result).
</output>

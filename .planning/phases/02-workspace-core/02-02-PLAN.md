---
phase: 02-workspace-core
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/shell/DiviChip.tsx
  - src/shell/DiviChip.module.css
  - src/shell/RailToggleButtons.tsx
  - src/shell/RailToggleButtons.module.css
  - src/shell/TitleBar.tsx
  - src/shell/TitleBar.module.css
  - src/shell/TitleBar.test.tsx
  - src/App.tsx
  - src/App.module.css
  - src-tauri/tauri.conf.json
autonomous: true
requirements: [RAIL-01]
must_haves:
  truths:
    - "D-03: the floating rounded window (Tauri transparent:true, 20px inset backdrop, 10px inner card) lands as its own slice after the title-bar chrome"
    - "The title bar renders the Sourcerer wordmark, a DIVI toggle chip, a faint corpus label, two rail-toggle SVG buttons, and window controls in a 40px bar"
    - "The rail-toggle buttons call shellStore.cycleRailMode() (a RAIL-01 rail-mode control)"
    - "Exactly one data-tauri-drag-region element survives (the shrunk spacer) — no button or chip swallows drag"
  artifacts:
    - path: "src/shell/DiviChip.tsx"
      provides: "DIVI toggle chip (active/inactive from store, console-stub click)"
    - path: "src/shell/RailToggleButtons.tsx"
      provides: "Two aria-labeled SVG buttons cycling rail mode"
    - path: "src/App.module.css"
      provides: "Floating rounded card + radial backdrop wrapper"
      contains: "border-radius"
  key_links:
    - from: "src/shell/RailToggleButtons.tsx"
      to: "shellStore.cycleRailMode"
      via: "onClick handler"
      pattern: "cycleRailMode"
    - from: "src-tauri/tauri.conf.json"
      to: "floating window"
      via: "transparent:true"
      pattern: "transparent"
---

<objective>
Rework the Phase 1 chrome to the `bespoke_rails_shell` handoff: a 40px title bar with a DIVI toggle chip, corpus label, and two rail-toggle SVG buttons, plus the floating rounded window (Tauri `transparent:true`, radial backdrop, 20px inset, 10px inner card). This is D-03's chrome-rework prerequisite — RAIL/DOCK plans mount into the frame this plan produces.

Purpose: Rail/dock chrome must render inside the correct 40px/floating frame. The rail-toggle buttons are the title-bar rail-mode control for RAIL-01.
Output: New DiviChip + RailToggleButtons components, reworked TitleBar + test, floating-window App wrapper + tauri.conf change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspace-core/02-UI-SPEC.md
@.planning/phases/02-workspace-core/02-PATTERNS.md

<interfaces>
<!-- Current TitleBar (Phase 1): fill the spacer, preserve exactly one drag region. -->
From src/shell/TitleBar.tsx:
```tsx
<div className={styles.titleBar}>
  <LogoCluster />
  <div className={styles.spacer} data-tauri-drag-region />
  <WindowControls />
</div>
```
<!-- Store hook + action available from plan 01: -->
<!-- import { useShellStore, shellStore } from "../store/shellStore"; -->
<!-- shellStore.getState().cycleRailMode(); useShellStore(s => s.railMode) -->
<!-- Guarded-imperative idiom to copy (WindowControls.tsx) — store calls do NOT need the try/catch. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DiviChip + RailToggleButtons components</name>
  <files>src/shell/DiviChip.tsx, src/shell/DiviChip.module.css, src/shell/RailToggleButtons.tsx, src/shell/RailToggleButtons.module.css</files>
  <read_first>
    - src/shell/LogoCluster.tsx (click-stub button idiom to copy for DiviChip)
    - src/shell/WindowControls.tsx (icon-button cluster, aria-label, button-per-action convention for RailToggleButtons)
    - src/shell/TitleBar.module.css (CSS-module token-var convention)
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Chrome Rework — chip active/inactive states; Typography — chip Plex Mono 11px/500)
    - .planning/phases/02-workspace-core/02-PATTERNS.md (DiviChip.tsx, RailToggleButtons.tsx sections)
  </read_first>
  <action>
    Create `DiviChip.tsx`: a functional component (no props) reading an active boolean from the store via `useShellStore` (a Home-toggle slice, e.g. `railApplet === 'Home'`; Phase 2 chip is a chrome-only stub — click handler is a `console.log` no-op matching LogoCluster's stub, NOT wired to any Home overlay per D-03). Render "DIVI" in Plex Mono 11px/500. Its CSS module toggles `.active` (accent border + `rgba(134,163,140,0.14)` bg + `--color-accent` text) vs inactive (`--color-line-2` border + `--color-muted` text), rectangular (zero radius). Create `RailToggleButtons.tsx`: two `<button type="button">` elements with `aria-label`s ("Cycle rail mode" pair per the two 16×12 SVG buttons that "fill to show rail state"), each `onClick` calling `shellStore.getState().cycleRailMode()` (direct store call — no `withWindow` guard needed). Render inline 16×12 `<svg>` rects whose fill reflects `useShellStore(s => s.railMode)`. Both CSS modules follow TitleBar.module.css flat token-var style with `--radius: 0`.
  </action>
  <verify>
    <automated>npx vitest run src/shell/TitleBar.test.tsx 2>&1 | tail -5; node -e "require('fs').accessSync('src/shell/DiviChip.tsx');require('fs').accessSync('src/shell/RailToggleButtons.tsx');console.log('files exist')"</automated>
  </verify>
  <acceptance_criteria>
    - `DiviChip.tsx` renders text "DIVI" and toggles an `.active` class from a store boolean; click handler is a console stub (no Home wiring).
    - `RailToggleButtons.tsx` renders two aria-labeled buttons; each onClick references `cycleRailMode`.
    - Both `.module.css` use `var(--…)` tokens only, zero border-radius.
  </acceptance_criteria>
  <done>Both components + CSS modules exist, compile, and reference store actions per spec.</done>
</task>

<task type="auto">
  <name>Task 2: Rework TitleBar (40px, DIVI chip, corpus label, rail toggles) + test</name>
  <files>src/shell/TitleBar.tsx, src/shell/TitleBar.module.css, src/shell/TitleBar.test.tsx</files>
  <read_first>
    - src/shell/TitleBar.tsx (target — current LogoCluster→spacer→WindowControls row)
    - src/shell/TitleBar.module.css (target — height var, .spacer flex rule)
    - src/shell/TitleBar.test.tsx (target — existing single-drag-region + aria-label assertions to extend, not replace)
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Chrome Rework "Title bar content" row; Typography wordmark 12px/600, corpus faint mono)
    - .planning/phases/02-workspace-core/02-PATTERNS.md (TitleBar.tsx — preserve exactly one drag region)
  </read_first>
  <action>
    Rework `TitleBar.tsx`: keep `LogoCluster` (or its wordmark) at left, then insert `<DiviChip />`, then a corpus label rendering `— {activeCorpus}` (read `useShellStore(s => s.activeCorpus)`, Plex Mono, `--color-faint`), then the flex `.spacer` carrying the SINGLE `data-tauri-drag-region`, then `<RailToggleButtons />`, then `<WindowControls />`. Update `TitleBar.module.css` so height binds `var(--titlebar-h)` (now 40px) and keep `.spacer { flex: 1 1 auto; }`. Update `TitleBar.test.tsx`: keep the "exactly one data-tauri-drag-region on the spacer" assertion and the WindowControls aria-label assertions; add assertions that the DIVI chip renders ("DIVI") and both rail-toggle buttons render with their aria-labels; replace the Phase-1 "·"+"Home" crumb assertions with the DIVI chip + corpus label content (wordmark "Sourcerer" assertion stays). The single-drag-region assertion must still pass with the chip and buttons present.
  </action>
  <verify>
    <automated>npx vitest run src/shell/TitleBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `TitleBar.test.tsx` green: exactly one `data-tauri-drag-region`, three window-control aria-labels, DIVI chip text present, both rail-toggle aria-labels present.
    - `TitleBar.module.css` height binds `var(--titlebar-h)` (40px), spacer keeps `flex: 1 1 auto`.
    - Chip/corpus/toggle order matches UI-SPEC left→right layout.
  </acceptance_criteria>
  <done>TitleBar renders the reworked 40px chrome; test suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Floating rounded window (transparent + backdrop + inner card)</name>
  <files>src-tauri/tauri.conf.json, src/App.tsx, src/App.module.css</files>
  <read_first>
    - src-tauri/tauri.conf.json (target — windows[0] has decorations:false, shadow:false)
    - src/App.tsx (target — current: renders `<AppShell />` only)
    - .planning/phases/02-workspace-core/02-UI-SPEC.md (Chrome Rework "Window frame" row — inset/radius/border/shadow/backdrop values)
    - .planning/phases/02-workspace-core/02-PATTERNS.md (tauri.conf.json — add transparent:true; AppShell.tsx floating-window wrapper note)
    - src/styles/tokens.css (the --window-* vars added in plan 01)
  </read_first>
  <action>
    Add `"transparent": true` to `src-tauri/tauri.conf.json` windows[0] alongside `decorations:false, shadow:false`. In `src/App.tsx`, wrap `<AppShell />` in two layers: an outer full-viewport backdrop div (`styles.backdrop`) painting `var(--window-backdrop)` radial gradient with `var(--window-inset)` (20px) padding, and an inner card div (`styles.card`) with `border-radius: var(--window-radius)` (10px), `border: var(--window-border)`, `box-shadow: var(--window-shadow)`, `overflow: hidden`, `height: 100%`. Create `src/App.module.css` with `.backdrop` and `.card` rules using tokens only. Confirm `html, body, #root` stay `height:100%` (already in tokens.css) so the backdrop fills the transparent OS window. Do NOT change AppShell's internal grid here (rail/dock plans own that).
  </action>
  <verify>
    <automated>node -e "const c=require('./src-tauri/tauri.conf.json'); if(c.app.windows[0].transparent!==true){console.error('transparent not set');process.exit(1)} const css=require('fs').readFileSync('src/App.module.css','utf8'); if(!/--window-radius/.test(css)||!/--window-backdrop/.test(css)){console.error('card/backdrop vars missing');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `tauri.conf.json` windows[0] has `transparent: true` (kept with decorations:false, shadow:false).
    - `App.module.css` `.backdrop` uses `var(--window-backdrop)` + `var(--window-inset)`; `.card` uses `var(--window-radius)`, `var(--window-border)`, `var(--window-shadow)`.
    - `npm run build` (tsc) succeeds.
  </acceptance_criteria>
  <done>Floating window structure in place; verify node gate prints OK; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview ↔ Tauri window IPC | Rail-toggle/window-control buttons and the drag region cross into native window control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | title-bar drag region | mitigate | Preserve EXACTLY one `data-tauri-drag-region` (asserted in TitleBar.test.tsx); no chip/button carries it so clicks fire handlers, not drag |
| T-02-03 | Elevation | `transparent:true` + `decorations:false` window | accept | Local desktop shell, no remote/untrusted web content is loaded into the window; transparency only affects the OS-level backdrop inset |
</threat_model>

<verification>
- `npx vitest run src/shell/TitleBar.test.tsx` green.
- Task 3 node gate prints OK; `npm run build` succeeds.
- Chrome renders under the 40px bar (visually confirmed by plan 03's human-verify).
</verification>

<success_criteria>
- 40px title bar renders wordmark + DIVI chip + corpus label + two rail-toggle buttons + window controls, with exactly one drag region.
- Rail-toggle buttons cycle rail mode via the store.
- Floating transparent window with radial backdrop + 10px inner card is wired and builds.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-core/02-02-SUMMARY.md` when done.
</output>

---
phase: 08-spike-k-nix-native-substrate-service
plan: 05
type: execute
wave: 5
depends_on: [08-04]
files_modified:
  - .planning/spikes/012-nix-native-substrate-service/README.md
  - .planning/spikes/MANIFEST.md
  - .planning/spikes/CONVENTIONS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SPIKE-01]

must_haves:
  truths:
    - "The exemption set is written down so Phase 15's compiler and Phase 19's audit are built on evidence, not guesswork"
    - "A reader with no memory of this session can read the README and know which hardening directives a self-sandboxing service tolerates, and why each concession was made"
    - "Every measured rung has exactly one Investigation Trail row naming directive, failure signature, concession, and reason — no row says only 'it didn't work'"
    - "A timebox stop with a partial exemption set is recorded as a valid verdict, not written up as a failure"
    - "The research corrections are recorded as corrections — D-09's capability prediction, the zero-hardening stock module, and the silent jail-degrade behaviour are all stated as findings a future reader can act on"
    - "Open questions that stayed open are named with the downstream consumer they are deferred to"
  artifacts:
    - path: ".planning/spikes/012-nix-native-substrate-service/README.md"
      provides: "The spike's findings doc — frontmatter, the seven-heading body, the Investigation Trail, the per-rung Results table, package provenance, and caveats"
      contains: "## Investigation Trail"
      min_lines: 90
    - path: ".planning/spikes/MANIFEST.md"
      provides: "Row 012, with validates/verdict kept in sync with the README frontmatter"
      contains: "nix-native-substrate-service"
  key_links:
    - from: ".planning/spikes/012-nix-native-substrate-service/rungs.tsv"
      to: "README ## Investigation Trail"
      via: "one trail row per measured rung — VALIDATION.md's rung-count == trail-row-count gate"
      pattern: "Rung"
    - from: "README frontmatter verdict:"
      to: "MANIFEST.md row 012 Verdict column"
      via: "kept identical, as 010 and 011 both do"
      pattern: "verdict:"
---

<objective>
Turn the run logs into the deliverable: a findings document that a reader with no memory of this
session can use to build Phase 15's manifest-to-unit compiler and Phase 19's
`systemd-analyze security` audit.

Purpose: SC-4 is the only success criterion that is not a measurement. D-10 says the output IS
the deliverable — an exemption set nobody can read is worth nothing, and Phase 15 and Phase 19
are many phases downstream. VALIDATION.md's cold-read row is explicit: no row may say only
"didn't work".

Output: `.planning/spikes/012-nix-native-substrate-service/README.md`, the `012` row in
`MANIFEST.md`, any genuinely new system-level landmine appended to `CONVENTIONS.md`, and the
SPIKE-01 / ROADMAP updates the evidence now supports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Host map:** this plan is pure documentation authoring **in this repo**. No `wsl.exe`, no
guest commands, no rebuilds. Every fact written must come from `run.log`, `rungs.tsv`, or the
plan 01-04 summaries — do not re-run anything to fill a gap, and do not invent a value that is
not in the logs. If a fact is missing, write that it is missing.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-spike-k-nix-native-substrate-service/08-CONTEXT.md
@.planning/phases/08-spike-k-nix-native-substrate-service/08-RESEARCH.md
@.planning/phases/08-spike-k-nix-native-substrate-service/08-PATTERNS.md
@.planning/phases/08-spike-k-nix-native-substrate-service/08-VALIDATION.md
@.planning/phases/08-spike-k-nix-native-substrate-service/08-01-SUMMARY.md
@.planning/phases/08-spike-k-nix-native-substrate-service/08-02-SUMMARY.md
@.planning/phases/08-spike-k-nix-native-substrate-service/08-03-SUMMARY.md
@.planning/phases/08-spike-k-nix-native-substrate-service/08-04-SUMMARY.md
@.planning/spikes/010-nixos-wsl-substrate/README.md
@.planning/spikes/011-tauri-multiwebview/README.md
@.planning/spikes/MANIFEST.md
@.planning/spikes/CONVENTIONS.md

<interfaces>
Verdict vocabulary — the manifest currently uses only `VALIDATED` and `PARKED`. This spike needs
a third value because D-10's timebox clause makes a partial exemption set a legitimate outcome.
Use exactly one of:

  VALIDATED             the ladder reached a pass rung (LADDER_STOP=PASS), the jail was verified
                        real (not degraded relative to the plan 02 control), the declarative
                        pass matched, and WINDOWS_REACH=ok
  PARTIALLY VALIDATED   the ladder stopped on BUDGET or DEADLINE with a recorded partial
                        exemption set, OR the jail could not be verified (PASS-UNVERIFIED),
                        OR WINDOWS_REACH failed for a reason attributed to hardening
  PARTIALLY VALIDATED — WSL2 KERNEL GAP
                        USERNS_OK=no: unprivileged user namespaces are unavailable in this
                        substrate, so coolwsd could never build its jail regardless of
                        hardening. The directive-level findings still stand; the jail-depth
                        question does not.

The chosen value goes in the README frontmatter `verdict:` field AND the MANIFEST row's Verdict
column, identical in both — 010 and 011 both keep these in sync.

README body headings, copied from 010/011 in this exact order:
  # Spike 012: Nix-Native Substrate Service (Container Platform "spike K")
  ## What This Validates
  ## Research
  ## How to Run
  ## What to Expect
  ## Investigation Trail
  ## Package provenance          <- the 012 analogue of 010's "Source image provenance"
  ## Results                     <- opens with a bolded **VERDICT: ...** line, then the tables

MANIFEST.md row format (tab-free markdown table row, matching rows 010/011):
  | 012 | nix-native-substrate-service | standard | <the same Given/When/Then sentence as the
  README frontmatter `validates:`> | <verdict> | collabora-online, systemd-hardening, nixpkgs,
  container-platform |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the spike 012 README — frontmatter, body, Investigation Trail, and the exemption set</name>

  <read_first>
    - `.planning/spikes/012-nix-native-substrate-service/run.log` — the source of every fact in this document
    - `.planning/spikes/012-nix-native-substrate-service/rungs.tsv` — one trail row per data row here
    - `.planning/spikes/010-nixos-wsl-substrate/README.md` — the primary shape: frontmatter field set, heading order, the "Attempt N failed, hypothesis X -> Attempt N+1 confirmed" trail form, the Results table, the provenance callout, and the "Product lessons" close
    - `.planning/spikes/011-tauri-multiwebview/README.md` — the "Caveats (open, not blocking)" subsection form, one bullet per caveat naming the downstream consumer
    - `.planning/phases/08-spike-k-nix-native-substrate-service/08-PATTERNS.md` — "Pattern Assignments -> README.md", which specifies every one of these forms concretely
  </read_first>

  <action>
Create `.planning/spikes/012-nix-native-substrate-service/README.md`.

Frontmatter: `spike: 012`, `name: nix-native-substrate-service`, `type: standard`, a single
Given/When/Then `validates:` sentence mirroring D-09's kill-question (given `services.collabora-online`
from nixpkgs inside the NixOS-WSL substrate, when the full §7 hardening baseline is applied and
then relaxed one directive at a time, then the surviving exemption set is recorded with the log
signature that forced each concession, and the UI is reachable on 127.0.0.1 from Windows),
`verdict:` set from the vocabulary in `<interfaces>`, `related: [010-nixos-wsl-substrate]`, and
`tags: [collabora-online, systemd-hardening, nixpkgs, container-platform, spike-K]`.

`## What This Validates` — the kill-question and why it moved. State plainly that this phase was
rescoped from "engine-less OCI unit" on 2026-08-04 by the Nix-native decision (D-01/D-02), and
that the question under test is whether a service that sandboxes itself survives the §7 baseline
under WSL2's kernel.

`## Research` — three things a cold reader needs and cannot infer:
  1. The nixpkgs `services.collabora-online` module ships ZERO hardening — quote the stock
     `serviceConfig` captured in plan 02. There was no hardened starting point to relax; the
     baseline was constructed by this spike from CONTAINER-PLATFORM.md §7.
  2. The nixpkgs build passes `--disable-setcap`, forcing `coolwsd` onto its unprivileged
     user+mount-namespace jailing path, so it should need no OUTER capabilities. State whether
     the ladder confirmed or refuted this, and therefore whether D-09's `CapabilityBoundingSet`
     prediction held. If D-09 was wrong, say so directly — that correction is exactly what
     Phase 15 needs.
  3. `coolwsd` silently degrades: it logs `"Security warning: running without chroot jails is
     insecure"` and keeps serving jail-less. Explain that this is why every rung's verdict
     cross-checks the jail state and why a green `curl` was never accepted as a pass.
  Also record the `RestrictNamespaces` addition (RESEARCH Assumption A2) as a spike-discovered
  member of the baseline that CONTAINER-PLATFORM.md never names, and say whether it turned out
  to be load-bearing.

`## How to Run` — the leg selector (`./run.sh preflight|stock|ladder|declarative`), the resume
form (`RUNG_START=<n> ./run.sh ladder`), the knobs (`RUNG_BUDGET`, `LADDER_DEADLINE_MIN`), and
the cleanup command `wsl --unregister SourcererSpike` stated but explicitly not run — the distro
is left registered and warm for Phase 10 per CONTEXT's discretion default. State the final
registration state as fact, from `DISTRO_FINAL_STATE` in `run.log`.

`## What to Expect` — the userns pre-flight verdict, the rebuild timings actually measured, the
approximate per-rung cost, and where the ladder stopped.

`## Investigation Trail` — the load-bearing section. Two parts:
  (a) Timestamped bullets in 010's form (**timestamp** -> **what failed, verbatim error string**
      -> **root cause** -> **product lesson tied to a specific downstream phase**) for the
      non-rung events: the distro triage and any re-import, the WSL version outcome, the userns
      pre-flight, the substitution proof, the stock control measurement, the drop-in teardown,
      and the declarative pass.
  (b) One row per measured rung, sourced 1:1 from `rungs.tsv`, in the table shape PATTERNS.md
      specifies: `| Rung | Directive relaxed | Signature observed | Concession | Result |`.
      The Signature cell carries the verbatim log line (including the `src=systemd` /
      `src=coolwsd` seccomp attribution where present), and the Concession cell carries the
      REASON, not a restatement of the directive. Per RESEARCH's anti-patterns and
      VALIDATION.md's manual row, no cell may say only "didn't work". The number of rung rows
      MUST equal the number of data rows in `rungs.tsv`.

`## Package provenance` — 010's provenance-callout form applied to the package rather than the
image: the resolved `/nix/store/...-collabora-online-<version>` path, the version, the closure
size, the nixpkgs channel and `nixos-version` revision, and whether it matched D-07's recorded
store path. Also record the substrate: NixOS-WSL 2605.7.2, sha256
`e7180ad555fdcb8e1e057e2ef056de467603a5e502ff8531053738371be3f6b9`, and the kernel string.

`## Results` — open with a bolded `**VERDICT: <value>**` line, then:
  - The four success criteria as a table with the evidence for each (substitutes-from-cache;
    baseline-first-then-relaxed; reachable from Windows; exemption set recorded).
  - **The exemption set itself**, as the verbatim `systemd.services.coolwsd.serviceConfig` Nix
    block that plan 04 generated and proved. This is the single most-consumed artifact in the
    document — put it under its own bold label so a Phase 15 reader can find it without reading
    the trail.
  - A per-directive disposition table: for every one of the seventeen baseline directives, one
    row saying KEPT / RELAXED-TO / ALREADY-SATISFIED / MASKED, with the reason. `StateDirectory`
    is ALREADY-SATISFIED, not a relaxation. `ProtectSystem` may be MASKED by `DynamicUser`.
  - The `systemd-analyze security` exposure score for the final unit, with the caveat that the
    score reflects declared hardening and says nothing about whether coolwsd degraded its own
    jail.
  - `coolwsd-systemplate-setup` recorded as an out-of-baseline setup-tier oneshot, with the
    reason and the Phase 15 open question it raises.
  - A leg-timing table in 010's shape (`| Leg | Result | Time |`).
  - `**Product lessons (feed Phase 15 / Phase 19 planning):**` — numbered, each naming the
    downstream phase it feeds, in 010's closing form.
  - `**Caveats (open, not blocking):**` — 011's form, one bullet per Open Question still open,
    each naming the phase it is deferred to. Candidates: socket activation if rung 4a was
    inconclusive; the jail-depth question if `USERNS_OK=no`; `IPAddressDeny`/`CPUQuota` if the
    WSL2 cgroup path could not enforce them; whether `confinement.enable` (the NixOS-only
    mechanism §7 names) would have been a better baseline than the directive ladder — that was a
    documented stretch goal, and saying it was not attempted is a legitimate, useful caveat.

Write every number from the logs. Where a log value is missing, write "not measured" and why —
never a plausible-looking invented figure.
  </action>

  <verify>
    <automated>cd .planning/spikes/012-nix-native-substrate-service && test -f README.md && grep -q '^spike: 012$' README.md && grep -q '^name: nix-native-substrate-service$' README.md && grep -q '^## Investigation Trail$' README.md && grep -q '^## Package provenance$' README.md && grep -q '^## Results$' README.md && grep -q 'VERDICT:' README.md && grep -q 'systemd.services.coolwsd.serviceConfig' README.md && test "$(awk -F'\t' 'NR>1' rungs.tsv | wc -l)" -le "$(grep -c '^| [0-9]' README.md)" && echo PLAN05_TASK1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `README.md` frontmatter has all six fields: `spike`, `name`, `type`, `validates`, `verdict`, `related`, `tags`
    - `verdict:` is exactly one of `VALIDATED`, `PARTIALLY VALIDATED`, or `PARTIALLY VALIDATED — WSL2 KERNEL GAP`
    - All seven body headings are present in the documented order
    - The Investigation Trail's rung table has at least as many data rows as `rungs.tsv` has data rows — assert `awk -F'\t' 'NR>1' rungs.tsv | wc -l` <= the rung-table row count
    - No trail cell contains only `didn't work`, `failed`, `n/a`, or an empty string in the Signature or Concession column — `grep -Ec '\| *(didn.t work|failed|n/a|) *\|' README.md` counts zero in the rung table
    - The exemption set appears as a verbatim `systemd.services.coolwsd.serviceConfig` Nix block under its own bold label
    - The per-directive disposition table covers all seventeen baseline directives — `grep -o -E 'DynamicUser|ProtectSystem|ProtectHome|NoNewPrivileges|CapabilityBoundingSet|AmbientCapabilities|RestrictNamespaces|SystemCallArchitectures|SystemCallFilter|PrivateNetwork|IPAddressDeny|PrivateDevices|MemoryMax|CPUQuota|TasksMax|StateDirectory|User' README.md | sort -u | wc -l` is 17
    - `StateDirectory` is marked ALREADY-SATISFIED, not RELAXED
    - The document states explicitly whether D-09's `CapabilityBoundingSet` prediction held
    - The document states explicitly whether `RestrictNamespaces` (the A2 spike addition) was load-bearing
    - `coolwsd-systemplate-setup` is named with its out-of-baseline reason and the Phase 15 question
    - A `systemd-analyze security` score is quoted with its declared-vs-actual caveat
    - `**Caveats (open, not blocking):**` exists with at least one bullet, each bullet naming a downstream phase
    - The store path, version, closure size, channel, `nixos-version`, kernel, and the image sha256 `e7180ad555fdcb8e1e057e2ef056de467603a5e502ff8531053738371be3f6b9` all appear under `## Package provenance`
    - Every numeric claim traces to a `run.log` line; any unmeasured value reads `not measured` with a reason
  </acceptance_criteria>

  <done>The README is a self-contained findings document: the exemption set is quoted verbatim in its declarative form, every measured rung has a trail row naming directive/signature/concession/reason, the research corrections are stated as corrections, and the open questions name the phase that inherits them.</done>
</task>

<task type="auto">
  <name>Task 2: Update the spike index, conventions, and the downstream project docs</name>

  <read_first>
    - `.planning/spikes/MANIFEST.md` — the file being modified; the existing table rows 010 and 011 are the exact format to match
    - `.planning/spikes/CONVENTIONS.md` — the file being conditionally modified; the "System-level spikes (Windows host, established in 010/011)" section is the ONLY section that may be touched
    - `.planning/REQUIREMENTS.md` — the file being modified; the SPIKE-01 line (line 9) and its status row (line 119)
    - `.planning/ROADMAP.md` — the file being modified; the Phase 8 entry, specifically its `**Notes**:` line
    - `.planning/spikes/012-nix-native-substrate-service/README.md` — the source of the `validates:` sentence and `verdict:` value, which must be reproduced identically
  </read_first>

  <action>
Four edits, all in this repo, all sourced from the README written in task 1.

1. `.planning/spikes/MANIFEST.md` — append one row after row 011, matching the existing column
   format exactly: `| 012 | nix-native-substrate-service | standard | <validates sentence> |
   <verdict> | collabora-online, systemd-hardening, nixpkgs, container-platform |`. The
   `Validates` cell must be the SAME sentence as the README frontmatter's `validates:` field,
   not a paraphrase — 010 and 011 both keep these identical, and a drift here is what makes an
   index useless. Same for `Verdict`. Do not touch MANIFEST.md's `## Requirements` section; it
   is a project-wide list and this spike surfaces nothing cross-cutting for it.

2. `.planning/spikes/CONVENTIONS.md` — CONDITIONAL. Append at most one new bullet to the
   existing "System-level spikes (Windows host, established in 010/011)" section, and only if
   the spike surfaced a genuinely new system-level landmine that the five existing bullets do
   not already cover. Candidates that would qualify: a WSL2-specific sysctl or cgroup behaviour
   that bit the run; a `nixos-rebuild` non-interactive quirk; a systemd drop-in interaction with
   `nixos-rebuild` that a future spike would repeat. Do NOT restate the existing five bullets,
   do NOT create a new heading, and do NOT touch the `## Stack` / `## Structure` / `## Patterns`
   / `## Tools & Libraries` sections above it — those govern the 001-005 harness spikes and are
   out of scope. If nothing new was learned, make no edit at all and say so in the summary.

3. `.planning/REQUIREMENTS.md` — update SPIKE-01. Tick the checkbox to `[x]` only when the
   README verdict is `VALIDATED`. When the verdict is either `PARTIALLY VALIDATED` form, leave
   the box unticked and append a bracketed status clause to the requirement text naming what was
   and was not proven, plus the spike path. Update the status-table row (currently
   `| SPIKE-01 | Phase 8 | P0 | Windows box | Pending |`) to `Validated` or
   `Partially validated` to match. Do not restate the whole requirement; edit in place.

4. `.planning/ROADMAP.md` — update the Phase 8 entry's `**Notes**:` line with the spike's actual
   findings, in two or three sentences: whether D-09's capability prediction held, what the
   exemption set turned out to be at a headline level, and the `systemd-analyze security` score
   Phase 19 will be auditing against. Add a pointer to
   `.planning/spikes/012-nix-native-substrate-service/README.md`. Leave the Goal, Depends on,
   Execution host, Requirements and Success Criteria lines alone — they were the plan, and the
   Notes line is where the outcome belongs. Also check the Phase 15 entry's `**Notes**:` line:
   it already promises "the baseline and its exemption set come from Phase 8's recorded
   evidence", so if the ladder produced a result that changes what Phase 15's compiler must
   handle (for example a required setup-oneshot tier, or a directive that cannot be enforced
   under WSL2 at all), add one clause there naming it.

Do not mark the phase complete, do not edit STATE.md, and do not touch PROJECT.md's Key
Decisions table — D-01/D-02 are already recorded there and this spike did not change them.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q '^| 012 | nix-native-substrate-service | standard |' .planning/spikes/MANIFEST.md && grep -q '012-nix-native-substrate-service' .planning/ROADMAP.md && grep -Eq 'SPIKE-01.*(Validated|Partially validated)' .planning/REQUIREMENTS.md && test "$(grep -c '^## ' .planning/spikes/CONVENTIONS.md)" -eq 5 && echo PLAN05_TASK2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `MANIFEST.md` has exactly one `| 012 |` row, placed after the `| 011 |` row, with six pipe-delimited cells matching the header
    - The MANIFEST `Validates` cell string is byte-identical to the README frontmatter `validates:` value (compare with a diff of the two extracted strings)
    - The MANIFEST `Verdict` cell string is identical to the README frontmatter `verdict:` value
    - `CONVENTIONS.md` still has exactly five `## ` headings — no new heading was created
    - `CONVENTIONS.md`'s "System-level spikes" section grew by at most one bullet, and its existing five bullets are byte-unchanged (`git diff` shows only additions in that section, or no diff at all)
    - `REQUIREMENTS.md` SPIKE-01's status-table cell reads `Validated` or `Partially validated`, never still `Pending`
    - `REQUIREMENTS.md` SPIKE-01's checkbox is `[x]` if and only if the README verdict is exactly `VALIDATED`
    - `ROADMAP.md` Phase 8 `**Notes**:` names the D-09 outcome, the headline exemption set, and the `systemd-analyze security` score, and links `.planning/spikes/012-nix-native-substrate-service/README.md`
    - `ROADMAP.md` Phase 8's Goal / Depends on / Execution host / Requirements / Success Criteria lines are unchanged (`git diff` shows no edits to them)
    - `.planning/STATE.md` and `.planning/PROJECT.md` are unmodified: `git status --porcelain .planning/STATE.md .planning/PROJECT.md` prints nothing
    - No file under `src/` or `src-tauri/` changed
  </acceptance_criteria>

  <done>The spike index carries row 012 with its verdict in sync with the README, any genuinely new system landmine is one appended bullet in the right section, SPIKE-01's status reflects the actual verdict, and ROADMAP's Phase 8 and Phase 15 notes carry the findings forward to the phases that consume them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cold-read the findings and confirm they are usable by a reader with no memory of this session</name>

  <read_first>
    - `.planning/spikes/012-nix-native-substrate-service/README.md` — the document under review
    - `.planning/phases/08-spike-k-nix-native-substrate-service/08-VALIDATION.md` — "Manual-Only Verifications" rows 2 and 3, which define this check
  </read_first>

  <what-built>
    The spike 012 findings document with the exemption set quoted in its proven declarative
    form, a per-rung Investigation Trail, a per-directive disposition table, package provenance,
    the `systemd-analyze security` score, product lessons, and open caveats — plus the MANIFEST
    row, the conditional CONVENTIONS bullet, and the SPIKE-01 / ROADMAP updates.
  </what-built>

  <how-to-verify>
    1. Read `.planning/spikes/012-nix-native-substrate-service/README.md` COLD — deliberately
       without referring to `run.log`, `rungs.tsv`, or this session's context. That is the exact
       position Phase 15's and Phase 19's implementers will be in.
    2. Answer these four questions from the README alone. If any cannot be answered, the
       document is not done:
       - Which hardening directives can a self-sandboxing service tolerate, and which had to be
         given up?
       - For each concession, WHY — what specific log signature forced it?
       - Is the recorded jail real, or did coolwsd silently degrade?
       - What would Phase 19's `systemd-analyze security` audit score this unit, and what is
         that number's limitation?
    3. Check the Investigation Trail: every rung row must name a directive, a real signature, a
       concession, and a reason. A row saying only "didn't work" or with a blank signature is a
       defect — send it back.
    4. Confirm the verdict is honest. If the ladder stopped on `BUDGET` or `DEADLINE`, the
       document should say `PARTIALLY VALIDATED` and treat the partial exemption set as the
       result — it should NOT be written up as a failure, and it should NOT be dressed up as a
       full pass.
    5. Confirm the MANIFEST `Validates` sentence and `Verdict` match the README frontmatter word
       for word.
    6. Confirm `wsl.exe -l -v` still lists `SourcererSpike` and the README says so.
    7. Confirm `git status --porcelain src src-tauri` prints nothing.
    8. **Next step, not part of this check:** per CONTEXT's discretion default, package these
       findings via `/gsd-spike --wrap-up` so Phase 15 and Phase 19 can load them as a skill —
       `spike-findings-sourcerer` is the existing precedent for that output form. Say whether
       you want that done.
  </how-to-verify>

  <acceptance_criteria>
    - All four cold-read questions in step 2 are answerable from `README.md` alone, and the reviewer states each answer
    - Every rung row in the Investigation Trail names directive, signature, concession, and reason — no row is blank or reads only "didn't work"
    - The rung-row count is at least the data-row count of `rungs.tsv`
    - The verdict matches the recorded `LADDER_STOP`, `DECLARATIVE_MATCH`, `WINDOWS_REACH` and jail state — a `BUDGET`/`DEADLINE` stop is written up as `PARTIALLY VALIDATED`, not as a failure and not as a full pass
    - The MANIFEST `Validates` and `Verdict` cells are byte-identical to the README frontmatter values
    - `README.md` records the final distro state and `wsl.exe -l -v` agrees
    - No file under `src/` or `src-tauri/` changed
  </acceptance_criteria>

  <resume-signal>Type "approved" plus the four cold-read answers in one line each, and say whether to run `/gsd-spike --wrap-up`. Or describe what a cold reader could not answer.</resume-signal>
</task>

</tasks>

<verification>
Maps onto `08-VALIDATION.md` Per-Task Verification Map rows:

| VALIDATION.md row | Task | Command |
|---|---|---|
| Each rung's relaxation recorded with directive + reason + observed signature | 08-05-01 | Investigation Trail row count >= `rungs.tsv` data row count; no blank/`didn't work` signature or concession cell |
| Exemption set written down, evidence not guesswork (SPIKE-01d) | 08-05-01, 08-05-03 | artifact check — the verbatim `systemd.services.coolwsd.serviceConfig` block plus the per-directive disposition table |
| Exemption set legible to a cold reader (Manual-Only row 2) | 08-05-03 | manual — the four cold-read questions |
| Jail degradation did not go unnoticed (Manual-Only row 3) | 08-05-01, 08-05-03 | every rung row shows jail state explicitly, not just service-active |
</verification>

<success_criteria>
- `.planning/spikes/012-nix-native-substrate-service/README.md` follows 010/011's frontmatter and heading form exactly
- The exemption set is quoted verbatim in the declarative `systemd.services.coolwsd.serviceConfig` form plan 04 proved
- Every measured rung has exactly one Investigation Trail row naming directive, verbatim signature, concession, and reason
- The per-directive disposition table covers all seventeen baseline directives with KEPT / RELAXED-TO / ALREADY-SATISFIED / MASKED
- The research corrections are recorded as corrections — the zero-hardening stock module, whether D-09 held, the silent jail-degrade, and whether `RestrictNamespaces` was load-bearing
- A `BUDGET`/`DEADLINE` stop is written up as `PARTIALLY VALIDATED` with the partial exemption set as the result, not as a failure
- `MANIFEST.md` row 012 is in sync with the README frontmatter, `CONVENTIONS.md` gained at most one bullet in the right section, and SPIKE-01 / ROADMAP reflect the actual verdict
- A cold reader can answer all four questions from the README alone
- Zero application-tree files changed
</success_criteria>

<output>
Create `.planning/phases/08-spike-k-nix-native-substrate-service/08-05-SUMMARY.md` when done.
Next step after this plan, per CONTEXT's discretion default: run `/gsd-spike --wrap-up` to
package the findings as a `spike-findings-*` skill for Phase 15 and Phase 19.
</output>

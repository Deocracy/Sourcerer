---
phase: 05-notes-applet
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/applets/Notes/index.tsx
  - src/applets/Notes/Notes.module.css
  - src/applets/Notes/Notes.test.tsx
autonomous: true
requirements: [NOTE-02]
must_haves:
  truths:
    - "User can click Summarize on the selected note and receive a real AI completion rendered inline below the toolbar"
    - "While in flight the button reads 'Summarizing…' and is disabled"
    - "The summary is ephemeral — it clears when the user switches notes and is never persisted to host.storage"
    - "A summarize failure renders the honest-degrade copy 'Couldn't summarize this note.' / 'Check your connection and try again.' — one error, never a hang"
    - "The full registry → host → storage → ai loop is proven end-to-end by one real applet"
  artifacts:
    - path: "src/applets/Notes/index.tsx"
      provides: "Summarize handler calling host.ai(prompt) + inline ephemeral result + error state"
      contains: "host.ai"
    - path: "src/applets/Notes/Notes.test.tsx"
      provides: "mocked-host tests for summarize success + summarize error"
    - path: "src/applets/Notes/Notes.module.css"
      provides: "muted serif-italic summary block styling (tokens.css only)"
  key_links:
    - from: "src/applets/Notes/index.tsx"
      to: "host.ai"
      via: "await host.ai(`Summarize this note...`) in a try/catch"
      pattern: "host\\.ai\\("
---

<objective>
Add the AI Summarize action to Notes: a "Summarize" button that calls `host.ai(prompt)` and renders
the real completion inline as a muted serif-italic block, ephemeral (not persisted, D-03), with an
honest-degrade error state. This completes the NOTE-02 slice and closes the full framework loop —
registry → host → storage → **ai** — through one real applet.

Purpose: Prove Notes is `host.ai()`'s first real consumer. Per Phase 4 D-01 (superseding the
roadmap's "stub response in v1"), `host.ai()` routes to the live Pi sidecar, so Summarize returns a
genuine completion and must handle real latency/errors.

Output: A working Summarize action wired to the existing `host.ai()` seam — no new host surface, no
streaming machinery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** researcher, **I want to** invoke AI summarize on a note and see the result inline,
**so that** the note applet proves the full registry → host → storage → ai loop works end-to-end.

<context>
@.planning/phases/05-notes-applet/05-CONTEXT.md
@.planning/phases/05-notes-applet/05-UI-SPEC.md
@.planning/phases/05-notes-applet/05-RESEARCH.md
@.planning/phases/05-notes-applet/05-01-SUMMARY.md

<interfaces>
<!-- host.ai contract (src/host/aiComplete.ts, src/host/types.ts). Full excerpts in read_first. -->
host.ai(prompt: string, opts?: { onDelta?: (text: string) => void }): Promise<string>
- Resolves with the completion string; rejects with a plain Error(message) on an in-band error
  event OR the 120s inactivity watchdog (Phase 7, shipped). One try/catch covers every failure mode.
- Skip onDelta for v1 (RESEARCH Open Question 2) — await the Promise directly; UI-SPEC needs only a
  two-state loading/done interaction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author failing NOTE-02 tests — summarize success + summarize error (RED)</name>
  <files>src/applets/Notes/Notes.test.tsx</files>
  <read_first>
    - src/applets/Notes/Notes.test.tsx (current NOTE-01 tests from Plan 05-01 — extend, keep makeStubHost)
    - src/applets/Library/Library.test.tsx (mocked-host override-per-test idiom)
    - src/host/aiComplete.ts (the Promise contract: resolves with string, rejects with Error)
    - .planning/phases/05-notes-applet/05-RESEARCH.md (Validation Architecture → -t "summarize", -t "summarize error")
    - .planning/phases/05-notes-applet/05-UI-SPEC.md (Copywriting: AI action label + error copy)
  </read_first>
  <action>
    Extend src/applets/Notes/Notes.test.tsx with two tests whose names include the substrings
    "summarize" and "summarize error". Seed the stub host with at least one note (via storage.get)
    and select it. (a) Success: override the stub host's `ai` to an `async () => "A concise summary."`
    (also assert it was called — make `ai` a `vi.fn()` wrapper), click the "Summarize" button, await,
    and assert the returned summary text renders inline and host.ai was called with a prompt string
    containing the note's title/body. (b) Error: override `ai` to `async () => { throw new Error("boom"); }`,
    click Summarize, await, and assert the honest-degrade copy "Couldn't summarize this note." renders
    and no unhandled rejection/hang occurs. Use `findByText`/`await` for the async resolution. These
    tests MUST fail now because Plan 05-01's index.tsx has no Summarize action yet.
  </action>
  <verify>
    <automated>npx vitest run src/applets/Notes/Notes.test.tsx -t "summarize"</automated>
  </verify>
  <acceptance_criteria>
    - Notes.test.tsx defines tests matching `-t "summarize"` and `-t "summarize error"`
    - `npx vitest run src/applets/Notes/Notes.test.tsx -t "summarize"` exits non-zero (RED) — the Summarize control does not yet exist
    - The NOTE-01 tests from Plan 05-01 still pass (extension did not break them)
  </acceptance_criteria>
  <done>NOTE-02 tests present and failing for the right reason (Summarize not yet implemented).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Summarize action — host.ai() call, inline ephemeral result, honest-degrade error (GREEN)</name>
  <files>src/applets/Notes/index.tsx, src/applets/Notes/Notes.module.css</files>
  <read_first>
    - src/applets/Notes/index.tsx (current editor toolbar from Plan 05-01 — add the Summarize button beside delete)
    - src/host/aiComplete.ts (host.ai Promise+watchdog contract — single try/catch)
    - src/applets/Library/index.tsx (async-in-component + local state precedent)
    - .planning/phases/05-notes-applet/05-UI-SPEC.md (AI action label states; result = muted serif-italic block; error copy)
    - src/styles/tokens.css (--font-serif, --color-muted/--color-faint, --fs-body — zero new tokens)
    - .planning/phases/05-notes-applet/05-CONTEXT.md (D-03 ephemeral)
  </read_first>
  <behavior>
    - Clicking "Summarize" sets a `summarizing` state (button label → "Summarizing…", disabled) and
      calls `await host.ai("Summarize this note in 1-2 sentences:\n\n" + title + "\n\n" + body)`.
    - On resolve: the completion renders inline below the toolbar as a muted serif-italic block.
    - On reject (in-band error OR 120s watchdog): a `summarizeError` state renders "Couldn't summarize
      this note." + "Check your connection and try again." — exactly one error, never a hang.
    - Switching the selected note clears `summary` and `summarizeError` (D-03 ephemeral — never
      written to host.storage); unmount handles tab close.
  </behavior>
  <action>
    In src/applets/Notes/index.tsx add local state `summary: string | null`, `summarizing: boolean`,
    `summarizeError: boolean`. Add a "Summarize" button (accent) to the editor toolbar next to the
    delete control. Implement an async `handleSummarize(note)` that sets summarizing true, clears
    summarizeError, then `try { setSummary(await host.ai(prompt)) } catch { setSummarizeError(true) }
    finally { setSummarizing(false) }` — prompt = `Summarize this note in 1-2 sentences:` followed by
    the note title and body (discretionary text per CONTEXT; keep it a single instruction+content
    string). Do NOT use onDelta (RESEARCH Open Q2 — await the Promise). While summarizing, the button
    label reads "Summarizing…" and is disabled. Render the resolved summary inline below the toolbar
    as a muted serif-italic block; render the error copy block on summarizeError. In the existing
    `selectNote` handler from Plan 05-01, also reset summary + summarizeError to their initial values
    so the summary is ephemeral and never leaks across notes (D-03). Never call host.storage.set for
    the summary.

    Add the muted serif-italic summary-block class to Notes.module.css using only tokens.css
    properties (--font-serif, --color-muted or --color-faint, --fs-body, font-style italic).
  </action>
  <verify>
    <automated>npx vitest run src/applets/Notes/Notes.test.tsx -t "summarize" && npx vitest run src/applets/Notes && npx vitest run src/applets/boundary.test.ts</automated>
    <human-check>Build+launch sourcerer.exe detached with the Pi sidecar running; open Notes, write a note, click Summarize → a real completion renders inline within ~seconds; switch notes → summary disappears (ephemeral); stop the sidecar and Summarize again → "Couldn't summarize this note." renders without hanging.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/applets/Notes/Notes.test.tsx -t "summarize"` passes (both success + error cases)
    - `npx vitest run src/applets/Notes` full Notes suite green
    - `npx vitest run src/applets/boundary.test.ts` green (still host-only; host.ai is the AI seam)
    - index.tsx calls `host.ai(` inside a try/catch and never calls invoke("host_ai") directly
    - index.tsx never persists the summary (no host.storage.set carrying summary text)
    - selectNote resets summary + summarizeError (grep: summary reset in the selection handler)
    - Notes.module.css summary block uses `var(--font-serif)` + italic, no new tokens
  </acceptance_criteria>
  <done>Summarize works against the live host.ai() seam, ephemeral inline result + honest-degrade error; NOTE-02 satisfied and the full framework loop proven.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/applets/Notes` — all Notes tests green (create/edit/delete + summarize success/error)
- `npx vitest run src/applets/boundary.test.ts` and `npx vitest run src/host/index.test.ts` — seam invariants intact
- `npx vitest run` — full suite green, no regressions
- Manual (end-of-phase): live Summarize returns a real completion; ephemeral on note switch; honest-degrade when the sidecar is down
</verification>

<success_criteria>
- User can invoke Summarize and receive a real host.ai() completion inline
- Summary is ephemeral (cleared on note switch, never persisted)
- Summarize failure shows honest-degrade copy without hanging
- The registry → host → storage → ai loop is proven end-to-end by Notes
</success_criteria>

<output>
Create `.planning/phases/05-notes-applet/05-02-SUMMARY.md` when done.
</output>
